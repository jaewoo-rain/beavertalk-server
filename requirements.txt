sqlalchemy>=2.0,<2.1
alembic>=1.13
psycopg2-binary>=2.9
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic[email]>=2.6
pydantic-settings>=2.2
python-dotenv>=1.0
bcrypt>=4.0
pyjwt[crypto]>=2.8
python-multipart>=0.0.9
httpx>=0.27
h2>=4          # httpx HTTP/2 (APNs VoIP 발송 — core/apns.py)
# normalcall: Gemini Live(실시간 음성) + 분석(generateContent) + 표현 TTS(Gemini-TTS). websockets 를 끌어온다.
google-genai>=1.0
# normalcall: 표현 TTS = Google Cloud Text-to-Speech(Chirp3-HD). Vertex(빌린 프로젝트)는
# Cloud TTS 를 못 켜므로 우리 소유 프로젝트 서비스계정 키(tts_key.json)로 직접 호출.
google-cloud-texttospeech>=2.16
# 오디오 저장: GCS 단일 비공개 버킷(통화 원본/TTS/연습 녹음). 미설정/자격증명 부재면 voice_url=None.
google-cloud-storage>=2.16
# 발음 챌린지 서버 STT: Speech-to-Text 스트리밍(같은 GCP SA 재사용). STT_FAKE/키부재면 미사용.
google-cloud-speech>=2.27
# 인증 주체(Supabase GoTrue) 토큰 검증용 클라이언트. Storage 는 GCS 로 이전됨.
# 신형 시크릿 키(sb_secret_…) 지원 버전으로 하한 고정(로컬 검증 2.31). 머지 후 deps 재해석
# 으로 구버전이 깔려 create_client 가 sb_secret_ 를 거부하는 드리프트를 방지.
supabase>=2.31,<3
# 예약전화: FCM data-only 발송(firebase-admin). 서비스계정 미설정이면 발송만 비활성.
firebase-admin>=6.5,<7
