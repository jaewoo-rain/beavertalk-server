# 09-06 마감 — 어디까지 했고 내일 뭐부터 하나

작업일시 2026-09-06 20:10 / 브랜치 `dev` (푸시 완료, 작업트리 깨끗)
선행: `_1030_통화1310-전사와-QA관찰.md` · `_1203_이어하기-QA1일차-*.md` · `_1820_재접지-*.md`

> **내일 첫 할 일: 통화 1건.** 서버 3건(A·D·B)이 배포까지 끝나 있고 **아직 한 번도 안 재봤다.**

## 1. 오늘 닫은 것

### ✅ 안드로이드 버벅임 — 해결 확인 (08-23부터 미검증이던 건)
클라 `a167e3d`(flutter_sound → record). 통화 **1324**(18:03) 실측:

| 시점 | 1310 수정 전 | 1324 수정 후 |
|---|---|---|
| 50s | 105 | 9 |
| 150s | 436 | 3 |
| **300s** | **1688** | **46** |

5초 이후 창 59개 전부 100ms 미만(최대 49 · 평균 15.6). **선형 증가가 사라졌다.**
⚠ 남은 것 하나 — **5초 지점 598ms**(그 창 `q=172800`). 시작 1회성이고 지속 안 되지만
  오전(81ms)보다 높다. **새로 생긴 자리**다.

### ✅ 재접지 — 원인 규명 + 수정 3개 배포
`e84b04c`(A) + `690241c`(D+B) → 리비전 **00272-9xs** (20:08, 트래픽 100%, /health ok).

- **A** arm 을 바닥 기준으로. 옛 조건은 절대값이라 **통화 +8초에 참**이었다(바닥 7,194 > 6,800).
- **D** 최소 간격 60 → 150초. 5분 5~6회 → **2회**. 끊는 횟수를 줄인다.
- **B** 프롬프트엔 예비 5개만(카드는 30 유지). 지시문 ≈1,200토큰 ↓ = 턴당 원가 ~10% ↓.

⭐ **오늘 안 새 사실**: 압축이 8,000 에서 안 돈다. 실측 **15,000~15,780 → 7,100~7,690**.
  트리거는 "넘으면 다음 **턴 경계**에서 자른다"인데 오디오 턴 하나가 ~7,600 토큰이라
  두 배로 오버슛한다. 증가율 **93 tok/s**. ⇒ A 는 첫 주입을 +8초→+30초로 미룰 뿐이고,
  B 도 압축 주기를 86s→99s 로 늘릴 뿐이다. **끊는 횟수를 직접 조인 D 가 오늘의 본체다.**

## 2. 내일 첫 할 일 — 통화 1건

앱 재설치 불필요(전부 서버 변경). 폰엔 `a167e3d` 빌드가 이미 깔려 있다(17:56, versionName 1.0.0).

로그는 `.env` 가 이 PC 에 없어 DB 직결(`dev_inspect_call.py`)이 안 된다. **Cloud Run 로그**로 뽑는다:
```
SINCE=$(date -u -d '2 hours ago' +%Y-%m-%dT%H:%M:%SZ)
gcloud logging read "resource.type=\"cloud_run_revision\" AND \
  resource.labels.service_name=\"beavertalk-app-demo-api\" AND timestamp>=\"$SINCE\"" \
  --project bt-dev-web-01 --limit 5000 --order=asc --format="value(timestamp, textPayload)"
```
⚠ `--freshness` 는 `--order=asc` 와 같이 쓰면 **무시된다**(8월 로그가 나온다). timestamp 필터를 써라.

볼 것 — arm 로그에 `바닥=/대화=` 를 새로 찍게 해뒀다:
| 지표 | 1324(전) | 기대 |
|---|---|---|
| 바닥 | 7,194 | ~6,000 (B) |
| arm 횟수 | 5 | **2** (D) |
| 첫 arm | +8초 | +30초 이후 (A) |
| 압축 | 6회 | 5회 안팎(거의 그대로 — 예상된 것) |
| 턴당 prompt | 10,476 | ~9,300 (B) |

체감 확인: **첫인사 재발(QA #1)** · **답하는 도중 점프(QA #2)**.

## 3. 아직 안 한 것

- **C(트리거 8,000 → 11,000)** — 보류. "8,000 유지" 지시가 있었다.
  ⭐ 다만 그 근거였던 비용 논리가 실측과 어긋난다: 턴당 10,476 중 **바닥 7,194(69%)는
  압축해도 안 준다**(매 턴 다시 실림). 압축이 잘라내는 건 나머지 31% 뿐인데, 그 대가로
  5분에 압축 6회(비버가 앞부분을 여섯 번 잊음)를 샀다. **사장님께 올려볼 만한 건이다.**
- **QA #5 감정 클립 4번 재생** — 클라. `9748eae` 회귀. 오늘 안 고쳐졌다(추정 — diff 미확인).
- **예비 5개가 15분(이어하기)에서 모자라는지** — 안 쟀다.
- **5초 지점 598ms** — 원인 미규명.

## 4. ⛔ QA 끝나면 되돌릴 것 (demo-api env)
CI 는 `--image` 만 바꾼다 ⇒ 이 env 들은 **이후 자동배포에도 계속 남는다.**

| 변수 | 현재 | 되돌릴 값 |
|---|---|---|
| `USE_VERTEX` | `false` | `true` |
| `GEMINI_API_KEY` | app-api 와 같은 값 | 제거 |
| `NORMAL_CALL_DURATION_S` | `900` (08-23 잔재) | 정리 대상 |

`LIVE_CTX_TRIGGER_TOKENS` 는 **안 건드렸다**(8000/7000 그대로).

## 5. 저장소 상태

**서버** `dev` @ `690241c`, 작업트리 깨끗, origin 과 동기.

**클라** `C:\dev\beavertalk-flutter-dev` — ⭐ 이게 빌드 폴더다(바탕화면 것은 7월 死클론).
- `dev` @ `a167e3d`, origin 과 동기.
- ⛔ **`stash@{0}` = "0823-메인스레드-홉-수정+심박계-계측(미검증)"** — 오늘 dev 받으려고 넣어뒀다.
  버린 게 아니다. dev 의 `05b7610`(심박계)와 겹치는지 대조 후 처분할 것. **백업은 이 stash 뿐이다.**
- 잔여 변경 2건은 빌드 산물(`.kotlin/sessions`, `GeneratedPluginRegistrant.swift`) — 무시해도 된다.

**폰** SM-N986N(`R3CNB0E8VWN`). adb: `C:\Users\user\AppData\Local\Android\Sdk\platform-tools\adb.exe`
(PATH 에 없다). ⚠ USB 디버깅 승인이 자주 풀린다 — 화면 잠그면 `unauthorized` 로 돌아간다.
logcat 은 오늘 결국 못 받았다(서버 로그로 충분했다).
