# 09-03 아침 · 배포 완료 — 통화 1건 대기 상태로 저장

> 작업 09:10~09:27 KST · 주제 = 8/31 밤에 실패했던 dev 배포 재시도 → **성공**. 코드 변경 0.
> 앞선 문서: `20260901_0030_진행사항-이어하기.md` (§3 판정표·§4 계측이 이 문서의 전제다)
> **사장님이 지금 통화를 못 하셔서 여기서 끊는다. 다음 세션은 §3 부터 시작하면 된다.**

---

## §1. 오늘 한 일 — 푸시 한 번이 전부다

8/31 밤 상황: `1b38421`(웃음도 모국어로 + 모드 전환은 학습자 발화로만)을 배포하려다
Cloud Build `6666092b` 가 68초 만에 `INTERNAL_ERROR` 로 죽었다. CI 테스트는 통과했고
도커 빌드의 pip install 중 `gcb-internal` 스텝이 exit 2 — **인프라 일시 오류** 판정.
재트리거용 빈 커밋 `a274911` 을 만들어 뒀는데 **푸시가 안 돼 있었다.**

⇒ 오늘 확인해 보니 `origin/dev` 가 여전히 `1b38421` 이었다. 재시도 자체가 안 일어난 것.
`git push origin feat/live-prompt:dev` 한 번으로 끝났다.

| 항목 | 값 |
|---|---|
| Cloud Build | `9123296d` **SUCCESS** — 09:15:54 → 09:18:02 (2분 8초) |
| 이미지 다이제스트 | `sha256:b4a0096b07c9c4f5…` |
| 리비전 | **`beavertalk-app-demo-api-00251-n6g`** (09:18:12 생성) · 트래픽 **100%** |
| 헬스체크 | `HTTP 200` · `{"status":"ok","env":"test"}` |
| URL | `https://beavertalk-app-demo-api-ig4qiawz4q-du.a.run.app` |

빌드가 만든 다이제스트 = 서빙 중 이미지 다이제스트를 **대조해서 확인했다.**
목표였던 **00250 이상**을 넘겼고 `1b38421` 이 실제로 떠 있다. 지난 실패는 예상대로
일시 오류였다 — **같은 코드로 재트리거만 하면 통과한다**는 게 실증됐다.

`origin/dev` = `51b74d1` (빈 커밋 `a274911` + 이어하기 문서까지 올라갔다).

---

## §2. 계측 4종 — 전부 배포본에 들어 있다 (코드 실물 확인함)

`domains/learning/realtime/call_session.py`

| # | 계측 | 위치 | 로그 문자열 |
|---|---|---|---|
| ① | INTERRUPTED | `:2676` `elif event.kind == "interrupted"` | `🔬 INTERRUPTED #n (턴 …, 경과 …s, 오디오 …B)` |
| ② | 턴 절단 의심 | `:2694` (turn_end 분기) | `🔬 턴 절단 의심: 턴 … 오디오 …B(…초) 벽시계 …s 인터럽트누적=n` |
| ③ | 오디오 공백 | `:2640` (audio 분기, 임계 1.0s) | `🔬 오디오 공백 …s (턴 …, 누적 …B)` |
| ④ | 턴 열린 시각 | `:2636` `diag_turn_open_ts` | ①②의 경과/벽시계 값으로 나온다 |

- 핵심은 ①. `core/gemini_live.py:369` 가 `interrupted` 를 내보내는데 받는 분기가 없어
  **조용히 버려지고 있었다.** 이제 `:2676` 에 있다.
- `0de6eb9` 의 초기화 버그 수정도 들어 있다 — 턴이 열리는 경로가 둘(audio/out_tr)이라
  리셋이 영영 안 돌던 문제. 지금은 audio 분기가 "안 열렸으면 연다"만 하고 **리셋 소유권은
  `turn_end`**(`:2699~2701`). ⛔ 절단 판정(②)이 리셋 **전에** 바이트를 읽는 순서 유지.
- ⛔ **클라로 안 나간다.** `protocol.py` 판별 유니온을 바꾸면 구버전 앱이 깨진다.
  전부 서버 로그로만 남으므로 **Cloud Run 로그를 봐야 한다.** 앱 화면엔 아무것도 안 보인다.
- 절단 판정 기준: **오디오 48,000B 미만**(PCM24k mono 16bit = 48,000 B/s ⇒ 1초)으로 끝난 턴.

---

## §3. ⭐ 다음 세션 첫 수 — 통화 1건 (배포 확인은 끝났으니 건너뛴다)

**두두로 3분 한 통.** 긴 영어 문장을 중간에 숨 쉬며 몇 번 섞어 달라고 요청(끊김 재현용).
끝나면 `call_id` 를 주면 된다. → `scripts/dev_inspect_call.py <call_id>` + Cloud Run 로그의
`🔬` 만 뽑아 아래 두 표로 가른다.

### 표 A — 프롬프트 수정 3건 판정 (`1b38421` 이 이번에 처음 실통화에 오른다)
| # | 볼 것 | 통과 기준 | 어제(call 1264) |
|---|---|---|---|
| 1 | 웃음 언어 | `Hehe`(로마자) + 문장은 영어 | ⛔ `헤헤, 잘했어요!` → 한국어 |
| 2 | 잡담 이탈 | 항목 5개 이상 연속 | ⛔ t15부터 게임 잡담 |
| 3 | 모드 전환 | `모드 전환 채택 study→chat` **0건** | ⛔ 3통 연속 떴음 |

웃음 근거(1264 상관이 깨끗하다): 턴 첫 조각이 `헤헤`면 그 턴이 한국어(t5·t11·t23·t25·
t27·t29·t33·t37), 없으면 영어(t1·t3·t7·t9·t15). 1263 t33 에서 로마자로
`Kind person! **Hehe**, you got it.` 이라 쓴 턴은 **영어를 유지했다.**

### 표 B — 끊김("두 마디 하고 멈췄다 다시") 원인 판정
| 로그 | 결론 |
|---|---|
| `🔬 INTERRUPTED` + `🔬 턴 절단 의심` | 인터럽트 확정 → 마이크 게이트(클라) 또는 VAD |
| `🔬 턴 절단 의심` 만 | Gemini 쪽 생성 중단 |
| `🔬 오디오 공백` 동반 | **우리 서버 이벤트 루프 블로킹**(GCS 업로드·flush) — 제일 싸게 고침 |

유력 후보 ① VAD 조기 판정 ② 긴 모국어 발화에서 턴 경계 흔들림
③ ⭐ **반이중 게이트 지연** — `state.turn_id` 는 첫 오디오/자막이 와야 세팅된다.
   생성 시작 ~ 첫 오디오 도착 사이 **마이크가 열려 있다.**
배제됨: 클라 오디오 파이프라인 · GoAway/세션재개/WS순단 · 재접지 주입 · 힌트 사이드카.

---

## §4. ⛔ 다시 하지 말 것 (8/31 목록 유지 + 오늘 1건 추가)

1. **규칙 3 에 언어 문장 추가** — 세 번 했고 세 번 다 안 먹었다.
2. **"규칙에 대한 규칙"을 지시문에 쓰기** — 비버가 낭독했다(1263 t40).
3. **`헤헤` 를 personality 에서 빼기** — 웃음소리는 캐릭터 소유(규칙 6). 사장님 정정.
4. **personality 를 범인으로 의심하기** — 다섯 캐릭터 personality 가 글자까지 같다.
   차이는 `role` 이다.
5. **(오늘 추가) 빌드 실패를 코드 문제로 의심하기 전에 푸시가 됐는지부터 확인** —
   `git log origin/dev` 한 줄이면 갈린다. 어제는 재시도 커밋이 로컬에만 있었다.

⛔ `realtime_input_config`(VAD 임계)는 **불변식 금지**다(간헐 무음 버그,
`test_gemini_live_config.py:114` 강제). 손대려면 R4 + **클라 마이크 RMS 계측 숫자**가 먼저.
⛔ 캐릭터·프롬프트 작업과 끊김 작업을 섞지 마라.

---

## §5. 잔가지 (급하지 않음)

- 깃허브 `feat/live-prompt` 브랜치가 **8/19 19:59(`4bbaa52`) 에 멈춰 있다.** 사장님이
  "맞출 필요 없음" 하셨다. 다만 `4bbaa52`("Live 지시문의 끊긴 참조 2건")는 **dev 에도
  로컬에도 없는 유일한 커밋**이다 — 언젠가 내용 확인은 필요하다.
- 이 문서는 **커밋만 하고 푸시하지 않았다.** 문서 1개 때문에 dev 가 다시 빌드·배포되는 게
  아까워서다. 다음에 코드 푸시할 때 같이 올라가면 된다.
- `gcloud` 인증은 오늘 아침 `gcloud auth login` 으로 갱신했다(`202411868@jbnu.ac.kr`,
  프로젝트 `bt-dev-web-01`). `gh` CLI 는 이 PC 에 **설치돼 있지 않다** — GitHub Actions
  상태는 브라우저로 봐야 한다.
