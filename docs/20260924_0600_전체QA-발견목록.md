# 전체 QA 발견 목록 (2026-09-24, 8영역 병렬)

- 방식: 8개 에이전트가 영역을 나눠 병렬 QA → **bt-back 이 근거를 직접 재확인한 것만** 여기 남긴다
- 기준 커밋: `86de1b7` (dev·main 동일). 회귀 1909 passed
- ⛔ **이 목록이 닫히기 전에는 app-api 배포 승인을 보류한다**

## 기각된 지적 (검증에서 탈락)

| 지적 | 왜 기각 |
|---|---|
| 「앱이 `tz` 를 전혀 안 보낸다」 | 에이전트가 로컬 체크아웃(`feat/seamless-fragment`)만 봤다. **`origin/dev` 에 구현돼 있다** — `lib/core/time/device_timezone.dart`(`FlutterTimezone.getLocalTimezone()` · `tz` · `tz_offset_min`), calendar·daily-status 까지 전송. 프론트 보고가 맞았다 |
| 「과제 통화 목표 미주입 [상]」 | 코드 결함은 사실이나 운영 `assignment` 2행(시험) · `assignment_item` **0행** ⇒ 주입할 목표가 없다. **[중] 으로 하향** |

---

# P0 — 지금 새고 있다 (배포 전 필수)

## P0-1 조각 통화가 문장을 중복 생성 → TTS 과금이 그만큼
- 근거: `call_session.py:4246-4258`(조각 종료마다 `_trigger_analysis`, 가드 없음) ·
  `normalcall_service.py:2793`(추출은 전사 전체) · `:2666`(`seen` 이 **이 호출 범위**뿐, 기존 DB 행 미대조)
- **운영 실측(bt-back 직접 쿼리)**:
  ```
  call 1653 (910초·3조각): 활성 문장 34행 / 고유 14개 / voice_url 있는 행 34
  → TTS 14회로 끝날 일을 34회 과금
  다른 통화: 1604(2.4배) 1631(1.8배) 1668(1.5배) 1614(1.6배) 1608(1.4배)
  ```
- ⚠ **현지인 표현 짝은 아직 미배포**(`kind='native'` 0행) — 지금 막으면 증폭(≈4.9배)을 피한다. **짝보다 이게 먼저다**
- 고칠 쪽: 서버 — `(call_id, korean_sentence)` 기존 행 대조, 또는 조각 분석의 추출 범위를 `since_turn_index` 로 좁힘(검증은 이미 좁혀 놨다)

## P0-2 학습달력 단어 수가 사실상 안 찍힌다
- 근거: `call_session.py:4417`(`new = segments[persisted_count:]`) → `:4424` 로 단어를 센다. 그런데
  `_periodic_flush`(`:4901`)가 **커서만 올리고 단어를 안 센다.** `FLUSH_INTERVAL_S=60`
- **운영 실측**: `user_word_count` → **NULL 1,570건 / 값 있음 3건**
- 설계는 «통화 전체» 다 — `scripts/dev_backfill_word_count.py` 가 `role='user'` 전사를 전부 모아 같은 함수로 센다. **실시간 경로만 부분**
- 고칠 쪽: 서버. 기존 행은 백필 스크립트로 복구(단, `--all` 은 NULL 만 잡으니 과소집계 non-NULL 은 call_id 명시)

## P0-3 자유대화 「기억」 요약이 구조적으로 항상 실패 → 1,200자에서 영구 동결
- 근거: `normalcall_service.py:1191-1198` — `run_db`(threadpool) 안에서 `asyncio.run(...)` 으로
  **공유 async genai 클라이언트**를 쓴다 ⇒ 다른 이벤트 루프에 묶인 객체라 매번 예외.
  포괄 except 가 삼키고 `chat_memory_service.py:75` 폴백이 **`f"{old} {new}"[:1200]`** — 옛것 우선·뒤를 자름
- ⇒ 통화당 조각 100~250자 → 5~8통 뒤 상한, 그 뒤 **새 내용이 한 글자도 안 들어간다**
- 고칠 쪽: 서버 — 재압축은 `extract_and_merge_chat_memory`(이미 async·메인 루프)에서 먼저 돌리고
  `run_db` 안엔 순수 DB 병합만. **폴백 방향도 최신 우선으로 뒤집는다**

## P0-4 중단된 재측정 1건이 차시 완료 표시를 전량 삭제 (되돌릴 수 없음)
- 사슬: 재측정 요청이 mll 행 삭제(`mastery_service.py:851`) → 레벨 NULL 이라 「레벨 있으면 안 쓴다」
  가드(`normalcall_service.py:3547`)가 **안 걸림** → 목표어 0자면 「표본 미달」이 `_MIN_LEVEL_NO=1` 배정(`:3595`)
  → L2 가 진도를 no=1 로 → **L7 이 `no>=1` 완료표시 전량 리셋**
- 다시 제대로 재측정해 레벨 4를 받아도 L7 은 `no>=46` 만 만지니 **1~45차시는 영구 「미완료」**
- ⚠ 앱에 **경고 다이얼로그도 없다** — 실수로 2초 만에 끊으면 그걸로 끝
- 고칠 쪽: 서버 — ①표본 미달 판정에서는 진도를 옮기지 않는다(측정이 아니라 사고다)가 가장 곧다

## P0-5 예산 집계를 클라가 직접 무력화할 수 있다
- `POST /api/v1/calls` 의 `total_time` 에 **`ge` 제약이 없다**(`schemas/call.py:47`). 예산은 `SUM(total_time)`
  ⇒ `-100000` 한 번이면 그날 상한 소멸, 매일 반복 가능(실측 재현됨)
- 같은 축: `DELETE /calls/{id}` **하드 삭제 = 예산 환급**(`call_service.py:953`)
- 앱은 두 경로를 안 쓰니 사고는 아니지만 **Live 원가가 무제한으로 새는 구멍**
- 고칠 쪽: 서버 — `ge=0` + soft delete(또는 예산용 사용량 별도 보존)

## P0-6 L3 이 레벨을 **내린다** (D3 자동 강등 없음 위반)
- `curriculum_service.py:538`: `if nxt.level_no > lesson.level_no` — **차시 레벨끼리만** 비교하고
  회원 레벨을 절대값으로 덮는다. 회원 레벨을 한 번도 읽지 않는다
- 재현: 회원 레벨 3 · 진도 no=3(레벨1 차시) → 프리토킹 완료 → **레벨 2 로 강등** + 이력에 «1→2» 거짓 기록
- ⛔ **bt-back 이 브리프에 이 산식을 직접 적어 줬다.** 빌더는 지시대로 구현했다 — 설계 오류다
- 도달 경로: `/__dev/cur-reset`(포인터만 옮기고 레벨은 안 건드림) · 기획서 §P1 «알려진 구멍 (b)» 인터리빙
- 고칠 쪽: 서버 — `max(현재 레벨, nxt.level_no)` + 이력 `from_level` 도 회원 레벨로

---

# P1 — 배포와 함께

| # | 발견 | 근거 | 고칠 쪽 |
|---|---|---|---|
| P1-1 | 발음 리포트가 「6개 중 2개」 — 서버가 짝을 세서 **앱이 못 고친다** | `pronunciation_report_service.py:126` `total_n = len(sentences)`, 응답 DTO 에 `kind` 없음 | 서버 |
| P1-2 | `GET /calls/{id}`(지난 통화)가 `kind`·`nuance` 를 안 채우고 정렬도 안 함 | `call_service.py:993-1006`·`:593` | 서버 |
| P1-3 | 옛 `pro`/`max` 가 DB 에 다시 들어오면 결제자가 조용히 Free | 표 키는 `None`/`premium` 뿐(`call_service.py:178`), **옛 배포(app-api)는 아직 pro/max 를 쓴다** + 같은 DB 공유 | 서버(해석부에서 정규화) |
| P1-4 | 통화 준비 구간 예외 → 최대 **9분** 「이미 통화 중」 잠김 | 핸들러(`:4039`)가 `state` 를 쓰는데 대입은 `:3676` — 그 앞 예외면 `UnboundLocalError` 를 `suppress` 가 삼킨다 | 서버 |
| P1-5 | 자정 걸친 조각 체인이 새 날 예산을 안 깎음 → **premium 이 한 시간에 30분** | `normalcall_service.py:941`(call_date 고정) × `call_repository.py:131`(call_date 로 창) | 서버 |
| P1-6 | 프리미엄 프리토킹은 **통화 도중** 레벨이 바뀐다(조각1이 레벨업 확정) | `call_session.py:4073·4184` · 조각2·3 은 옛 차시를 새 레벨 밴드로 | 서버 |
| P1-7 | 자유대화 첫 인사 시드가 **대괄호 없이 user 턴**으로 → 비버가 자기 말에 답함 | `core/prompts/chat.py:21` vs 다른 모든 시드 `[통화 이어감] …`(`locked/seeds.py:32`) | 서버 |
| P1-8 | 기억 추출이 마지막 전사 저장보다 **먼저** → 끝 60초 누락, **60초 미만 통화는 기억 0** | `call_session.py:4121` vs `:4245` · `_resume_transcript` 는 DB 만 읽음 | 서버 |
| P1-9 | 조각 경계에서 이탈하면 그 통화 기억이 **영구 소실**(만회 경로 0) | `call_session.py:4568` 게이트 · 호출부 2곳뿐 | 서버(+앱 확인) |
| P1-10 | L8 알람 순서 구멍 — 알람 `auto` + start `chat` + 레벨 NULL → 레벨테스트 건너뜀 | L8(`:3120`)이 알람 override(`:3137`) **앞** | 서버 |
| P1-11 | `resume-status` 만 `tz` 를 안 받음 → 앱이 tz 를 보내는 지금 **최대 9시간 어긋남** | `routers/call.py:172` | 서버 |
| P1-12 | 앱이 `call_started.remaining_s` 를 안 읽음 → 항상 서버 안전판이 끝냄(예고 없는 작별) | `call_allowance.dart:23` 5분 상수 | **앱** |

## ⚠ 판매 개시 전 반드시
| P1-13 | 아무 회원이나 `POST /subscriptions` 로 premium 을 켤 수 있고 IAP 가 스텁 영수증을 통과시킨다 | `routers/subscription.py:18` · `core/config.py:450`(`IAP_VERIFY_ENABLED=False`·`IAP_ALLOW_STUB=True`) | 서버 |

---

# P2 — 판단 오염 (동작 무해, 다음 사람을 속인다)

⭐ 이게 이번 QA 의 가장 큰 수확이다. 오늘만 이 때문에 **틀린 지시가 두 번** 나갔다.

## P2-1 [상] `docs/prompts/README.md` — CLAUDE.md 가 지정한 **정본**이 거짓
- CLAUDE.md 규율: «프롬프트를 만지기 전 `docs/prompts/README.md` 를 먼저 읽는다(정본)»
- 그런데 그 문서는 **삭제된 캐스케이드 엔진을 현재형으로** 서술한다(「캐스케이드」 23회, §2 «두 엔진의 차이» 전 절,
  절차 6단계가 `test_cascade_*.py` 실행을 지시 — 그 파일들 0개)
- 더 나쁜 것: **실제로 매 통화 도는 `build_chat_instruction`·`seed_chat_opening` 이 `docs/prompts/` 전체에서 0건**
- ⇒ 「일반 통화 프롬프트를 튜닝해라」를 받은 사람이 정본대로 `persona_prompt.py` 를 고치면 **프로덕션은 한 글자도 안 바뀐다**

## P2-2 [상] `CLAUDE.md` 자체가 3곳 거짓 (매 세션 로드되는 파일)
| 행 | 내용 | 사실 |
|---|---|---|
| `:7` | 「인증은 **자체 JWT** + Supabase 토큰」 | `core/security.py` 삭제됨. `JWT_SECRET` 읽는 곳 **0건** |
| `:37` | 「**이메일**/**소셜** … 키 없으면 스텁·폴백」 | `core/email.py`·`core/social.py` 둘 다 삭제 |
| `:77` | 「dev 데모: `/__cascadedemo`」 | 그 라우트 없음 |

## P2-3 [중] ⛔ 아무도 안 읽는 `JWT_SECRET` 이 **운영 기동을 막는다**
- `config.py:477` — `ENV == "prod"` 이고 `JWT_SECRET` 이 dev 기본값이면 **`ValueError` 로 기동 차단**
- 그런데 그 값을 읽는 코드는 0건(인증은 Supabase 로 이전)
- ⇒ 언젠가 `ENV=prod` 로 올리는 날 **아무 기능도 안 쓰는 값 때문에 부팅이 거절된다**

## P2-4 [중] 죽은 설명 4건 + [하] 8건
- `protocol.py:408` — 존재하지 않는 «서버 방어 관문(aec)» 을 현재형 선언. 하네스가 지금도 `aec` 를 보내고 조용히 버려진다
- `core/stt.py:228-234` — **한 주석 안에서 자기모순**(「살아있는 호출부는 라이브 하나뿐」 ↔ 「미사용 경로다」)
- `normalcall_service.py:1586` — `finalize_call` 이 «유일한 호출부는 cascade_session.py» 라 지목(삭제된 모듈)
- `:1943` — `save_call_usage` 가 없는 캐스케이드 호출부에 규약을 지시
- `[하]` 8건: `core/audio.py:71` · `core/tts.py:383` · `persona_prompt.py:9,52` · `locked/rules.py:77` ·
  `deploy_demo.sh:62`(배포마다 404 찍음) · `level_call_demo.html:160` · `core/stt.py:207,269`(Live 통화가 로그에 `[stt-v2]` 로 찍힌다)

## P2-5 [중] `load_call_setup` 반환 키 5개를 아무도 안 읽는다 — 매 통화 버려지는 DB 작업
- 읽는 곳 0건: `known_items`·`promotion_notice`(기지) + **`recent_topics`·`history`·`lang_band`(신규 발견)**
- 조건부 고아: `study_items`·`candidates` 는 **`chat` 일 때만** 읽힌다. `auto`(→expression/freetalk) 통화에서는
  `_load_study_materials` 전체(≈7~8 SELECT)가 **전량 폐기** — 통화 시작 지연 경로
- ⚠ 정본 `README.md:178` 이 이걸 「Live 는 넘긴다」고 적어 **반대로 읽히게** 만든다

## P2-6 [하] 고아 코드·설정·의존성
- `core/tts.py` `synthesize_stream`·`build_streaming_config`(캐스케이드 전용, 프로덕션 0) —
  ⚠ 보호 목록의 «복습 TTS» 는 `synthesize()` 축이라 **별개**
- `core/audio.py` 함수 6개(`upsample_16k_to_24k`·`align_pcm16`·`has_audible_signal`·`trim_silence_edges` 등) 호출부 0
- `call_session.py:3866` 도달 불가 `else` — C14-a 가 지운 것과 **똑같은 모양이 한 군데 더**
- 죽은 import 8건 · 미사용 설정 11개(`JWT_*`·`RESEND_*`·`EMAIL_*`·`GOOGLE_CLIENT_ID`) ·
  **`bcrypt` 의존성 참조 0건**

---

# 통과 확인된 것 (좋은 소식)

- **옛 `cascade:` 행(77건) 원가 계산 정상** — 0 + 「미상」 폴백, 곁가지 몫은 engine 분기 위에서 더함. 회귀가 잠금
- **`remaining_s` 두 의미 분리 깨끗** — 360 클램프가 `call_started` 한 곳뿐
- **언어 섞임 0건** — 레벨 조회·갱신이 전부 language 스코프, ko 폴백은 `if language == "ko"` 안에만
- **조각 이중계산 없음 · 동시통화 이중 게이트 · 540s 백스톱 충돌 없음 · admin 면제 4곳 일치**
- **키 생략 규약** 지켜짐(`model_serializer` 가 response_model 재직렬화에도 살아남음)
- **L3 멱등** 이중 가드 · **L7 이 `done_at` 을 안 비운 판단 정확**(소비처 전수 확인)
- 모르는 플랜은 4개 표 전부 Free 폴백 · `plan_override` 는 admin 검증 통과값만
- 달력 N+1 없음(2쿼리) · 서머타임·음수 오프셋·422 검증 정상
- 레벨테스트 전 경로 · 발음 STT v1 체인 · 복습 TTS `synthesize()` 무손상
