# 공부 모드 설명 언어 · 입력 전사 언어 수정

- **작성** 2026-08-20
- **발단** 사장님 실통화(`call_id=1097`, demo-api, locale=en, 5분) 로그 검토
- **범위** 로그에서 찾은 7건 중 **2건만**(사장님 지시). 나머지 5건은 §4 에 남긴다
- **승인** A안(예시 교체만) — 2026-08-20 사장님

---

## 1. 무엇이 이상했나

### ① 입력 전사가 엉뚱한 언어로 찍힌다

| 학습자가 말한 것 | 전사된 것 |
|---|---|
| "피우다" | `フィウダ` (일본어) |
| "다" | `套` (중국어) |
| "아주" | `और च` (힌디어) |
| (짧은 응답) | `für 10` · `kumite` · `तककी` · `Sí.` |

⛔ 화면만의 문제가 아니다. 이 전사는 `CallRawData.content` 로 **저장**되고, 이어하기
요약(`normalcall_service._resume_transcript`)과 통화후 문장 추출·증거 인용 검증이 그걸 읽는다.
⇒ **증거가 안 잡히고 레벨업이 안 도는 원인**이 될 수 있다.

### ② 공부 모드인데 설명·지시가 한국어로 나간다

모국어가 영어(`locale=en`)인 학습자에게:

```
오늘은 "피우다"라는 단어 배울게. "피우다"는 "to bloom" or "to smoke" 이런 뜻이야. 따라해 봐.
"다" 아니고 "다". 디귿 발음이야. 다시 해봐.
```

불변 규칙 3 은 정반대를 지시한다 — *"'공부 모드'는 … 설명·지시·리액션은 레벨과 무관하게
전부 {locale_label}로 한다"*.

---

## 2. 원인

### ① 전사 — 언어 힌트를 안 준다

`core/gemini_live.build_live_config` 이 빈 설정을 넘긴다:

```python
input_audio_transcription=types.AudioTranscriptionConfig(),   # 언어 힌트 0
```

설치된 SDK(google-genai **2.18.1**)의 `AudioTranscriptionConfig.language_codes` 설명:

> BCP-47 language codes providing hints about the languages present in the audio.
> **If omitted or empty, defaults to automatic language detection.**

⇒ 지금은 자동 감지에 전부 맡긴 상태다. 1~2음절 한국어는 자동 감지가 못 버틴다.

### ② 공부 모드 — 규칙은 맞고 **예시가 어긋난다**

지시문을 실제로 조립해 확인했다(`build_system_instruction`, locale=en, study_items 3건, 4,542자).
규칙 3 은 정확히 쓰여 있다. 문제는 절차 블록(`core/persona_prompt._study_procedure`)이다:

```
- 문법: ① 뜻·쓰임을 1~2문장 설명 … ④ 응용 질문(예: "너는 주말에 뭐 하고 싶어?")
```

- **`docs/prompts/README.md` 원칙 4 정면 위반** — 지시문 4,542자를 통틀어 **리터럴 대사가
  나오는 유일한 자리**가 여기고, 그게 한국어다. *"모델은 규칙과 예시가 다르면 예시를 따른다."*
- **멀티랭귀지 버그(덤)** — `{target}` 치환이 아니라 **박힌 한국어**다. 프랑스어 학습자에게도
  "너는 주말에 뭐 하고 싶어?" 가 그대로 간다.

---

## 3. 수정

### ① 전사 언어 (코드)

1. `core/gemini_live.py` — `build_live_config(..., input_language_codes=None)`.
   **None/빈 값이면 종전과 바이트 동일** ⇒ 기존 테스트·스냅샷 무영향
2. `open_session` 통과 → `call_session._input_language_codes()` 가 `[학습언어, 모국어]`를 만든다
3. `core/config.py` — 킬스위치 `LIVE_INPUT_LANGUAGE_CODES`(기본 on).
   ⛔ Gemini 가 이 필드를 거부하면 **통화가 전부 안 열린다**. env 하나로 되돌릴 구멍을 남긴다(R5)
4. **출력 전사는 안 건드린다** — 로그상 비버 쪽 전사는 멀쩡하다

#### ⭐ 매핑을 새로 만들지 않았다 (착수 후 계획 수정)

계획엔 `core/languages.py` 에 ISO→BCP-47 표를 새로 만든다고 썼다. **그렇게 하지 않았다** —
`core/stt.normalize_language_codes` 가 이미 있고, 캐스케이드가 **같은 결함을 2026-08-08 에
이미 겪고** 만든 것이다(모국어 발화 5회 연속 36초가 `text=''` 로 사라진 사건).
표를 하나 더 만들면 같은 질문에 답이 둘이 된다.

그쪽 규율 하나가 특히 중요했다 — **"검증한 코드만 매핑하고 모르는 건 버린다"**
(`_STT_LANGUAGE_ALIASES = {"en": "en-US", "ko": "ko-KR"}`). 내가 처음 쓴 표에는
`ja-JP`·`zh-CN`·`fr-FR`·`vi-VN` 이 **추측으로** 들어 있었다. 틀린 코드는 그 언어를 조용히
안 들리게 만든다 — 지금 고치려는 결함과 **같은 실패**다. 그래서 되돌리고 기존 것을 썼다.

#### ⛔⛔ 부분 힌트는 무힌트보다 나쁘다 (all-or-nothing)

기존 함수를 그대로 쓰면 `ja` 는 버려지고 `en-US` 만 남는다. 그건 **일본어 발화를 영어로
알아들으라고 시키는** 꼴이라 지금보다 나쁘다. 그래서 `_input_language_codes()` 는
**하나라도 매핑이 안 되면 통째로 포기한다**(빈 목록 = 힌트 없음 = 종전 동작).

| 학습/모국어 | 결과 |
|---|---|
| ko / en | `["ko-KR", "en-US"]` ← 오늘 사장님 통화 |
| ko / ko | `["ko-KR"]` (중복 제거) |
| ja / en · ko / vi | `[]` — **오늘과 완전히 같다** |

⇒ 실제로 동작이 바뀌는 조합은 **ko·en 뿐**이다. 나머지 언어는 회귀 위험이 0 이고,
표를 채우고 싶으면 v2 supported-languages 표를 확인해 `_STT_LANGUAGE_ALIASES` 에 넣으면
두 경로(캐스케이드·라이브)가 같이 좋아진다.

⚠ **한계**: SDK 에 필드가 있는 건 확실하나, Live 백엔드가 실제로 존중하는지는
**통화 1건으로만** 확인된다. 안 먹으면 서버 후처리(스크립트 판별 `core.languages._SCRIPT_RANGES`)로
거르는 2안으로 간다.

⚠ 사소한 흠: 코드를 버릴 때 찍히는 경고가 `[stt-v2]` 접두어를 단다(라이브 통화인데).
로그를 읽을 때 헷갈릴 수 있어 적어 둔다.

#### ⛔ 캐스케이드를 지울 때 (사장님 지적 2026-08-20)

*"캐스케이드가 아니라 라이브만 고치는 거 알지? 캐스케이드 안 쓴대."* — 맞다. 캐스케이드
세션 파일은 한 줄도 안 건드렸다. 다만 **라이브가 `core/stt.py` 의 함수 하나를 빌려 쓴다.**

위험한가 확인했다:
- `core/stt.py` 는 캐스케이드 전용이 **아니다** — 발음 챌린지(`stt_session.py` → `/pron/stt/ws`)도
  쓴다. 캐스케이드를 들어내도 이 파일은 남는다.
- 무거운 `google.cloud` import 는 전부 **함수 안**에 있다 → import 만으로 라이브가 무거워지거나
  죽지 않는다(R5 안전).
- 빌려 쓰는 건 **순수 함수 하나**(코드 표기 변환)다. 캐스케이드 로직이 아니다.

⇒ **지금은 그대로 둔다**(사장님 판단). 대신 나중에 안 깨지게 **기록을 세 군데** 박았다:
| 어디 | 무엇 |
|---|---|
| `core/stt.py` 의 `normalize_language_codes` 바로 위 | "캐스케이드 전용이 아니다 — 같이 지우지 마라" + 쓰는 곳 셋 |
| `docs/20260813_0040_캐스케이드-데모잔재-정리목록.md` **§2-b** | 정리 작업자가 실제로 여는 문서. [유지] 항목으로 등재 |
| `tests/test_stt_languages.py` ④ 절 머리 | "여기부터는 라이브 회귀다 — 지우지 마라" |

끊고 싶어지면 `_STT_LANGUAGE_ALIASES`(두 줄) + `_BCP47_RE` + 함수 본문을 라이브로 옮기면
된다(5분).

### ② 공부 모드 언어 (프롬프트) — **A안만**

`_study_procedure` 의 ④ 리터럴 한국어 예시를 언어중립 표현으로 교체한다.

⛔ **B안(절차에 "설명은 모국어로" 재명시)은 하지 않는다.** 원칙 3(중복은 한 규칙에 위임)과
마찰한다 — 설명 언어는 규칙 3 이 소유한다. A안 뒤 통화를 다시 듣고, 그래도 한국어로 설명하면
그때 판단한다.

⛔ Live 공용 본문(`_INVARIANTS_TEMPLATE`)은 건드리지 않는다 — 규칙 3 자체는 맞게 쓰여 있다.
스냅샷 동결본(`tests/test_persona_prompt._ORIG_INVARIANTS_TEMPLATE`)도 그대로다.
기존 테스트는 `"유형별 절차:"` 헤더만 검사하고 ④ 예시는 안 본다(확인함).

---

## 4. 이번에 **안 고친 것** (같은 통화에서 나온 나머지 5건)

| # | 증상 | 메모 |
|---|---|---|
| 1 | 비버가 자기 질문을 버리고 작별 — `305.9s` 질문 → `308.2s` "Whatever. I'm busy." | 종료 시드(`call_session._close_seed`)가 "**네가 방금 질문한 경우**"를 안 다룬다. 사장님이 우선순위에서 뺐다 |
| 2 | 마지막 힌트가 작별 발화 도중 도착(`308.8s`) | 위와 같은 자리 |
| 3 | 전사에 `{}` · `{NO}` 잡음이 섞여 DB 로 들어간다 | 전사 아티팩트. ①이 해결하는지 통화 후 확인 |
| 4 | 반말·존댓말 혼용(`따라해 봐` ↔ `뭐 좋아해요?`) | 캐릭터 DB(`character.personality`) 쪽일 수 있다 |
| 5 | 커리큘럼 항목 `다`(all) 를 단독 발음 연습 / `피우다` 를 초급에 "to smoke" 로 | 프롬프트가 아니라 **커리큘럼 데이터**(`learning_item`) |

---

## 5. ⛔ 테스트 가짜 팩토리 29개가 깨졌다 (예상 못 한 대가)

첫 전체 회귀에서 **29 failed**. 전부 같은 원인이었다:

```
factory() got an unexpected keyword argument 'input_language_codes'
```

가짜 세션 팩토리들이 `(client, settings, *, system_instruction, voice)` 로 **엄격하게**
선언돼 있다. 이건 사고가 아니라 **의도된 설계**다 — 운영 코드가 `tools`·`resume_handle` 을
"값이 있을 때만" 넘기는 이유가 그것이고(`_run_one_generation` 주석), 그 규율 덕에
"운영이 실제 `open_session` 이 모르는 인자를 넘기는" 사고가 테스트에서 잡혀 왔다.

내 코드도 같은 규율을 따랐지만(`if state.input_language_codes:`) **소용이 없었다** —
기본 시나리오가 ko 학습/en 모국어라 값이 **항상** 있다. `tools` 처럼 "평소엔 None"이 아니다.

⇒ **가짜 팩토리 쪽을 `**_kw` 로 열었다**(9곳, 3파일). 운영 코드를 테스트 더블에 맞춰
구부리는 것보다 낫다고 판단했다. 대가는 정직하게 적는다 — 그 그물이 **한 칸 성겨졌다.**
`build_live_config`/`open_session` 이 이 인자를 실제로 받는지는 이제
`tests/test_gemini_live_config.py` 의 새 케이스 2건이 지킨다.

---

## 6. 검증 (실측)

| 시점 | 결과 |
|---|---|
| 1차 전체 회귀 | **29 failed / 1289 passed** — 전부 위 시그니처 문제 |
| 시그니처 수정 후 해당 3파일 | **168 passed** (`test_normalcall_ws` · `test_hint_teaching` · `test_level_test_call`) |
| `test_cascade_vad_anchor` | 1차에서 1건 실패했으나 **단독 9 passed** — CLAUDE.md 가 경고한 **부하 flake**(내 변경은 캐스케이드를 안 건드린다) |
| **2차 전체 회귀** | ⭐ **1318 passed / 0 failed** (9분 57초) |
| 그 뒤 추가한 것 | 주석·문서뿐(동작 변경 0). 해당 3파일 재확인 **34 passed** |

남은 것:

1. PR → `dev` 머지 → 자동 배포(GitHub Actions) → `/__enginedemo` 에서 통화 1건
3. 통화 후 볼 것:
   - `input_transcript` 가 한국어/영어로만 찍히는가 (①)
   - 공부 모드에서 설명·지시가 영어로 나오는가 (②)
   - ⚠ ①이 안 먹으면 `LIVE_INPUT_LANGUAGE_CODES=false` 로 즉시 되돌린다(재배포 불필요)

---

## 7. 🔖 이어하기 (2026-08-20 중단 시점 — 사장님 식사)

### 지금 상태

- 브랜치 `feat/live-prompt` (HEAD `4bbaa52`) · **전부 미커밋 작업 트리 상태**
- 12파일 `+212 / -16`
- 전체 회귀 **1318 passed / 0 failed** 통과한 코드 그대로다
- ⛔ 커밋·rebase·푸시·배포 **하나도 안 했다**

```
 M core/config.py                    LIVE_INPUT_LANGUAGE_CODES 킬스위치
 M core/gemini_live.py               build_live_config/open_session 에 input_language_codes
 M core/persona_prompt.py            공부 절차 문법 ④ 리터럴 예시 제거(A안)
 M core/stt.py                       주석만 — "라이브도 쓴다, 같이 지우지 마라"
 M domains/learning/realtime/call_session.py   _input_language_codes() + state 슬롯 + 팩토리 인자
 M tests/test_gemini_live_config.py  신규 2건
 M tests/test_stt_languages.py       신규 4건(④ 절 = 라이브 회귀)
 M tests/test_{normalcall_ws,hint_teaching,level_test_call}.py   가짜 팩토리 **_kw (9곳)
 M docs/prompts/README.md            §8 결정 로그
 M docs/20260813_0040_...정리목록.md  §2-b 캐스케이드 삭제 대비 기록
 ?? docs/20260820_1130_...수정.md     이 문서
```

### 이어서 할 일 (순서대로)

1. **커밋** — 모델 변경 0 이라 **마이그레이션 불필요**. 프롬프트/코드가 섞여 있으니
   2커밋으로 쪼개도 좋다(① 전사 언어 ② 공부 모드 프롬프트)
2. **`dev` 위로 rebase** — 브랜치가 **4커밋 뒤처져 있다**:
   `c4c53a0` `711bc3c` `c801417` `2c4410e`
   ⚠ `2c4410e`("재연결 기계를 들어낸다")가 **`call_session.py` 에서 136줄을 지웠다** — 내가
   고친 그 파일이다. 충돌 가능. 다만 내가 끼워 넣은 자리(`factory_kwargs`, `_run_one_generation`)는
   `dev` 에도 **그대로 남아 있음을 확인**했다(origin/dev:1810~1817)
3. **rebase 후 통화 회귀 재실행** — R4. 최소 `tests/test_normalcall_ws.py`
   (rebase 로 통화 코드가 섞였으므로 "이미 통과했다"를 재사용하면 안 된다)
4. PR → `dev` 머지 → 자동 배포(GitHub Actions) → `/__enginedemo` 통화 1건
5. 통화 후 확인 — §6 참조

### 통화에서 볼 것

| 볼 것 | 성공 | 실패 시 |
|---|---|---|
| `input_transcript` 가 한국어/영어로만 찍히나 | `套`·`für 10`·`फ`류가 사라짐 | `LIVE_INPUT_LANGUAGE_CODES=false` 로 즉시 원복(재배포 불필요) → 서버 후처리 2안 |
| 공부 모드 설명·지시가 영어로 나오나 | `"피우다" means "to bloom"` 처럼 영어 설명 | B안(절차에 언어 재명시) 검토 — 원칙 3 과 마찰하니 사장님 판단 |

### 이번에 안 고친 것

§4 표 5건(작별 무례·힌트 지각·`{}` 잡음·반말존댓말·커리큘럼 데이터). 우선순위는 §4 참조.


---

## 8. 🔖 배포 후 실통화 검증 (2026-08-22, `call_id=1136`)

`dev` 푸시 `c4c53a0..2592abb` → 자동 배포 → `/__enginedemo` 실통화(locale=en, 조각 2개).

### 앞선 두 수정의 결과

| | 결과 |
|---|---|
| **②공부 모드 언어** | ✅ **해결.** 설명·지시가 전부 영어(`It means 'to bloom' like a flower.`). 08-20 의 `"피우다"는 ... 이런 뜻이야` 같은 한국어 설명 0건 |
| **①전사 언어** | 🔶 **크게 좋아졌으나 미완.** 한국어 6건 정확(`이 책이 아주 재미있습니다.` · `우리 반 친구를 다 알아요.` · `야, 조용히 해.`) — 전엔 `套`·`और च` 였다. 그러나 힌트 밖 언어 2건 잔존: `péri`(불) · `Já vou. 1,9.`(포) |
| **§4-#5 `{}`·`{NO}` 잡음** | ✅ 이 통화에서 안 보였다 |
| **§4-#3 종료 시드** | ⬜ 미검증 — 연결이 끊겨(1005/1006) 종료 규약을 안 겪었다 |

⇒ ⭐ **`language_codes` 는 "제한"이 아니라 "힌트"다.** 다국어 오인식은 확 줄지만 0 은 아니다.
완전 차단이 필요하면 계획해 둔 **2안(서버 후처리 — 스크립트 판별)** 으로 간다.

### ⭐ 새로 나온 결함 2건 (7건 목록 밖)

#### (가) 비버가 웃음·머뭇거림을 과도하게 길게 낸다 → **이번에 A안으로 대응**

| 턴 | 길이 |
|---|---|
| 정상 턴 18개 | 1.2~4.5초 |
| `a82c0242` | **19.3초** |
| `3049819d` | **39.1초** |

둘이서 통화의 약 20%. 두 턴 모두 전사가 첫 조각 뒤로 안 온다 = **글자 없는 소리만** 나갔다.

⛔ **barge-in off 와 겹쳐 심각해진다** — 비버 발화 중엔 학습자 마이크가 서버로 안 간다
(`call_session.py` 의 `state.turn_id is None`). 그 39초 동안 "조용히 해"가 **닿지 않는다.**
로그: 턴 종료 `111.7s` → 학습자 발화 `114.5s`. 그 전 발화는 전부 버려졌다.
⚠ 게다가 비버는 그 항의(`조용히 시끄러`)를 **한국어 연습 실수로 받아 교정했다.**

**사장님 관측** — "자막으로는 알겠다고 뜨는데 소리는 계속 웃고 있음".

### ⛔⛔ 이 어긋남은 **웃음·"음~" 같은 비언어 발성 한정이다 — 말에서는 안 난다**

사장님이 두 번 짚었다(2026-08-22): *"딱 웃음소리 음 이런 거만 그래"*.
⚠ **"오디오가 전반적으로 밀린다"로 읽지 마라.** 내가 처음 그렇게 적었고 그건 과장이다.
말하는 구간에서는 자막과 소리가 맞는다.

왜 비언어 발성에서만인가 — **그 구간엔 자막이 아예 없다.** 로그에 그대로 있다:
`172.7s → 177.7s`(5.0초) · `181.3s → 187.1s`(5.8초) 동안 전사 조각이 **한 개도 안 온다**.
소리는 그 시간을 채우는데 자막은 그 구간을 **건너뛰고 다음 말로 넘어간다** ⇒ 딱 여기서만
"자막은 다음 말, 소리는 아직 웃음"이 된다. 말할 때는 자막과 소리가 같은 내용을 같이
흘려보내므로 어긋난 게 안 느껴진다.

한 번 벌어진 차이는 안 줄어든다 — `scripts/engine_demo.html:175-176` 이 이렇다:
```js
const t = actx.currentTime; if (playT < t) playT = t;   // 대기열이 **비었을 때만** 따라잡는다
n.start(playT); playT += ab.duration;                   // 앞서 있을 때 줄이는 코드는 없다
```
`playT` 를 만지는 곳은 통화 시작의 `playT = 0` 뿐(`:198`)이라 통화 끝까지 그대로 간다.

방증 — 긴 웃음 턴 **뒤에만** 학습자 발화가 비정상적으로 끊긴다(소리 끝나기를 기다린 시간):

| 턴 종료 | 다음 학습자 발화 | 공백 |
|---|---|---|
| `128.8s`(8.2초 웃음 턴) | `159.6s` | **30.8초** |
| `188.1s`(15.9초 웃음 턴) | `236.8s` | **48.7초** |
| 그 외 정상 턴 | — | 보통 3~8초 |

⚠ **미확정 한 가지**: "비언어 발성 오디오가 몰아서 온다"까지는 확인 못 했다 — 데모 페이지가
오디오 조각 도착 시각을 안 찍는다. `playPCM24k` 에 `playT - actx.currentTime`(밀린 초)을
찍는 줄 하나면 다음 통화에서 **웃음 구간에서만 튀는지** 바로 보인다.
⭐ 다만 **고치는 쪽은 이 답을 몰라도 된다** — 대기열이 N초 이상 앞서면 잘라내는 처리는
원인이 무엇이든 똑같이 듣는다. 계측은 고치기 위해서가 아니라 이해하기 위해 한다.

**대응(사장님 지시: "A 먼저, A만")**: **규칙 6 신설** — 말이 아닌 소리의 길이.
⛔ 처음엔 규칙 5(응답 길이)에 붙였다가 사장님 지적으로 **되돌렸다** — 길이 규칙에 두
대상을 묶으면 할 말까지 줄어든다. 규칙 5 는 바이트 그대로다.
상세·왜 리터럴을 안 썼는지는 `docs/prompts/README.md` §8 의 2026-08-22 항목.
⚠ 네이티브 오디오가 비언어 발성을 지시로 통제할 수 있는지는 **검증된 바 없다.**
안 먹으면 B(턴 길이 상한·R4) 또는 C(클라 큐 상한)로 간다.

#### (나) 힌트 사이드카 필드 오염 — **미처리**

- `roman` 에 한글이 섞인다: `"Jeoneun 주로 jipeseo swieoyo."` · `"Ne, yeonghwa boneun geo joahae-yo. 주로 aeksyeon yeonghwa bwayo."`
- `korean` 에 영어가 들어간다: `"korean":"It means, \"Today the weather is hot.\""`

### 안 한 것 (B·C)

사장님이 **A만** 지시했다. B(턴 길이 상한)는 모델에 안 기대는 확실한 카드지만 턴
상태기계 = R4 불변식이라 제일 비싸다. C(클라 오디오 큐)는 **계측이 먼저다.**
A 를 통화로 확인한 뒤 판단한다.


---

## 9. 🔖 이어하기 (2026-08-22 밤 중단 — 아침에 통화 예정)

### 지금 상태

- `dev` = **`849a25b`**, demo-api 배포 완료(21:49 확인, 이미지 태그 일치 + health 200).
- 작업 트리 깨끗. **미커밋 없음.**
- 오늘 `dev` 로 나간 것 4덩어리:

| 커밋 | 무엇 | 통화 검증 |
|---|---|---|
| `21b6183` | Live 지시문 끊긴 참조 2건(08-19 작업분, 그동안 dev 에 없었음) | ⬜ |
| `a3fb96b` | 입력 전사 언어 힌트 | ✅ 한국어 정확히 찍힘(잔여 2건) |
| `2592abb` | 공부 절차 리터럴 예시 제거 | ✅ 설명이 영어로 나감 |
| `65eddf4` | 규칙 6 신설(비언어 발성 길이) | ✅ 웃음 148초 → 60초 |
| `849a25b` | 종료 시드 우선순위(무음 종료) | ⛔ **미검증 — 아침에 이것부터** |

### ⛔⛔ 아침에 **가장 먼저** — 앱이 보는 서버를 바꿔야 한다

**앱은 오늘 배포한 서버를 안 본다.** `beavertalk-flutter/.env` 가 이렇다:

```
API_BASE_URL=https://beavertalk-app-test-api-333511894671.asia-northeast3.run.app
```

| 서버 | 마지막 배포 | 앱이 보나 |
|---|---|---|
| `beavertalk-app-test-api` | **2026-08-04** (18일 전) | ✅ 여기 |
| `beavertalk-app-demo-api` | 2026-08-22 (`849a25b`) | ❌ |

CI 는 `dev` push → **demo-api** 로만 배포한다. `test-api` 는 파이프라인에 **없어서** 8월 4일
이미지 그대로다. ⇒ 지금 APK 로 통화하면 **오늘 고친 게 하나도 안 보인다.**

**사장님 결정(2026-08-22): A안** — `.env` 의 `API_BASE_URL` 을 demo-api 로 바꾸고 재빌드한다.

```
API_BASE_URL=https://beavertalk-app-demo-api-333511894671.asia-northeast3.run.app
```

⚠ `.env` 는 `pubspec.yaml:112` 에서 **assets 로 번들**된다 ⇒ 값만 바꾸면 안 되고 **APK 재빌드
필수**다(런타임 로드 아님).
⚠ `.env` 에는 Supabase 키도 있다 — **사장님이 직접 고친다.** 내가 만질 파일이 아니다.
⛔ B안(`test-api` 에 손으로 배포)은 안 한다 — CI 밖 수동 배포는 하지 않기로 돼 있다.

### ⚠ 그래서 안드로이드 버벅임 조사도 기준이 흔들린다

지금까지 겪은 버벅임은 **전부 `test-api`(8월 4일 코드)** 에서 난 것이다. 그 사이 서버가 많이
바뀌었다(재연결 기계 제거, 캐스케이드 제거, 이어하기 등). ⇒ **demo-api 로 옮긴 뒤 증상이
그대로인지부터 다시 봐야 한다.** flush 가설도 그 위에서 재확인이다.
⚠ `_periodic_flush` 가 8월 4일 코드에도 있었는지 확인하면 가설이 더 단단해진다(아직 안 봤다).

### 아침에 통화하면서 볼 것 (우선순위 순)

1. ⭐ **5분 종료** — 비버가 **소리 내어** 작별하는가 / **평서문**으로 끝나는가.
   유일하게 검증 안 된 배포분이다. 안 되면 §8 의 B안(절차 재명시) 검토.
2. **응답 분량이 짧아지지 않았나** — 규칙 6이 규칙 5로 번졌는지 보는 신호.
   짧아졌으면 규칙 6 문구를 더 좁혀야 한다(사장님이 이 위험을 짚어 규칙 5 수정을 되돌린 이력).
3. **웃음 길이** — 60초에서 더 줄었는지. 벽시계 말고 **아래 방법으로 실측**한다.

### ⭐ 실측 방법 (이번에 확립 — 재사용할 것)

```bash
# 통화의 비버 턴별 실제 오디오 길이 (mp3 128kbps CBR → 바이트 ÷ 16000 = 초)
gcloud logging read 'resource.type="cloud_run_revision"
  AND resource.labels.service_name="beavertalk-app-demo-api"
  AND textPayload:"calls/25/<call_id>"' --project bt-dev-web-01   --limit 200 --freshness=6h --format="value(textPayload)"   | grep -o "[0-9]\{4\}_beaver\.mp3 ([0-9]* bytes)"
```

서버가 자체 계측도 남긴다 — `BEAVER[tN] 읽기=X자per초 [lang:N자/M초]`.
**정상 10~15자per초 / 웃음 턴 2.0~4.1자per초** 로 확 갈린다(웃음 판별에 이게 제일 빠르다).

⚠ **벽시계(turn_start~turn_end)로 재지 마라** — 실제 소리의 1/3.4 로 나온다.
Live 는 오디오를 실시간보다 ~3.4배 빠르게 보낸다(자막 창 1.2초 = 소리 4.3초).

### 🔍 안드로이드 버벅임 — 조사 중단 지점

**증상**: 안드로이드에서만 라디오처럼 버벅인다. **원래 1분마다**였는데 클라 버퍼를 키운 뒤
**5분 이후**로 밀렸다(사장님). iOS 는 괜찮다.

**유력 가설**: `_periodic_flush`(`FLUSH_INTERVAL_S = 60.0`)가 통화 중 1분마다 세그먼트를
mp3 로 변환(ffmpeg 프로세스)하고 GCS 에 올린다 → 그동안 오디오 전송이 밀린다.

| 근거 | 상태 |
|---|---|
| 원래 증상 주기 1분 == `FLUSH_INTERVAL_S` 60초 | ✅ 정확히 일치 |
| 라이브 통화 경로에 **다른 1분 주기 작업이 없다** | ✅ 확인(`CASCADE_SEGMENT_FLUSH_S` 는 캐스케이드, `NATIONALITY_PCM_MAX_S` 는 타이머 아님) |
| flush 1회가 실제로 **1.6~3.1초** 걸린다 | ✅ 실측(call 1137: 14개/3.1초, 12개/3.0초, 6개/1.6초) |
| Cloud Run **CPU 1개** | ✅ 확인(`--cpu 1`, memory 2Gi, concurrency 80) |
| CPU 사용률이 그때 튀는가 | ❌ **못 가림.** 1분 평균이라 통화 중 0.4%→9.8% 로만 보인다. "3초 100%"와 "60초 10%"를 구분 못 함 |

⚠ **기전이 CPU가 아닐 수 있다.** CPU 총량이 낮게 나온 걸 보면 **GIL 경합**(GCS 업로드가
파이썬 스레드에서 GIL 을 잡았다 놨다 하며 asyncio 루프를 밀어냄)일 가능성이 있다.
⇒ **그러면 `--cpu 2` 실험이 무의미하다.** 실험 설계 전에 이걸 먼저 가려야 한다.

**클라 쪽 사실**(`beavertalk-flutter/lib/features/normalcall/presentation/normalcall_controller.dart`):
- `FlutterSoundPlayer.startPlayerFromStream(sampleRate: 48000, bufferSize: 16384, onBufferUnderflow: ...)`
- 16384 B @ 48k/16bit/mono = **0.17초**. 주석에 *"Larger buffer gives the AudioTrack more headroom"*
  — **사장님이 고쳤다는 게 이것**이고, 1분 → 5분으로 민 레버다.
- 서버는 24k 로 보내고 **클라가 24k→48k 업샘플**한다(`_upsample24to48`).
- 단일 소비자 + `asyncMap` 백프레셔 큐(`_playbackLoop`) — 설계는 멀쩡하다.
⇒ **서버가 방아쇠, 안드로이드 버퍼가 취약점.** 버퍼를 더 키우는 건 완충재 두껍게 하기이고,
  1분마다 때리는 것 자체를 멈추는 게 맞다(고치면 iOS·안드로이드·15분 통화 모두 이득).

**다음 수 후보** (⛔ 셋 다 사장님 승인 필요):
| | 무엇 | 막는 것 |
|---|---|---|
| 계측 | 서버에 **이벤트 루프 지연** 로그 추가(주기 태스크가 sleep 실측) — flush 때 튀면 확정 | 코드 변경 |
| 실험 | `gcloud run services update --cpu 2` (즉시 되돌림 가능) | **안드로이드 폰 필요** + GIL 이면 무효 |
| 수정 | flush 를 잘게 쪼개 한 번에 2~3개씩만 변환 | 코드 변경, 크래시 시 덜 저장 |

### ❓ "그럼 브라우저(데모 페이지)로 통화할 땐 왜 안 버벅이나" — 자주 나오는 질문

같은 서버인데 브라우저는 멀쩡하다. 가설을 검토할 때마다 걸리는 질문이라 답을 박아 둔다.

⚠ 먼저 표현 정정 — **서버 CPU 가 "터지는" 게 아니다.** 실측 통화 중 CPU 는 10% 남짓이다.
정확히는 **1분마다 몇 초 동안 오디오 전송이 밀린다.** 소리가 사라지는 게 아니라 늦게 간다.

그래서 **늦게 가도 티가 안 나는 클라**와 **바로 굶는 클라**가 갈린다:

| | 브라우저 | 안드로이드 |
|---|---|---|
| 재생 방식 | `n.start(playT)` — 오디오를 **하드웨어 타임라인에 미리 예약** | `AudioTrack` 에 조각을 **계속 밀어 넣어야** 함 |
| 앱이 멈추면 | 예약된 소리는 **그대로 재생됨** | 즉시 굶음 |
| 앞당겨 잡아둔 양 | 몇 초(3.4배로 오니 자연히 쌓임) | `bufferSize 16384` = **0.17초** |

⇒ 버퍼 "크기"가 아니라 **누가 그 버퍼를 들고 있느냐**가 다르다. 브라우저는 하드웨어가 들고
있어 앱 사정과 무관하고, 안드로이드는 앱 스레드가 0.17초 창으로 계속 떠먹여야 한다.
iOS 가 괜찮은 것도 같은 구조로 보인다.

✅ **1분 주기는 클라 것이 아니다** — Flutter 클라에 60초 타이머가 **없다**(확인: 1초=경과 표시,
3초=종료 마감뿐). 서버 `_periodic_flush` 가 여전히 유일한 1분 주기 작업이다.

### ⭐ 사장님 판단 (2026-08-22): 클라에서 할 수 있는 건 다 했다

*"내가 볼 때는 이게 문제가 맞는 거 같아. 클라에서 해 볼 수 있는 거 다 했거든."*
⇒ 완충재를 키울 만큼 키웠는데도 남았다 = **때리는 쪽(서버)을 멈추는 것 말고 답이 없다.**
클라 버퍼를 더 키우는 방향은 접는다.

### 📋 그래서 내일 순서 (안드로이드)

1. `.env` → demo-api, **재빌드**(위 0번)
2. 안드로이드로 통화. **버벅인 시각을 메모**한다
3. 내가 그 통화의 `점진 flush` 로그 시각과 대조 → 겹치면 **확정**
4. 확정되면 아래 중 선택

**서버 수정 후보** (⛔ 전부 승인 필요):

| | 무엇 | 지키는 것 / 잃는 것 |
|---|---|---|
| **C1** | flush 1회에 **변환 개수 상한**(예: 3개) + 주기 단축(예: 20초) — 총량은 같고 **잘게 편다** | 3초 버스트 → 0.7초 이하. 크래시 내성·RAM 해제 유지. 코드 소폭 |
| **C2** | 통화 중엔 **raw PCM 으로 업로드**하고 mp3 변환은 통화 후 | ffmpeg 를 통화 경로에서 완전히 뺀다. 크래시 내성·RAM 해제 **둘 다 유지**. 대신 통화 중 업로드량 ~3배(48KB/s vs 16KB/s), 후처리 단계 신설 |
| **B** | `--cpu 2` | 코드 무변경·즉시 롤백. ⚠ 기전이 GIL 이면 **무효** |

⚠ C2 가 성질상 제일 깨끗하지만 변경이 크다. **C1 로 먼저 재보고**, 부족하면 C2 로 가는 게
순서로 맞아 보인다.

### ⛔⛔ 폐기된 가설 — 다시 파지 마라

| 가설 | 왜 폐기 |
|---|---|
| 무음 종료의 원인이 **컨텍스트 압축** | 같은 통화에서 압축이 `t=90`·`t=234` 에도 걸렸고 그때는 멀쩡히 말했다. 같은 초에 찍힌 걸 인과로 읽은 실수 |
| 웃음 60초가 **파이프라인이 늘린 것** | 서버는 받은 바이트만 쌓는다(`cur_beaver_pcm.extend(event.audio)`). mp3 엔트로피 60초 전 구간 7.86~7.93 균일(무음 없음). 1,248 프레임 중 **중복 0**(루프 아님) ⇒ 모델이 진짜 60초를 낸다 |
| **오디오 큐를 잘라** 자막에 맞추기 | 웃음 **뒤에** 오는 말까지 잘린다(웃음이 앞, 질문이 뒤) |
| **자막 없는 구간**으로 웃음 판별 | 웃음도 `ㅋㅋㅋ` 로 전사된다 |
| **턴 시간 상한**으로 자르기 | 위와 같음 + 정상 턴 최대가 15.4초라 여유가 얇다 |
| flush 에서 `upload_audio=False` 로 미루기 | pending 이 PCM 을 들고 있어 **RAM 이 폭증**한다(`_release_persisted_pcm` 이 막던 바로 그것) |

### 미결 (사장님 판단 대기)

1. 웃음 60초를 더 줄일 것인가 — 프롬프트를 더 조일지, 캐릭터 DB(`character.personality`)를 볼지
2. 안드로이드 버벅임 — 위 세 후보 중 무엇부터
3. 자막/소리 어긋남 — **웃음 구간 한정**이고 말에서는 안 난다(사장님 확인 2회).
   데모 페이지에서 자막을 재생 시점에 맞추는 수정을 만들었다가 **승인 없이 고쳐서 되돌렸다.**
   할지 말지 미정

---

## 10. 🔎 2026-08-23 아침 — 사람 없이 되는 조사 3건 (코드 변경 0)

통화·`.env` 는 사장님 손이 필요해 막혀 있다. 그 사이 §9 에 "아직 안 봤다"로 남긴 것과,
실험 설계를 막고 있던 의문을 코드로 가렸다. **전부 읽기만 했고 고친 것은 없다.**

### 10.1 ✅ 8/4 코드에도 `_periodic_flush` 가 있었다 — 기준은 안 흔들린다

§9 에서 "확인하면 가설이 더 단단해진다"고 남긴 것. 확인했다.

`test-api` 가 물고 있는 8월 4일 스냅샷(`ddc82e5`)의 `call_session.py`:

| | 8/4 (`ddc82e5`) | 현재 (`HEAD`) |
|---|---|---|
| `FLUSH_INTERVAL_S` | **60.0** | **60.0** |
| `_periodic_flush` 루프 | 있음 | 있음 (**본문 동일**) |
| `save_segments(...)` 호출 | 같음 | 같음 |
| 차이 | — | `_release_persisted_pcm` 한 줄 추가뿐 |

⇒ **방아쇠는 두 서버에 똑같이 있다.** demo-api 로 옮긴다고 버벅임이 사라질 이유가 없고,
사라진다면 오히려 원인이 다른 데 있다는 신호다. §9 의 "기준이 흔들린다" 걱정은 해소됐다
(단, 그 사이 다른 변경이 많으니 **재확인은 여전히 한다** — 순서는 안 바뀐다).

### 10.2 ⭐ GIL 가설은 약하다 — 무거운 일이 둘 다 **파이썬 밖**에 있다

§9 는 "기전이 GIL 경합이면 `--cpu 2` 실험이 무효"라 적고 그래서 B 를 뒤로 미뤘다.
배관을 끝까지 따라가 보니 **그 걱정의 근거가 약하다.**

```
_periodic_flush
  └ svc.run_db → run_in_threadpool(_work)        ← 워커 스레드. 이벤트 루프 직접 차단 아님
      └ save_segments → _upload_segment_pcm       ← 세그먼트마다 1회씩
          ├ pcm16_to_mp3 → _ffmpeg_to_mp3
          │    └ subprocess.run(["ffmpeg", ...])  ← ⭐ 별도 **프로세스**
          └ storage.upload → blob.upload_from_string  ← ⭐ **네트워크 I/O**
```

- **ffmpeg 는 라이브러리가 아니라 subprocess** (`core/audio.py:205`). 인코딩은 다른
  프로세스에서 돌고, 파이썬 스레드는 파이프에서 **기다린다** ⇒ 그동안 GIL 을 놓는다.
- **GCS 업로드는 소켓 I/O** ⇒ 역시 GIL 을 놓는다.

⇒ 1분마다 도는 무거운 일 중 **GIL 을 오래 쥐는 구간이 없다.** 남는 기전은
**프로세스끼리의 CPU 경합**이다 — `--cpu 1` 컨테이너에서 ffmpeg 가 코어를 물면
파이썬 asyncio 루프가 스케줄에서 밀린다. 이건 **`--cpu 2` 가 정확히 겨냥하는 것**이다.

⇒ **B(`--cpu 2`)를 "무효일 수 있다"고 미뤄 둘 이유가 사라졌다.** 코드 변경 0, 즉시 롤백,
가장 싼 실험이다. ⚠ 단 여전히 **가설이지 확정이 아니다** — 3번(로그 대조)이 먼저다.

**비용 형태도 같이 보였다**: 실측이 14개/3.1초, 12개/3.0초, 6개/1.6초로 **개수에 선형**
(≈0.22초/세그먼트)이다. 오디오 길이가 아니라 **세그먼트 개수**가 값을 정한다 ⇒
프로세스 기동 + 업로드 왕복의 **건당 고정비**라는 뜻이고, C1(개수 상한)이 버스트를
바로 자르는 이유도 이것이다.

### 10.3 ✅ 손으로 준 `--cpu 2` 는 CI 배포에 안 지워진다

B 를 걸어 놓고 잊으면 다음 배포가 되돌려 놓는 것 아닌가 — 확인했다. **안 지워진다.**

`.github/workflows/deploy.yml:120` 의 배포 스텝은 **`--image` 만** 준다
(주석에도 *"env·시크릿·서비스계정은 건드리지 않는다"* 로 못 박혀 있다).
`--cpu`/`--memory`/`--concurrency` 를 지정하지 않으므로 기존 서비스 설정이 그대로 유지된다.
⇒ 실험을 걸어도 CI 가 조용히 되돌리지 않고, **끝나면 사람이 명시적으로 되돌려야 한다**
(그래서 되돌리는 것도 잊으면 안 된다).

### 10.4 그래서 오늘 순서 (§9 에서 바뀐 것만)

§9 의 1~3번(`.env` 재빌드 → 통화·시각 메모 → flush 로그 대조)은 **그대로**다.
4번 "확정되면 무엇부터"만 바뀐다:

| 순서 | | 근거 |
|---|---|---|
| **1** | **B (`--cpu 2`)** | 10.2 로 무효 우려 해소, 코드 0줄, 10.3 으로 CI 안전. 제일 싸다 |
| 2 | **C1** (변환 개수 상한 + 주기 단축) | B 로 부족하거나 근본 해결을 원하면. 10.2 의 "건당 고정비"를 직접 겨냥 |
| 3 | **C2** (통화 중 raw PCM) | C1 도 부족할 때. 성질은 제일 깨끗하나 변경이 크다 |

⛔ 셋 다 사장님 승인 필요. 아직 아무것도 안 건드렸다.


---

## 11. ⭐⭐ 2026-08-23 저녁 — **안드로이드 버벅임 원인 규명. 서버가 아니라 클라였다**

실통화 2건(`call_id=1145` 7분49초, 이어서 8분+ 1건). 앱은 `C:\deveavertalk-flutter-dev`
릴리스 빌드를 실기기(Galaxy S22, Android 16)에 설치해 `adb logcat` 으로 전 구간 캡처했다.

### 11.0 ⛔ 먼저 — §9 의 클라 서술이 낡았고, 체크아웃도 틀렸었다

- `.env` 가 test-api 를 본다는 §9 의 진단은 **`바탕 화면eavertalkeavertalk-flutter`** 사본을
  본 것이었다. 실제 빌드가 나오는 곳은 **`C:\deveavertalk-flutter-dev`** 이고 거기 `.env` 는
  **8/16부터 demo-api** 였다(8/16 APK 안에 번들된 값을 풀어서 확인). 앱은 이미 demo-api 를 보고 있었다.
- §9 의 "`FlutterSoundPlayer bufferSize: 16384` = 0.17초" 도 옛 클라다. 실제 클라는
  `flutter_pcm_sound` **로컬 패치본** + Dart 자체 시계 푸시 + 쿠션 **저수위 1.2s / 목표 2.5s**.

### 11.1 ⛔⛔ **`_periodic_flush`(1분) 가설 — 기각**

굶음 38건 × flush 7회 전수 대조. **flush 창[완료−3초, 완료]에 든 굶음 0건.**

| flush 완료 | 가장 가까운 굶음 |
|---|---|
| 17:42:19 | +2.89s | 
| 17:43:22 | −3.42s |
| 17:44:24 | +0.73s |
| 17:45:26 | −8.04s |
| 17:46:27 | +6.02s |
| 17:47:29 | −14.95s |
| 17:48:30 | +24.15s |

굶음 평균 간격이 12.4초라 근처에 오는 건 우연이고, **정작 창 안은 전부 비어 있다.**
⇒ **두 달간 서버(flush·CPU·GIL·`--cpu 2`)를 판 것은 틀린 시스템이었다.**

### 11.2 클라의 `starved` 카운터는 과대계상이다 — 큰 굶음 3건은 "턴 사이 대기"였다

`audio resumed after 5320ms` (17:47:46~51) 를 서버 로그와 초 단위로 맞추니:
```
17:47:46.262  [클라] 굶음 시작
17:47:48.885  [서버] 👤 user: 꽃을 피워요.      ← 학습자가 말하던 시간
17:47:49.268  [서버] 🦫 beaver: "나무가 꽃을     ← 여기서야 새 턴 시작
17:47:51.582  [클라] audio resumed after 5320ms
```
2405ms 건도 같다. ⇒ `_beaverSpeaking` 상태가 턴 경계를 넘어 남아 있는 동안 큐가 빈 것까지 센다.
**`starved` 건수만으로 판정하지 마라**(8/02 의 "26회 중 실제로 들린 건 8회"와 같은 얘기).

### 11.3 ⭐ 진짜 신호 — **오디오를 손에 들고도 30%로 재생한다**

`fedAud`(엔진에 넘긴 오디오 초) ÷ 벽시계 = 재생 속도. 사장님이 지목한 시각과 정확히 겹친다.

```
 통화시각   재생속도   대기중 오디오
   305s       90%       5.0초
   310s       30%       3.5초   ← 사장님 "5분 9초"
   330s       36%       4.5초
   380s       60%       5.3초
   385s       30%       3.8초
```
**30 / 60 / 90% = 1.5초의 배수** — `_feedChunkBytes = 72000`(24kHz PCM16 = 정확히 1.5초) 1회분이다.
즉 **5초 동안 피드가 한 번밖에 안 일어났다.**

### 11.4 ⭐⭐ 계측을 넣어 갈랐다 — **A(feed 가 붙잡힌다) 확정 / B(타이머 굶음) 기각**

`normalcall_controller.dart` 에 로그 전용 계측 4곳 추가(동작 무변경):
`feed()` 왕복 시간 · `FEED SLOW`(300ms 초과 시) · 푸시 타이머 실제 간격 · 5초 `PUMP` 요약에 `feedMax/tickMax`.

실통화 결과 — **`FEED SLOW` 105회, 최장 6,504ms**:
```
[call 433.6s] FEED SLOW 2958ms to push 1500ms audio (queue 190634B)   ← 사장님 "7분 14초"
[call 444.4s] FEED SLOW 6231ms to push 1500ms audio (queue  46634B)
[call 450.9s] FEED SLOW 6504ms to push  971ms audio (queue      0B)
[call 476.9s] FEED SLOW 4759ms to push 1500ms audio                   ← 사장님 "7분 57초"
```
같은 창의 **`tickMax` 는 통화 내내 40~47ms**(500ms 넘은 창 0개, 유일 예외 123ms 1회).
⇒ **Dart 타이머는 완벽했다. B 기각.** 큐가 `0B` 인 순간에도 끊긴다 = **못 받아서가 아니라 못 넘겨서다.**

### 11.5 ⭐⭐⭐ 원인 — **메서드 채널이 안드로이드 메인 스레드를 탄다**

`FlutterPcmSoundPlugin.java:412` 의 feed 핸들러는 **블로킹 작업이 전혀 없다** — 락 없는
`LinkedBlockingQueue` 에 add 하고 즉시 리턴한다(`AudioTrack.write(WRITE_BLOCKING)` 은 별도
재생 스레드라 무관). 그런데 왕복이 6.5초다 ⇒ 그 시간은 **일한 시간이 아니라 메인 스레드가
비기를 기다린 시간**이다.

`:280` `new MethodChannel(messenger, CHANNEL_NAME)` — **TaskQueue 미지정 ⇒ 핸들러가 메인 스레드에서 돈다.**
게다가 `MAX_FRAMES_PER_BUFFER = 200` 이라 1.5초를 **180조각**으로 쪼개는 할당도 메인에서 한다.

⭐ 이건 `normalcall_controller.dart:362-366` 이 이미 적어 둔 문제다 — *"재생 경로의 심장박동이
안드로이드 메인 스레드를 탄다. 이 파일이 키워 온 모든 쿠션(200ms→600ms→1.5s)은 바쁜 UI
프레임을 상대로 시간을 사는 것이었다."* 그때 **트리거만** Dart 로 옮겼고 **전달 경로는 그대로
메인 스레드를 탔다.** 브라우저가 안 버벅이는 이유도 이 홉이 없어서다. 쿠션을 올리든 내리든
증상이 안 없어진 것도 전부 이걸로 설명된다.

### 11.6 넣어 둔 수정 — ⚠ **아직 빌드·검증 안 함**

`FlutterPcmSoundPlugin.java:280` 채널을 백그라운드 TaskQueue 로 내렸다
(`messenger.makeBackgroundTaskQueue()`). 플러그인 상태가 `LinkedBlockingQueue` + `Atomic*`
뿐이라 락 없이 안전한 모양이다. **커밋 안 했고 빌드 안 했다**(사장님 지시: 나중에).

⭐ **검증이 곧 수정이다** — 통화해서 `FEED SLOW` 가 사라지면 원인 확정 + 해결. 남으면 메인
스레드가 범인이 아니라는 뜻이니 그때 **누가 메인을 잡는지**(아바타 애니메이션 `sync_avatar.dart`,
자막 리빌드, `logNative` 채널 홉 — 셋 다 미커밋 수정본)를 찾는다.

### 11.7 남은 것 / 미결

1. ⛔ **demo-api 에 `NORMAL_CALL_DURATION_S=900` 이 걸려 있다.** 되돌려야 한다:
   `gcloud run services update beavertalk-app-demo-api --project bt-dev-web-01 --region asia-northeast3 --remove-env-vars NORMAL_CALL_DURATION_S`
   (Claude 는 분류기에 막혀 인프라 변경을 못 친다 — 사람이 쳐야 한다)
2. **종료 시드(`849a25b`)는 여전히 미검증** — 900초 설정인데 통화가 469초/500초에 끊겨 종료 절차까지 안 갔다.
3. 별건으로 **턴 사이 공백이 7.6초 → 25.1초로 자란다**(8/11 의 13.9→25.6과 같은 곡선).
   학습자 발화 시간이 포함된 값이라 그대로 지연은 아니지만 25초는 말 길이로 설명이 안 된다.
   **끊김은 클라, 굼뜸은 서버** — 둘을 섞지 마라.
4. 앱은 폰에서 **삭제했다**(사장님 지시). APK 는 `C:\deveavertalk-flutter-devuildpp\outputslutter-apk\` 에 있다.

### 11.8 ⛔⛔ 폐기 목록에 추가

| 가설 | 왜 폐기 |
|---|---|
| **버벅임의 원인이 `_periodic_flush`(1분)** | 굶음 38건 중 flush 창 안 **0건**(11.1). 큐에 오디오를 들고도 못 내보내는 게 증상이라 서버 작업과 무관 |
| **`--cpu 2` / GIL / CPU 경합** | 위와 같은 이유로 목표가 틀렸다. 걸지 마라 |
| **클라 쿠션(1.2s↔2.5s)을 조정해 해결** | 엔진이 322ms 넘게 안 차므로 문턱 자체가 죽은 값(`:382-391`). 원인은 채널 홉이다 |
