# 표현학습·프리토킹 — 결정 로그 (누적)

```
작성일   2026-09-10
목적     ⛔ 결정이 대화에만 있으면 컨텍스트 압축 때 사라진다. 여기가 원본이다.
기획     docs/plans/2026-09-10-표현학습-프리토킹-통화-분리.md
구현계획  docs/20260910_0230_표현학습-프리토킹-구현계획.md
프롬프트  docs/prompts/README.md §8 (2026-09-10 1·2차)
브랜치   feat/expression-course  (⛔ dev 아님)
```

⚠ 이 문서는 **결정과 그 근거**만 담는다. 설계 상세는 기획서, 구현 상세는 구현계획에 있다.

---

## 0. ⚠ 지금 제일 문제인 것 (2026-09-11, 위가 급함 — 노션 §0 과 동일)

해결되면 여기서 지우고 §4 에 결정으로 남긴다. 두 판 두지 않는다.

1. **퀴즈 통과 저장** — LLM 판정이 곧 영구 기록. 사이드카 → `expr_quiz_pass` → 조각 끝
   `member_item_progress.quiz_passed_at`(단조, D12) → 선별 `quiz_passed_at IS NULL` 로 영구 제외.
   오판(1398 거짓 통과 1건) = 항목 영구 소실. provenance·인용 없음 → 정정 수단 없음(Q25).
   레벨 시스템 관통원칙 ①(인용 검증)을 이 코스는 안 지킨다.
   → **T16~T18(2026-09-11 21:00) ◐ 거의 해결**: 서버가 퀴즈를 열고(3개마다 «[안내]» 큐) 그 창 안에서 passed/failed 를
   파이썬이 첫 사건으로 찾는다. LLM 은 STT 폴백만. 하네스 불일치 7/18 → 4/18·0/10 → 1/16·0/18. 남은 건 STT 철자(Q27)·Q25.
2. **드릴(배웠는가) 체크** — «배웠다» 가 아니라 «비버가 꺼냈다» 를 센다. `covered_nums` = 비버 발화
   라벨 문자열 대조(`_note_covered_items`, 즉시·append-only) ∪ 사이드카 drilled. 학습자가 따라
   말했는지(사장님 정의)는 안 본다. 침묵해도 «드릴함». 상태: 미결.
3. **5분 조각 이어하기 진도 전달** — 조각 끝 ①판정(상한 2.0s) ②state ③DB → 조각2 는 새 state 로
   선별 재실행(random). 2.0s 넘으면 마지막 구간 통과분 유실. 목록·번호가 바뀌고 `expr_quiz_fail` 은
   DB 에 없어 조각2 오답퀴즈 재료 0, 퀴즈 주기 1부터, «이미 한 번 배운 것» 사실 미전달. 상태: 배관만.
4. **비버 대본 미준수(드리프트)** — 1397 모국어 순서·판정 4중 실패, 1398 항목마다 퀴즈·거짓 칭찬·
   공개 뒤 되묻기. 서버가 강제할 수단은 재접지 쪽지뿐.
   → **T16 ◐ 퀴즈 박자 해결**: 서버 큐 18/18 앵커, 비버 단독 퀴즈 0, 거짓 칭찬 0(1407·1408). 남은 것: 되감기(G)·포기 2회(H).
5. ~~**판정 창 경계**~~ — ✔ T16 에서 개념 삭제(서버가 창을 연다). 겹침·경계선·커서·12k 절단 전부 제거(25c64fe).
6. **오답퀴즈 재료** — `expr_quiz_fail` 메모리 전용, 결과 화면 «못 맞힘» 없음. 미착수.
7. ~~**검증 수단**~~ — ✔ E2E 하네스 완성(`scripts/e2e_expression_call.py`, 06fce6a~f9d0ec0). 8통화(1401~1408) 자동 채점.
8. 기타 — 제어 태그 필터 휴리스틱 / `EXPR_REGROUND_MIN_ROOM_TOKENS=3000` 1건 추정 / dev_signup 공유
   클라이언트 오염 / `korean_level=None` 이면 `or 2` 로 L2 선별(`call_session.py:2321`).

## 1. 기획 단계 결정 (D1~D18) — 사장님 23문답, 2026-09-09~10

| # | 결정 | 근거·비고 |
|---|---|---|
| D1 | **통화 자체를 가른다**(모드 전환 없음). 버튼 2개로 진입 | ⛔ Live 의 `system_instruction` 은 setup 에서 한 번 정해지고 못 바꾼다 |
| D2 | 공통 규칙은 변수로 공유, 학습 플로우는 따로 | `core/prompts/common.py` 가 소유 |
| D3 | 체크 축 2개 → **노출 1회** + **퀴즈 통과** | ⚠ 최초 «3회» 를 사장님이 1회로 정정 |
| D4 | 못 한 항목은 다음 통화 앞으로. «못한 것 + 그다음» 으로 18개 | SQL 정렬이 자동 처리 |
| D5 | 15분 기준. **다 못 들어가도 된다** | |
| D6 | 퀴즈는 3개마다. 오답퀴즈는 **이 통화 오답만** | |
| D7 | **힌트 화면 UI 자체를 없앤다** | |
| D8 | 프리토킹은 **전부 학습 언어**. 항목·진도 기여 없음 | 주제·활용은 미기획 |
| D9 | 선별 함수를 새로 만든다. 어휘·문법 모두 예문 사용 | ⚠ chunk 는 예문 0개(전 언어) |
| D10 | 퀴즈 판정은 **사이드카(LLM)**, 주입은 **재접지와 같은 타이밍** | 혼자 두 번 말하는 버그 방어를 물려받는다 |
| D11 | 노출 카운트는 **서버 문자열 대조**(9638a26 재사용) | LLM 0 |
| D12 | 레벨업 = **그 레벨 전체 퀴즈 통과**. 강등·등급 불필요 | ⚠ 규모: L1 3통화 / 전체 627통화 |
| D13 | 생존회화(L1) 46개를 18개씩 잘라서 준다 | |
| D14 | 선별은 **동일 레벨 내 랜덤**. 통과분 제외 | |
| D15 | 퀴즈 = **배운 문장 맞히기**. 정답이 곧 항목 표면형 | ⭐ 이게 판정을 단순하게 만든다 |
| D16 | 선톡 후 **바로 1번 항목**. 딴소리는 한 마디 받고 복귀. 남으면 응용 | |
| D17 | 조각 경계는 **서버 상태를 지시문에 주입** | 사이드카·JSON 불필요 |
| D18 | 새 `call_type` 2개 + **폴더 분리** | |

---

## 2. 구현 중 결정

### 2-1. 빌더 질문 (묶음 A 착수 전)
| # | 결정 |
|---|---|
| Q1 | 조각2 에 통과분 포함 — 명세 §2-7 그대로 |
| Q2 | 퀴즈는 **하나씩** 묻는다 |
| Q3 | 레벨업은 **함수만 만들고 호출 안 함** ⚠ 그런데 함수 자체가 미구현으로 남았다(§4 참조) |
| Q4 | 진도 커밋은 **조각 종료 시에만** |
| Q5 | 일일 한도는 `call_type` 별로 **각자 센다**(그대로) ⚠ §3-1 로 대체 예정 |

### 2-2. 사장님 결정 A·B (묶음 A QA 뒤)
| # | 결정 | 근거 |
|---|---|---|
| **A** | 프리토킹 **모국어 예외 유지** | 예외는 **두 자리뿐**: ①학습자가 «이거 어떻게 말해요?» 물을 때 ②대화가 정말 멈췄을 때 한 마디. ⛔ 그 밖으로 넓히지 않는다 — 넓히면 일반 통화가 된다 |
| **B** | «다 끝내고 남으면» = **짚어 주기 + 산출, 둘 다** | ⛔ 빌더가 근거로 든 call 870(«본편 끝났다» → 4분24초 자체 종료)은 «끝났다» 어휘 금지 근거지 «설명 삭제» 근거가 아니다 — 내가 잘라냈다. ⭐ 빌더가 «산출 1회 = 끝 신호» 를 스스로 잡아 반복성 한 줄을 더했다 |

### 2-3. 조각 상한 (사장님)
```
조각 상한은 지금 그대로 — Free 1조각(5분) / Pro·Max 3조각(15분)
⇒ 표현학습만의 예외를 만들지 않는다. CALL_FRAGMENTS_BY_PLAN 무수정
⇒ Free 는 5분에 끝나고 미완은 다음 통화 앞으로(정렬이 처리)
```
⚠ 실측: Free 미도달 항목의 지시문 몫 = 청크 117자 / 어휘 837자(≈300~500토큰).
⇒ `EXPRESSION_ITEMS_PER_CALL` 을 **인자로 열어만** 두고 18 고정.

### 2-4. 드릴·정답 검출에 **학습자 발화까지** 본다 (사장님)
```
⛔ 내 명세가 틀렸다. 9638a26 의 «비버만 본다» 규율은 일반 통화 전제였다.
   표현학습은 비버가 모국어로 묻고 정답을 안 말한다:
      비버   : "헤어질 때 뭐라고 하지?"   ← 라벨 없음
      학습자 : "안녕히 가세요"             ← 여기서만 나온다
   ⇒ 비버만 보면 그 항목이 영원히 «안 다룬 것» 이 된다.
⭐ 적용 범위는 표현학습 한정. normal 은 «비버만» 그대로(9638a26 규율)
```

---

## 3. 별건으로 미룬 것

### 3-1. 일일 한도 → **시간 예산제**(사장님 설계, 큐 Q24)
```
Free 5분 / Pro 15분 / Max 15분      ← 코스 둘이 예산 **공유**
5분마다 재연결은 그대로
2분 쓰고 끊으면 → 다음 통화는 2:00 부터 시작
⛔ 맥락은 안 이어진다(대화는 처음부터)   ⭐ 진도는 이어진다(DB 행이라 자동)
그리고 normal 통화는 없앨 예정
```
⚠ **지금 당장은 아니다** — `ENV='test'` 라 한도가 아예 안 돈다(`call_service.py:342`).
⛔ 그동안 `DAILY_CALL_LIMIT` 에 `expression:1 · freetalk:1` 을 **넣어 둔다** —
빼면 `:352` 가 «정의되지 않은 콜타입은 막지 않는다» 로 떨어져 **무제한**이 된다.

### 3-2. 옛 승급 사슬 코드 정리 (큐 Q23)
등급·강등·게이트·fast-track·`status`·카운터 4종. ⛔ 지금은 «안 쓰기만» 한다.

### 3-3. `service/__init__.py` eager import
`quiz_judge` 를 패키지 경유로 import 하면 SQLAlchemy·settings 가 붙는다(640 모듈).
⇒ import 그래프 시험으로 계약만 잠갔다. 모듈 이동은 **안 한다**(계층이 어긋난다).

---

## 4. ⭐ 판정 구조 — **확정** (사장님, 2026-09-10 「맞아 이대로 지시해」)

### 뿌리: **서버가 «지금 몇 번 문항을 묻는지» 모른다**
비버가 퀴즈를 모국어로 내므로 한국어 정답이 비버 말에 안 나온다.

| # | 증상 | 결과 |
|---|---|---|
| **P0-1** | 틀린 답("모르겠어요")에 붙일 번호가 없다 | 오답 목록이 비어 **오답퀴즈가 낼 문제가 없다** |
| **P0-2** | 드릴 재시도와 퀴즈 정답을 «창 크기» 로 못 가른다 | 1턴 → 새고 · 2턴 → 정상 퀴즈를 막는다 |
| **P0-3** | 3개 묶음이라 마지막 1~2개는 퀴즈 기회가 없다 | `quiz_passed_at` 영원히 NULL ⇒ **레벨을 절대 못 뗀다**(L1 46 = 18+18+10 → 끝 1개) |

⇒ 그래서 **표현학습은 지금 레벨이 오르지 않는다.** `expression_level_complete` 도 미구현.

### ⛔ 내가 틀렸던 것 (기록)
```
1) «앵무새 창을 2턴으로 넓혀라» 지시 → codex 가 대본으로 반박. 되돌렸다
   ⭐ 그 대본을 회귀로 박제했다(test_a_quiz_right_after_the_drill_still_passes)
     — 누가 다시 2턴으로 넓히면 그 시험이 깨진다
2) «tool 로 문항 번호를 알리자»(A안) 권고 → 사장님 지적으로 철회.
   tool 은 안 될 경우가 많다(SET_FACE_TOOL + AI Studio 2.5 = 0/21 전부 1011).
   내가 «Vertex 는 괜찮다» 고 한 것은 **tool 없이** 40세션 잰 결과였다 — 비약이었다
3) «캐릭터 3필드(role·personality·rules)» → rules 는 7476bba 에서 드롭됐다. 2필드다
4) «§2-7 조각 승계 = 18개 전부 + 1~N 통과 표시» → 조각2는 새 소켓이라 picker 가 다시 돈다
5) «_EXTRA_CHARS_MAX 를 상수로 빼라» → 이미 상수였다. 내 grep 오류
```

### ⭐ 확정 설계 — «판정은 LLM 이, 쓰기는 파이썬이»

```
① «비버가 이 문장을 말했나»   서버 문자열 대조 — 유지 (판단이 아니라 사실 확인)
② «퀴즈를 맞혔나»             사이드카(LLM) 전담
                              ⛔ 문자열은 퀴즈 통과를 한 건도 주지 않는다
③ 쪽지 내용                   LLM 이 «다룬 것(①에 합집합) / 맞춘 것 / 틀린 것» 을 낸다
④ 주입 타이밍                 압축 감지 → LLM 띄움 → 학습자가 말하는 턴에 얹기
                              (마이크 프레임 + RMS 2관문 — 비버가 혼잣말하는 것 방지)
⑤ 조각 끝 순서                ⭐ LLM 이 마지막 판정 → 파이썬이 state 갱신 → DB 쓰기
                              (상한 1.2초, 늦으면 있는 것만 쓰고 진행 — R5)
⑥ 안전망                      통화후 분석 AI 가 전사를 한 번 더 읽어 확정·보정
⑦ 삭제                        포함 통과 규칙 · _EXTRA_CHARS_MAX · 앵무새 관문(양쪽) · 3갈래 분기
⑧ 프롬프트                    "3개가 안 남았으면 남은 것으로 퀴즈를 낸다"          → P0-3
⑨ 레벨업                      P0-3 이 막히면 expression_level_complete 를 넣는다
⛔ tool 안 쓴다 · 새 상태기계·순서 카운터 안 짠다
```

**세 문제가 이렇게 닫힌다**
| # | 어떻게 |
|---|---|
| P0-1 | LLM 이 «비버 질문 + 학습자 답» 을 짝으로 읽는다 — «고마울 때 뭐라고 해?» → 「감사합니다」 를 스스로 짝짓는다. 번호 카운터가 필요 없다 |
| P0-2 | 문자열이 통과를 안 주니 «따라 말한 것을 통과로 세는» 일이 애초에 안 생긴다. 창 크기 딜레마 자체가 사라진다 |
| P0-3 | 프롬프트 한 줄 (⑧) |

**⑤ 순서를 바꾸는 이유 + ⛔ 내가 처음 낸 근거의 정정**
```
⛔ 「조각 경계에 2.9초가 놀고 있다」 는 틀렸다.
   call_session.py:2070-2074 주석이 이 경합을 이미 기록해 뒀다 —
     07:00:48 조각1 저장 → 07:00:51 조각2 접속 → 07:00:53 요약 완성
   조각2 가 요약을 앞질렀고, 발췌 폴백을 타서 비버가 첫 인사를 글자까지 다시 했다.
   ⇒ 그 4.3초는 이어하기 요약(fire-and-forget)이 도는 시간이다. 놀고 있지 않다.
⭐ 그래서 상한은 2초가 아니라 **1.2초** — 실측 요약 1.0~1.4초, 최악 간격 3초 ⇒ 여유 1.8초
⚠ 최악 사례의 피해는 데이터 오염이 아니라 **재드릴 1회**다
   (:2385 주석 — "재드릴은 선별상 정상 경로다"). 막아야 하지만 착수를 미룰 이유는 아니다
⭐ 요구 회귀 2건: 판정이 멈춰도 저장된다 · 판정 결과가 저장에 반영된다
   + 로그 1줄(판정 ms · 상한 초과 여부) — 경합을 나중에 실측할 유일한 근거
```
지금은 5분 종료 → 파이썬이 즉시 DB 에 쓴다 ⇒ **마지막 arm 이후 구간의 답이 판정 없이
버려지고**, 조각2 가 그것들을 또 가르친다.

**⚠ ① 을 남기는 이유** (사이드카만으로는 안 되는 자리)
```
· arm 은 사이드카를 기다리지 않는다 — 실측 1.18초 vs 1.4초(통화 1360).
  문자열이 없으면 첫 쪽지가 빈손으로 나간다 — 그게 9638a26 이 고친 버그다
· 조각2 는 새 세션이라 이전 대화가 0이다 — 서버가 «어디까지» 를 알려줘야 한다
⇒ 단 LLM 결과와 **합집합**으로 얹는다(9638a26 이 정한 규율). 라벨과 다른 말로
  가르친 경우를 LLM 이 보완한다 (codex P1-7)
```

**⚠ 기획서 §2-8 정정 — «압축이 최대 적» 은 과장이었다** (사장님 지적)
```
· sliding window 는 최근 것을 남긴다 ⇒ 비버는 «방금 뭘 했는지» 를 안다
· 지시문(항목 목록)은 압축에 안 지워진다
· 통화 1360 의 진짜 원인은 9638a26 이 적은 그대로 —
  «원인은 압축이 아니라 우리가 넣어준 쪽지다» (covered=3/3 인데 실제 7개 드릴 → 부분 목록이 되감기)
⇒ 압축은 «적» 이 아니라 «쪽지를 최신단에 두어야 하는 이유» 다
⚠ 조각 경계는 압축과 무관하게 서버 상태가 필요하다 — 그건 그대로다
```

## 4-B. 사장님 결정 (2026-09-10 저녁)

```
① 커밋 귀속줄        「귀속줄 놔둬 다음부터 빼고해」
   ⇒ 이미 들어간 5건은 **그대로 둔다.** 소급 rebase 안 한다
      서버   05acffc · 2898236 · 42b2e37 · 0b09b41
      플러터 3a65886
   ⇒ 다음 커밋부터 Co-Authored-By·Claude-Session 을 뺀다
   ⚠ 전달이 늦은 것이 내 잘못이다 — 하위 에이전트 **첫 지시문**에 넣어야 한다

② Q0(회귀 34초)을 dev 로   「dev는 일단 놔둬 나중에 이거 테스트 다 되면 그때」
   ⇒ 지금은 feat/expression-course 에만 둔다(a985e01)
   ⚠ 대가: 다른 판넬·다른 브랜치는 **여전히 회귀 10분 35초**다.
      표현학습 검증이 끝나 dev 로 갈 때 같이 간다

③ 워크트리 정리       완료 — 약 74MB 회수
   삭제  feat-cascade-voice (워크트리 + 브랜치 · dev·main 완전 병합 · 고유 커밋 0)
   해제  chore-test-cleanup · feat-ptt-call (워크트리만 · **브랜치 보존**)
         ⛔ chore/test-cleanup 의 6d7d300 은 로컬 전용·미병합이다. 브랜치 지우면 영구 소실
   prune wt-anchor
   ⚠ 빈 폴더 껍데기 2개가 남았다(0MB · 셸이 cwd 로 잡고 있어 rmdir 만 실패 — 무해)

④ 마이그레이션        **실행 완료** (사장님 「아하 돌려」)
   e2f3a4b5c6d7 → c5a8e206d41f · 컬럼 4개(전부 nullable) · CHECK 확대 · 인덱스 1
   ⭐ 기존 데이터 온전: 219 / 1943 / 1292 행 → 변경 전과 동일 · 새 컬럼 전부 NULL
   ⭐ 실행 중 알게 된 것: DATABASE_URL_DIRECT 가 없어 풀러로 폴백되는데 그 풀러가
      **포트 5432 = 세션 모드**라 직결과 같게 동작했다 ⇒ DDL 안전.
      ⚠ CLAUDE.md 의 «DATABASE_URL_POOL(6543 pgbouncer)» 기술은 **낡았다**
```

## 4-C. ⛔ 실통화를 막는 것 — **서버 배포 하나뿐이다**

```
app-api  리비전 00137-47h · 이미지 태그 backend-split
origin/dev · origin/main  protocol.py → call_type: Literal["normal","level_test"]
origin/feat/expression-course = 2ed0ddf (docs 1건) ← 코드 8커밋은 **로컬에만**
⇒ 어느 경로로 붙어도 call_type:"expression" 이 pydantic 에서 튕긴다
```

### ⭐ 검증 경로 — 에뮬레이터·마이크가 병목이 아니다
| 방법 | 마이크 | 비용 | 쓸 자리 |
|---|---|---|---|
| ⭐ **웹 데모** `/__enginedemo` | 노트북 마이크 | **한 줄** (start 프레임에 call_type + 드롭다운) | 지금 바로. 대화 흐름 확인 |
| **오디오 파일 주입** | ⭐ 불필요 | 스크립트 1개 | 반복·자동 (2.5 vs 3.1 A/B 를 이 방식으로 했다) |
| 에뮬레이터/실기기 | 호스트/실제 | APK + merge/app-4way→dev | 앱 UI · 오디오 품질(끊김·에코·재생속도) |

⭐ `scripts/engine_demo.html` 이 이미 `/calls/stream` 에 붙어 실제 통화를 한다 —
  로그인·캐릭터·locale·시간 선택 + 통화/끊기/**이어하기(5분 구간)** + 원시 WS 로그.
  라우트 주석이 그 목적을 적어 뒀다: *"사장님이 플러터 대신 웹으로 테스트하기로 해서."*
⚠ 에뮬레이터 마이크는 되지만(AVD 호스트 오디오 입력) 오디오 경로가 실기기와 달라
  **끊김·에코·재생속도는 못 본다.** 그건 실기기 몫이고, «로직이 맞나» 는 웹이 낫다.

---

## 4-D. ⭐ 첫 실통화(call 1397) 감사 → 판정 프롬프트 재설계 (사장님 + 나, 2026-09-11)

### 통화 1397 사실 (사장님 직접 · member 20 · Leda · 316초 · 3인 QA + 나)
```
✅ 압축 4회 · 되감기 0 · 순서 1→7 · 건너뜀 0 · 끼어들기 0
✅ 드릴 검출 7/7 정확 (DB 드릴 7 = 전사 7)
✅ 3개마다 퀴즈 2회 · DB 구조·저장 순서·승급 stay 정상
⛔ 판정 — 4겹으로 틀림
   ① 앵무새를 통과로 셈 (지시문 :1135 무시)
   ② 드릴 산출을 퀴즈 통과로 셈 — 항목1 이 첫 퀴즈(34:05) 전인 32:53 에 통과 (codex)
   ③ failed 가 구조적으로 안 찍힘 — :1138 「마지막 결과 하나만」→ 마지막은 항상 복창 (fable)
   ④ 마지막 판정 1201ms > 1200ms → 마지막 145초(항목 4~7) 미판정
   ⇒ 엄격히 진짜 퀴즈 통과는 「이게 뭐예요?」 1개. DB 는 {얼마예요·잘 부탁드립니다·저는◯◯사람이에요}
⛔ 모국어 선질문 규칙 위반 — 항목3(33:25) 균열 → 4~7 「In Korean, that's '…'. Repeat」로 굳음
   첫 압축(36:04) 전이라 압축 탓 아님. 내 「I don't know 2회 적응」 가설은 codex 가 시간순으로 깸(위반 전 1회뿐)
⛔ [Country] 를 제어태그 누출로 오인 (33:37) — 대괄호 필터가 정상 문장을 자르고 복구 시드 주입 → t10 빈 턴
⚠ 첫 arm 18초 조기 발동 → 마지막 2분 쪽지 0 · 종료 시드가 「네」 답 전 주입(자가 회복) · 5분에 7개 처리
```
⚠ 내 실수: 전사 합본에서 t26(35:22)을 빠뜨렸다. fable·codex 는 원본 로그로 봤다.

### ⭐ 사장님 재정의 — 「체크할 건 두 가지: 배웠는가 / 맞췄는가」
```
드릴 단계   몰라서 비버가 알려준 걸 따라 말함  = 배웠다 OK (drilled)
퀴즈 단계   못 맞혀서 비버가 알려준 걸 따라 말함 = 틀렸다 (failed)
⇒ 앵무새 문제와 퀴즈 정답 문제는 **하나**다 — 판정기가 «지금이 드릴이냐 퀴즈냐»를 가르면 된다
```

### 결정 5 (사장님, 2026-09-11)
```
1. 퀴즈 앵커 강제     비버는 퀴즈 시작 시 {모국어}로 「퀴즈」를 알린다. 판정기는 그 말로 구간을 가른다
2. 힌트 뒤 정답       퀴즈에서 힌트만 받고 맞히면 passed (정답 전체 공개 전 = passed)
3. 정답 허용 폭       격식은 맞아야(반말 ✗) · 조사·어미 하나 누락은 허용.
                     판정은 **비버가 맞았다고 반응했는가**를 따른다 — 학습자가 들은 말과 DB 가 일치해야 한다.
                     대신 비버 대본에 격식 규칙을 박는다 (「얼마야?」를 «There you go!» 한 것이 사고)
4. 드릴 직후 퀴즈     퀴즈 구간이고 정답 공개 전이면 passed
5. 사장님 계정 초기화  ✅ 실행 완료 — member 20 의 drilled_call_id=1397 7행 → drilled_at·drilled_call_id·quiz_passed_at NULL
                     (call 1397 행 · expression_result 는 통화의 사실 기록이라 그대로)
```

### 프롬프트 초안 (QA 검수 → 빌더)
**A. 판정기** — «두 구간을 먼저 가른다 → drilled 는 구간 무관 → passed·failed 는 퀴즈 구간에서만 →
  정답 공개 전 스스로 냈으면 passed, 공개 뒤 복창은 failed 그대로 → 재출제는 나중 결과로 덮음»
**B. 비버 대본** — ① 퀴즈 앵커 새 줄 ② :121 「따라 말하기로라도」→ «먼저 시도한 뒤에도 못 하면 그때» + «직전 항목에서
  몰랐더라도 다음 항목은 다시 먼저 물어라» ③ 쪽지 착지문 :393 에 «먼저 {모국어}로 묻고 기다려라. 답을 먼저 말하지 마라»
  ④ 퀴즈에서 힌트는 되지만 정답 전체는 말하지 마라 ⑤ 격식 규칙(반말은 맞힌 게 아니다)

### 같이 고칠 것 (결정 불필요)
```
· [Country] 대괄호 필터 예외 — 커리큘럼 ◯◯ 자리표시가 영어로 [Country]/[Name] 으로 나온다
· 마지막 판정 입력을 «마지막 arm 이후 구간»으로 축소 (상한 유지)
· 첫 arm 조기 발동(18초) — floor/peak 산식이 지시문 크기에 기우는지 확인
```

---

## 4-E. 프롬프트 검수(codex·fable) → 결정 6 (사장님 「응 이대로」, 2026-09-11)

두 검수가 초안을 통화 1397 전사에 손으로 돌렸다 — 결과 일치: passed={이게 뭐예요?} · failed={얼마예요?·잘 부탁드립니다·저는◯◯사람이에요·도와주세요·처음 뵙겠습니다} · 「네」는 drilled 만.
초안의 구멍 3개(fable) + 3개(codex). 둘이 반대로 간 곳 없음.

```
1. 격식 판정 주체    ⭐ 판정기 독립. 결정 3 의 «비버 반응을 따른다» 를 «비버 반응은 보조 증거» 로 바꾼다.
                    근거: 1397 에서 비버가 같은 반말을 「얼마야?」는 칭찬·「도와줘」는 교정 — 일관성 없음.
                    캐릭터 톤이 강할수록 못 읽는다. quiz_passed_at 은 되돌릴 수 없어 거짓 양성이 비싸다.
                    대신 비버 대본에 격식 규칙을 박아 비버도 같은 기준으로 반응하게 한다 (그게 진짜 «일치»)
2. 힌트/공개 경계    목표 표현 전체 또는 어절 통째 · 완전 동의 표현 = 공개. 첫 음절·상황·뜻·«어미 하나 빠졌다» = 힌트
                    (「얼마예요?」 어절 = 공개 · 「이에요」 지적 = 힌트)
3. 앵커 폴백         명시 앵커 없어도 «이미 드릴한 여러 항목을 묶음 뒤 다시 묻는 구조가 명백» 하면 퀴즈.
                    모호하면 **그 항목만** 보류 (통화 전체 비우기 아님). 같은 항목 안의 «다시 해봐» 는 드릴 재시도
4. 어미 누락 허용    조사·어미 하나 누락 허용하되 **격식 표지는 제외** — 저→나 · -요/-습니다/-세요 제거는 불허
5. 통과 단조성       failed→passed 만. passed→failed 없음. 초안의 «나중 결과로 덮어라» 는 강등 위험이라 폐기
                    (공개 직후 «다시 말해 봐» 는 같은 회차 — 재출제 아님. 다른 문항 지난 뒤 다시 묻기만 재출제)
6. 무음 정책         침묵은 오답이 아니다. 첫 무음 = 답 공개 없이 모국어 재질문 1회 · 둘째 연속 무음 = 공개+복창 · 계속 = 이동
                    (1397 t10 에서 비버가 실제로 그렇게 했다). 넛지 1단은 퀴즈 중이면 힌트만
```

### 결정 없이 채택 (둘 다 타당)
```
프롬프트
· «정답 공개 전» 의 범위 = 그 문항 회차 안. 드릴 때 들려준 건 공개 아님  ← fable 이 찾은 제일 큰 구멍
  (통화 전체로 읽으면 드릴에서 들려준 항목은 영원히 passed 불가 — 1397 의 7개 중 6개)
· drilled 는 목표 표현이 비버나 학습자 발화에 실제 한 번 나와야 한다 (모국어 설명만으론 안 됨)
· 퀴즈 시작 전에 drilled 였던 항목만 그 회차에서 pass/fail 가능
· 퀴즈 종료·새 드릴 복귀도 모국어로 선언 · 오답 재출제 블록도 앵커
· 쪽지 착지문 «먼저 묻고 기다려라» 는 새 항목 시작에만. 방금 답이 있으면 그 답에 먼저 반응
· 판정기에 학습자 모국어 인자 + 영어 예시(quiz time / let's review / let's see if you remember). «앵커는 단어가 아니라 뜻»
  (판정기는 번호만 출력하니 리터럴 누출 무관 — README 원칙 4 예외)
· «Good, but / Almost / Just add» 는 정답 반응이 아니다
코드
· 마지막 판정 입력을 «마지막 arm 이후 구간» 으로 축소 (상한 1.2초 유지) — 새 프롬프트가 2배 길어져 필수
· tail 절단(12k)에서 최근 앵커/현재 phase 를 판정 입력에 별도 보존
· 앵커 누락 계측 로그
· [Country] 대괄호 필터 예외 (◯◯ 자리표시가 영어로 [Country]/[Name])
```
⚠ 제품 결과: 새 기준으로 1397 은 통과 1 · 오답 5. 「이거 얼마예요?」 는 드릴 첫 시도 완벽 → 퀴즈 오답 → failed.
  결과 화면 문구가 «퀴즈에서 못 맞힘» 임을 보여야 한다 (큐).

---

## 4-F. T14 3인 QA — 어떻게 결론 났나 (2026-09-11)

대상: 판정기 지시문 A · 비버 대본 7줄 B · 코드 4건 C. 검수자 나·codex·fable. **반려 3회, 커밋 5개, 회귀 1176→1209.**

```
차수  커밋      누가 잡았나   무엇                                                        결과
1차   daec11a   fable         P1 C1 입력 축소가 판정기 규칙과 충돌(잘린 전사→«처음 나온 항목=드릴»→통과 소실)   반려
                              P2 phase 빈값 잔존 · C4 «대문자 한 단어» 가 발명 태그와 겹침 · 픽스처 t26 결손 · 시험 3곳
                codex         P1 같은 뿌리 + P2-A 커서=len(segments)라 12k 절단 시 잘린 머리 영구 미판정
                              P2-B phase 가 커서 세대와 안 묶여 늦은 판정이 역전
      → 5522e0d 항목 목록에 서버 사실(이미 드릴함·통과·오답) · phase 세대 · 커서 «안 본 머리» · C4 위치 규칙(한글은 위치 무관)
결정① 4e1e960   나            통화중 판정도 커서+겹침 4 (두 경로 같은 창)
2차   b23270b   fable         P1 «축소하면 1.2초 안» 이 수치로 안 섬 — 새 지시문 +2.1k 가 전사 축소 −1.9k 를 도로 먹음
                              (옛 6.2k=1,201ms 초과 ↔ 새 6.4k)      → 규칙 앞·목록 뒤 + 상한 2.0초 + 실통화 계측
                              P2 겹침 경계 표시 없음 → 앵무새 복창이 경계 덕에 passed 승격  → 경계선 + «문맥 구간 제외»
                              P2 커서0 절단 시 영원히 0 → 커서를 «본 끝» 까지(손실 1회 한정)  ⚠ 빌더가 내 first_seen 지시를 고침 — 맞음
3차   a9bc102   codex         P1 경계 규칙 «시작된 회차 제외» 가 겹침의 목적(질문 위·답 아래)을 스스로 부정 → «결정적 순간의 위치» 로
                              P1 커리큘럼 표기 대괄호(grammar 459 중 21: N이/가 있어요[없어요]) 를 제어태그로 오인 → expr_items 허용 목록
4차   (좁게)    fable  통과 · 문구 P2: «질문**만** 위» 가 힌트 경로를 죽임 → «공개도 산출도 없으면» 한 줄
                codex  통과 · 정책 결정: 허용 조각 맨 앞도 통과 — **유지**(커리큘럼 정확일치 = 발명 태그 아님)
```

### 패턴 — 이번 QA 의 값어치
```
· 매 수정이 새 구멍을 열었고, **다른** 검수자가 잡았다
    경계선(fable 처방) → 정상 답 누락(codex 발견) · 입력 축소(내 지시) → 크기 원복(fable 발견)
· 빌더가 내 지시를 근거 들어 고친 것 2번, 둘 다 맞았다
    C4 한글 태그 위치 무관(call 706 회귀 유지) · 절단 시 커서 «본 끝»(first_seen 이면 12k 재독)
· 내 지시 오류 3건: C1 축소 전제(크기) · «맨 앞만 누출»(call 706) · 커서 first_seen
· 회귀 34초가 아니었으면 3차까지 못 갔다 (5회 × 10분 = 50분 vs 3분)
```

### 결론 = «코드가 설계대로» 까지. «실통화에서 판정이 맞나» 는 0건
```
✅ 판정기 지시문 확정 문구 20/20 · 대본 7줄 · 코드 4건 + 반려 반영 · 회귀 1209 · 바이트 동일 94 · R4 diff 0
⏳ 실통화 재측정 — 볼 것 5: 마지막 판정 ms(2.0초 안?) · 앵커(「퀴즈」 말하나) · 판정 정확도(전사↔DB) · [Country] · 모국어 선질문
⚠ 알고 가는 것: (통과) 3겹 불가역(큐 Q25) · 12k 초과 시 1회 유실(5분 소켓 도달 불가) · 허용 목록 정확일치(공백 변형은 누출)
```

---

## 4-G. 2번째 실통화(1398) 감사 → T15 (2026-09-11)

사장님 감상: 「개판인듯」. 3인 QA 일치: **«계기판은 좋아지고 수업은 나빠졌다»**. 삐삐 소리는 사장님 컴퓨터 쪽(서버 로그 근거 0) — 닫음.
```
              1397                       1398
모국어 선질문   3번부터 무너짐               6/6 ✅
퀴즈 주기      3개마다 ✅                  매 항목 직후 ⛔⛔  ← «개판»의 자리
격식          반말 승인                    t11 교정 / t9 오답 극찬(비비 캐릭터)
꼬리 판정      1201ms 초과                 1626ms < 2000 ✅
판정 정확도    3 중 진짜 1                  2 중 진짜 1 (안녕하세요 거짓양성)
자발 산출      1                          0
첫 arm        18초                        40초
```
원인 (fable·codex 일치)
· 퀴즈 1개마다 — T14 가 퀴즈 불릿을 1→5개로 늘려 «3개마다» 한 줄이 묻힘. 금지 규칙 부재
· 오답 극찬 — «반응만 하고 넘어가라» + 비비(욕하며 극찬) — «맞았다고 하지 마라» 부재. 1397 은 포기 상한에 안 닿아 못 봄
· 안녕하세요 거짓양성 — t17 «공개 + 질문형 되묻기»를 판정기가 새 문항으로 읽음. 창·표시·경계선은 원인 아님
· 드릴이 오답으로 — «(이미 드릴함)» 이 «지금까지 검출»이라 창 안 첫 드릴이 퀴즈로 읽힘 (fable 자기 권고 결함)
· 첫 arm 40초 — 임박 산식이 T14 긴 지시문에 바닥 커져 조기 발동. 라벨 «compress» 도 오해

우리 잘못
· 문장 존재만 검수, **비중**을 안 봄 · 포기 경로 아무도 안 봄 · 1397 한 건이 우연히 맞은 걸 계약으로 착각
· ⭐ 평가 축이 «판정기»였지 «수업»이 아니었다 — 무너진 건 전부 로그가 안 재던 축
· 통화 0건으로 대본 7줄 + 판정기 2배 한꺼번에 배포

T15 (커밋 41074e9 · 회귀 1219 · demo-api 00286-nnk)
```
1) 퀴즈 주기를 번호에 (3·6·9…) + «묶음 안 찼으면 절대 시작 금지» + «되묻기는 드릴 재시도»
2) 퀴즈 불릿 5개 → [퀴즈] 소제목 (언제 1줄 / 어떻게)
3) 포기 경로 «맞았다고 하지는 마라»
4) «(이미 드릴함)» = 직전 판정 시점 스냅샷
5) 판정기 «공개 뒤 질문형 되묻기도 같은 회차» + 비버 «공개 뒤 되묻지 마라»
6) arm 라벨 compress→compress_imminent · 표현학습 바닥 보정 EXPR_REGROUND_MIN_ROOM_TOKENS=3000 (1건 역산 · 로그에 peak·바닥 추가해 재결정)
```
새 규율: **통화 1회당 변경 한 묶음** · 평가 축에 «퀴즈 주기 · 자발 산출 · 거짓 칭찬» 추가
⭐ 사장님 제안: 학습자를 대본으로 대신하는 **E2E 하네스** — 정답을 우리가 아니까 ground truth 로 자동 채점. 승인 대기.

---

## 5. 진행 상태 (2026-09-10 기준)

```
묶음 A  T1·T2·T3    프롬프트 골격        커밋 05acffc   ✅ 바이트 동일 5/5 · 회귀 986
묶음 B  T4~T7       데이터·선별·라우팅    커밋 (진행)    ✅ 회귀 1021
묶음 C  T8~T12      런타임               커밋 대기      ✅ 회귀 1086 (QA 반려 7건 처리 후)
```

### QA 3인이 잡은 것 (혼자서는 못 잡았을 것)
```
빌더    [:10] 슬라이스가 18개를 10개로 자른다 (조용히 틀릴 자리)
        Alembic id 충돌 (a1b2c3d4e5f6 ↔ subscription_three_tier)
        teaching_plan 에 이번 통화에서 안 다룰 카드가 뜰 뻔
        candidates=None 이 «안 준다» 가 아니라 «기본 후보를 뽑아라» 였다  ← 정확히 반대로 돌 자리
        «산출 1회 = 끝 신호» · «정리» 는 종료로 미끄러지는 어휘
codex   ⛔ 캐릭터 rules 컬럼이 없다 (내 명세 오류)
        ⛔ 조각 승계 그림이 거짓 (내 명세 오류)
        ⛔ 앵무새 2턴이 정상 퀴즈를 막는다 (내 지시 오류)
        quiz_judge import 가 순수하지 않다 (시험이 소스 문자열만 grep)
        핫패스에서 18개 순회가 동기로 3.2초 (1M자 답)
fable   딴소리 처리 누락 (사장님이 명시한 것)
        페르소나 틀 3벌 복사 · freetalk 리터럴 한국어 + 그걸 안 보는 시험
        evaluate_level_up 이 두 코스에서도 무조건 돈다
        프리토킹이 normal 항목 기계를 물려받는다 (+ 표현학습 풀 소진 시에도)
        drilled_call_id 덮어쓰기로 지난 결과 화면이 비워진다
        «(통과)» 표식이 죽은 코드 + 독스트링 3곳이 거짓
```

### 남은 일
```
▶ T8 재설계 지시 완료 (2026-09-10) — expr-build 작업 중
    ①문자열 통과권 제거 ②사이드카 3목록 ③조각끝 LLM→DB 순서 ④P0-3 프롬프트 ⑤레벨업
⏸ 묶음 C 커밋은 이 재설계까지 포함해 한 번에 — 회귀 + 바이트 동일 5/5 재검증 후
⛔ 실통화 검증 0건 — 효과 미확정
⛔ 클라(버튼 2개 · WS course 파라미터) 미착수
```
