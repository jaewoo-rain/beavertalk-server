# 프롬프트 작업 기준 (BeaverTalk 통화)

> **이 폴더는 무엇인가**: 통화 프롬프트를 만질 때 매번 여는 문서.
> 정리 시점 **2026-08-16** · 정리 근거 = 사장님이 주신 노션 5개 페이지 + 그날 코드 실검증.
> 앞으로 프롬프트를 고칠 때마다 **§8 결정 로그에 이어 적는다.**

## 파일

| 파일 | 내용 |
|---|---|
| `README.md` (이 파일) | 카탈로그 · 엔진 차이 · 튜닝 원칙 · **지뢰밭** · 드리프트 · 작업 절차 · 결정 로그 |
| `notion-01-일반통화-프롬프트-6종.md` | 노션 원문 보존 — 사이클별 프롬프트 카탈로그 |
| `notion-02-일반통화-프롬프트-튜닝.md` | 노션 원문 보존 — **결정 로그(07-24~08-01)**. 이 폴더에서 제일 중요한 원본 |
| `notion-03-레벨테스트-인덱스.md` | 노션 정리본 — 현재 상태·주의사항 |
| `notion-04-레벨테스트.md` | 노션 정리본 — 레벨테스트 구조·프롬프트 4종·변경 이력 |
| `notion-05-인프라-cloudrun.md` | 노션 정리본 — Cloud Run CPU·통화후 파이프라인·비용 |

⚠ **노션 원본은 로그인이 필요해 도구로 못 읽는다.** 여기 보존본이 유일한 사본이다.
노션에서 프롬프트를 또 고치시면 이 폴더에도 반영해 주셔야 다음 세션이 안다.

---

## §0. ⛔ 제일 먼저 — 이 폴더의 문구를 코드에 복사하지 마라

오늘(2026-08-16) 확인한 사실: **노션 문구가 코드보다 한 세대 이상 뒤처져 있었다.**
그대로 적용하면 이미 고친 사고가 재발한다(§5 드리프트 표).

그래서 이 문서는 **프롬프트 전문을 싣지 않고, 줄번호도 적지 않는다.**
가리킬 때는 **파일 + 심볼 이름**으로만 가리킨다. 전문이 필요하면 §7 부록으로 **그때 뽑는다.**
> 사본은 반드시 낡는다. 오늘 배운 게 그것이다.

---

## §1. 프롬프트 카탈로그 (2026-08-16 코드 실검증)

모두 **순수 문자열 조립 — LLM 생성 0**. 어떤 조각도 AI 가 만들지 않는다.

### 통화 본체에 들어가는 것 (Gemini 에 직접)

| # | 심볼 | 파일 | 언제 | Live | 캐스케이드 |
|---|---|---|---|---|---|
| 1 | `build_system_instruction` | `core/persona_prompt.py` | 통화 시작 1회 | ✅ | ✅ (옵트인 2블록 추가) |
| 2 | `seed_opening` | `core/persona_prompt.py` | 시작 직후 1회(선톡) | ✅ | ✅ |
| 3 | `build_reground_reminder` | `core/persona_prompt.py` | 중반 재접지 | ✅ | ❌ **없음** |
| 3b | `build_continue_reminder` | `core/persona_prompt.py` | 후반 재접지(08-01 신설) | ✅ | ❌ **없음** |
| 3c | `build_reground_brief` | `core/persona_prompt.py` | 압축 트리거 통합 재접지 | ✅ | ❌ |
| 4 | `_close_seed` | `domains/learning/realtime/call_session.py` | 서버 시계 판단 시 | ✅ | ✅ (같은 함수 재사용) |
| 5 | `_NUDGE_SEED_1` / `_2` | `call_session.py` | 무음 3단 넛지 | ✅ | (캐스케이드는 자체 처리) |
| 6 | `build_leveltest_instruction` + `seed_leveltest_opening` + `close_seed_leveltest` | `core/persona_prompt.py` | 레벨테스트 통화 | ✅ | ❌ **레벨테스트 미지원** |

### 사이드카 (JUDGE_MODEL — 별도 LLM)

| # | 심볼 | 파일 | 언제 |
|---|---|---|---|
| 7 | `_hint_instruction` | `call_session.py` | 통화중, 비버 턴 끝난 뒤 백그라운드(커리큘럼 언어만). **캐스케이드도 재사용** |
| 8 | `_analysis_instruction` + `_DETECTION_INSTRUCTION` | `domains/learning/service/normalcall_service.py` | 통화후 1콜. **캐스케이드도 재사용**(`live._trigger_analysis`) |
| 9 | `_leveltest_turn_instruction` | `normalcall_service.py` | 레벨테스트 매 턴(종료 판정 전용) |
| 10 | `_leveltest_instruction` | `normalcall_service.py` | 레벨테스트 통화후(최종 레벨) |

### `build_system_instruction` 의 조립 순서

```
불변식 템플릿(규칙 1~5)
  └ 규칙1 모드 불릿 : 기본판 / 체크판 교체판(study_items·known_items 중 하나라도 있으면)
  └ 규칙3 밴드정책  : _LANG_POLICY[lang_band] 1줄 주입
[학습자 수준]      level_profile
[학습자 흥미·소재]  interests
[오늘의 공부 항목]  study_items      ← 조건부
[대화 모드 가이드]  known_items      ← 조건부
[최근 통화 소재]    recent_topics    ← 조건부
[최근 학습 이력]    history          ← 조건부
[승급 알림]         promotion_notice ← 조건부
[표기 규칙 — 감정 태그]  emotion_tags    ← ⭐ 캐스케이드 전용 옵트인
[표기 규칙 — 언어 마커]  language_marker ← ⭐ 캐스케이드 전용 옵트인
```

**⛔ 하위호환 계약**: 조건부·옵트인이 전부 꺼지면 출력은 **바이트 동일**하다.
`tests/test_persona_prompt.py` 의 동결 스냅샷이 그걸 지킨다.

---

## §2. 두 엔진의 차이 (프롬프트 관점)

|  | Live (`call_session`) | 캐스케이드 (`cascade_session`) |
|---|---|---|
| 엔진 | Gemini Live 네이티브 오디오 | STT → Gemini 2.5 Flash(텍스트) → TTS |
| system_instruction | 세션 시작 시 **1회** 주입, 컨텍스트에 상주 | ⭐ **매 턴 재전송**(`generate_content_stream` 을 턴마다 새로 연다) |
| ⇒ 프롬프트 길이의 의미 | 드리프트 요인(비용은 간접) | **턴당 입력 토큰 = 직접 원가** |
| 감정 태그 | ⛔ **절대 켜지 마라** — 모델이 태그를 소리로 읽는다(걷어낼 자리가 없다) | ✅ 서버가 걷어내고 TTS 스타일로 변환 |
| 언어 마커 | ❌ 불필요(모델이 직접 발음) | ✅ **필수** — 구간별로 그 언어 음성을 고른다 |
| 체크판·이력 재료 | ✅ 전부 주입 | ❌ **하나도 안 넘긴다**(§6 미해결) |
| 재접지 | ✅ 중반·후반·압축트리거 | ❌ 없음 |
| 레벨테스트 | ✅ 자동 라우팅 | ❌ 레벨 미확정도 `call_type=normal` 강행 |
| 응답 길이 | 상한 없음(문구는 기본 4문장) | `CASCADE_LLM_MAX_SENTENCES` 가 **문구와 강제를 같이** 만든다 |

**⭐ 캐스케이드만 고치는 두 가지 안전한 통로** (Live 를 안 건드린다):
1. **호출부 인자** — `cascade_session._system_instruction()` 에 넘기는 값
2. **옵트인 블록** — `_EMOTION_TAG_RULE` · `_LANGUAGE_MARKER_RULE`

⛔ 그 밖(규칙 1~5 본문·페르소나·선톡·종료 시드)은 **Live 와 공유**다. 건드리면 Live 통화가 같이 바뀌고
동결 스냅샷이 깨진다. 스냅샷 통과 = **Live 무영향의 자동 증명**이니, 깨졌다면 공용 자산을 건드린 것이다.

---

## §3. 튜닝 원칙 (노션 결정 로그에서 추출)

### 원칙 1 — 톤은 캐릭터에 반납한다
프롬프트에 **부사·예시 대사를 쓰지 않는다**("따뜻하게"·"부드럽게"·"친절히"·"이렇게 말해요").
캐릭터는 DB(`role`/`personality`)에 있고 제각각이다 — 톤을 처방하면 **츤데레·독설 캐릭터가 다정한 선생으로
뭉개진다.** 대신 `반응(칭찬·핀잔 등)은 네 캐릭터대로` 처럼 **캐릭터에 위임**한다.
> 2026-07-24 에 이 한 가지로 페르소나·규칙1·규칙4·체크판 블록 4곳을 고쳤다.

### 원칙 2 — 금지 예시는 씨앗이 된다. 전진 지시로 쓴다
`"슬슬 끊자" 같은 말 금지` 라고 적었더니 비버가 **그 예시를 그대로 뱉었다**(call 782).
"하지 마라" 대신 **"대신 이걸 해라"**로 쓴다.
⚠ 단 **일반화하지 마라** — 금지 예시가 없고 "안 하기"가 곧 올바른 행동인 부정 지시
(메타 발언 금지·자문자답 금지·과교정 금지)는 그대로 두는 게 맞다.

### 원칙 3 — 중복은 한 규칙에 위임한다
같은 말이 두 곳에 있으면 **어느 게 진짜인지 아무도 모른다.** 설명 언어는 규칙3, 교정 톤은 규칙4가 소유.
숫자도 마찬가지 — 손으로 쓴 값이 두 곳에 있으면 안 된다(응답 길이는 `_max_sentences()` 하나에서 나온다).

### 원칙 4 — ⭐ 예시가 곧 규칙이다
모델은 규칙과 예시가 다르면 **예시를 따른다.** 프롬프트를 고칠 때 **규칙만 고치고 예시를 안 고치면
아무것도 안 고친 것**이다. 2026-08-16 에 이 계열로 2건을 찾았다(§8).
⚠ 리터럴 예시는 **특정 언어 짝에서만 맞다.** 멀티랭귀지에서는 `{target}`/`{locale_label}` 로 쓴다.

### 원칙 5 — 개념을 지워야 수단이 사라진다
종료 메커니즘을 지시문에서 가르치면 모델이 그걸 **실행 수단으로 쓴다.** 세 번 당했다:
리터럴 노출 → 낭독 종료(706) / 난수 접미 → 난수까지 복사(852) / 성질만 서술 → **없는 태그 발명**(870).
흉내낼 원본을 지우는 걸로는 부족하다. **모델이 수단을 모르면 그 수단을 못 쓴다.**

### 원칙 6 — 프롬프트로 부탁하지 말고 파라미터로 시켜라
"또박또박"이 한국어 발화 속도의 절반을 먹고 있었다. **속도는 `speaking_rate` 가 맡는다** —
문구와 파라미터가 싸우면 어느 게 진짜인지 못 가린다.

---

## §4. ⛔⛔ 지뢰밭 — 되돌리면 사고가 재발한다

| 건드리면 안 되는 것 | 왜 | 증거 |
|---|---|---|
| **종료 규약(`_RULE_CLOSE_PROTOCOL`)에 종료를 다시 설명하지 마라** | 지금 이 문단은 "대화를 이어가라"와 "낭독 금지"만 말한다. 종료 신호·태그·마무리 절차가 **한 글자도 없다** | call 706·852·870 (원칙 5) |
| **제어 태그 2종을 합치지 마라** — `CONTROL_TAG`(안내) vs 종료 태그 | 둘 다 `[시스템]` 이던 시절 재접지·넛지가 **종료 신호로 오독**됐다 | call 683 (재접지 30초 뒤 작별) |
| **공부 블록 예비 슬롯에 조건절을 붙이지 마라** | "본편 다 끝났는데 종료 신호가 없으면"이라고 쓰면 비버가 **"오늘 할 건 끝났다"로 읽고 통화를 접는다** | call 870 (4분 24초 자체 종료) |
| **감정 태그를 Live 에 켜지 마라** | Live 는 모델이 직접 소리를 내므로 **태그를 그대로 읽는다**(서버가 걷어낼 자리가 없다) | 설계 제약 |
| **감정 태그를 대괄호로 가르치지 마라** | 프롬프트가 `[공부 모드]` 처럼 대괄호를 구획 라벨로 쓴다 → 모델이 태그 자리에 **모드 이름**을 넣는다 → 그 글자가 소리로 나가고 감정도 죽는다 | 실통화 00147/00148 |
| **감정 종류를 늘리지 마라** | 많을수록 표현이 흔들린다. 5종은 **클라 아바타 표정과 같은 축**이라 매핑 계층이 없다 | 사장님 결정 2026-08-12 |
| **감정 문구에 속도 어휘를 넣지 마라** | 원칙 6 | ko 1.3 → 6.2~7.4자/초 |
| **캐릭터 `rules` 를 부활시키지 마라** | 캐릭터별 '한국어 10%'가 전역 밴드 정책과 **정면충돌**했다 | 커밋 `7476bba`(컬럼 드롭) |
| **안전바닥(인신공격 금지)** | 2회 보류됨 — 욕설·독설 캐릭터가 흐려질 우려. **사장님 판단 사항** | 2026-07-24 · 07-24(사이드카) |
| **레벨테스트 종료 시드의 `곳 딱 맞는`** | `곧` 의 오타로 보이나 **실제 배포 문자열일 수 있다.** 임의 수정 금지 | 노션 03 §주의사항 |

---

## §5. 노션 ↔ 코드 드리프트 (2026-08-16 대조)

노션 문구를 그대로 적용하면 **되돌리기**가 되는 자리다.

| 블록 | 노션 | 실제 코드 |
|---|---|---|
| 규칙2 | "종료 신호는 정확히 `[통화종료]`…" + 마무리 절차 | 제목이 **"대화 지속(매우 중요)"**, 종료·태그·절차가 **한 글자도 없다** |
| 공부 블록 예비 | "종료 신호가 오지 않았을 때만 이어서" | `"이어서(본편을 끝내면 여기서 계속):"` — 조건절 삭제 |
| 진행 규칙 끝줄 | "종료 신호가 오면 즉시 규약을 따르라" | 삭제 → "재료를 다 쓴 뒤에도 대화는 그대로 이어진다" |
| 유형별 절차 | 문법 ①예문부터 / 단어 ①따라말하기부터 | ①에 **설명이 다시 들어옴** + **L1 왕초보 분기 신설**(문법 용어 금지) |
| `_CLOSE_SEED` | "따뜻하게"·"친구끼리 헤어지듯"·예시 대사 | 캐릭터 반납판(`f8e0ebb`) |
| 규칙3 | ★[학습자 언어에 끌려가지 마라] 없음 | 실재(2026-07-29) |
| 재접지 | 1종 | **2종**(중반 + 후반 `build_continue_reminder`) + 압축트리거 브리프 |
| 6종 카탈로그 | 캐스케이드 언급 0 | 캐스케이드 전용 옵트인 2블록 존재 |

---

## §6. 미해결 (프롬프트 품질 관련)

| 우선 | 항목 | 메모 |
|---|---|---|
| 🔴 | **캐스케이드에 체크판·이력 재료가 안 들어간다** | `load_call_setup` 이 `study_items`/`known_items`/`recent_topics`/`history`/`promotion_notice` 를 **이미 담아 주는데** 캐스케이드가 안 넘긴다. Live 는 넘긴다. ⇒ 캐스케이드 통화엔 커리큘럼이 없다(비버가 즉석 생성). **코드 작업이라 프롬프트 범위 밖으로 보류 중** |
| 🟠 | 감정 80% neutral 의 원인 미확정 | "모델이 하나만 내나" vs "우리가 뒤 태그를 버리나" — `read_emotion_tags` 계측이 답을 준다. **통화 1건이면 갈린다** |
| 🟠 | 대괄호 라벨 ↔ 태그 충돌의 뿌리 | 방어 2겹(꺾쇠 표기 + 서버 제거)만 있고 라벨 표기는 그대로. `버린태그:` 로 확정 |
| 🟡 | 응답 길이 문구 vs `max_output_tokens=200` | 잘리면 꼬리를 버린다. `truncated` 로 확정 |
| 🟡 | 규칙3 슬림 | 프롬프트의 절반. 캐스케이드에선 턴당 원가. **Live 공용이라 손대려면 결정 필요** |
| ⚪ | 캐스케이드 재접지 도입 여부 | 성질이 Live 와 달라 추측 금지 — 5분 통화 후반 톤을 듣고 판단 |

---

## §7. 작업 절차 (프롬프트 만질 때 매번)

1. **전문을 먼저 뽑아 눈으로 본다.** 소스만 읽으면 `.format` 뒤 실제 모습이 안 보인다 —
   오늘 찾은 결함 2건은 **조립된 전문을 보고서야** 드러났다(부록 스크립트).
2. **어느 상자인지 정한다** — 캐스케이드 전용(옵트인·호출부)인가, Live 공용인가.
   공용이면 **먼저 여쭙는다**(Live 통화가 같이 바뀐다).
3. **원칙 1~6 과 §4 지뢰밭을 대조한다.**
4. 고친다. **규칙을 고쳤으면 예시도 같이 고친다**(원칙 4).
5. **전문을 다시 뽑아 diff 한다.** 의도한 줄만 바뀌었는지 확인.
6. `pytest tests/ -q` — 특히 `test_persona_prompt.py`(Live 무영향) · `test_cascade_*.py`.
   `PYTHONIOENCODING=utf-8 conda run -n beavertalk-server python -m pytest tests/ -q`
7. **§8 결정 로그에 적는다** — 무엇을·왜·무엇을 안 했는지. 노션에도 반영.
8. 통화하고 **§9 로그를 본다.**

---

## §8. 결정 로그

> 노션 결정 로그(2026-07-24 ~ 08-01)는 `notion-02` §5 에 보존돼 있다. 여기부터는 그 뒤를 잇는다.

### 2026-08-20 — 공부 모드 절차: 한국어 리터럴 예시 제거

배경: 사장님 실통화(`call_id=1097`, locale=en, 5분)에서 **공부 모드인데 설명·지시가
한국어로 나갔다** — `오늘은 "피우다"라는 단어 배울게. "피우다"는 "to bloom" 이런 뜻이야.`
규칙 3 은 정반대를 지시한다("공부 모드의 설명·지시·리액션은 레벨과 무관하게 전부 모국어로").

⭐ **규칙은 멀쩡했고 예시가 어긋나 있었다** — 원칙 4 가 말한 바로 그 형태다.

| # | 결함 | 고침 |
|---|---|---|
| ① | `_study_procedure` 문법 ④ 의 `응용 질문(예: "너는 주말에 뭐 하고 싶어?")` — 조립된 지시문 4,542자를 통틀어 **리터럴 대사가 나오는 유일한 자리**였고 그게 한국어다. 모델은 규칙이 아니라 예시를 따른다 | 예시를 지우고 **질문이 갖춰야 할 성질**로 — "그 문법을 써야만 답할 수 있는 응용 질문" |

- **덤으로 멀티랭귀지 버그** — `{target}` 치환이 아니라 **박힌 한국어**였다. 프랑스어
  학습자에게도 "너는 주말에 뭐 하고 싶어?"가 그대로 갔다(원칙 4 의 ⚠ 그대로).
- **범위**: `_study_procedure` **한 줄**. ⛔ Live 본문(`_INVARIANTS_TEMPLATE`)은 안 건드렸다 —
  규칙 3 자체는 맞게 쓰여 있다. 스냅샷 동결본도 그대로다(재기준화 없음).
- **길이**: 4,542 → 4,541자(-1). 전문 diff 로 **의도한 1줄만** 바뀐 것을 확인했다.
- **안 한 것(B안 보류)**: 절차 블록에 "설명·지시는 모국어로"를 **다시 쓰지 않았다.**
  원칙 3(중복은 한 규칙에 위임)과 마찰한다 — 설명 언어는 규칙 3 이 소유한다.
  A안만으로 부족하면 통화를 듣고 그때 판단한다(사장님 승인 2026-08-20: "A안으로").
- **효과 미확정**: 통화로 확인하기 전엔 "고쳤으니 좋아졌다"고 말하면 안 된다.
- 같이 고친 것(프롬프트 아님): 입력 전사 언어 힌트 — 상세는
  `docs/20260820_1130_공부모드-언어와-전사언어-수정.md`

---

### 2026-08-19 — Live 본문: 끊긴 참조 2건

⛔⛔ **이번엔 Live 본문(`_INVARIANTS_TEMPLATE`)을 직접 바꿨다.** 지난번과 정반대다 —
그때는 캐스케이드 전용 옵트인이라 스냅샷 통과가 "Live 무영향 자동 증명"이었다.
**이제 그 그물은 안 친다** — 이 테스트를 "Live 무영향 증명"으로 인용하지 마라.

배경: 캐스케이드를 접고 Live 프롬프트로 전환(사장님 지시). `feat/live-face` 위에 작업.

| # | 결함 | 고침 |
|---|---|---|
| ① | 규칙 1 이 "**위** 선톡 질문에 대한 답을 듣고"인데 선톡 시드는 지시문에 **없다**(`call_session` 이 별도 턴으로 주입). "위"에 아무것도 없는 끊긴 화살표 | 가리키지 말고 **스스로 말하게** — "네가 통화 첫머리에 던진 모드 질문". ⚠ 질문 문구를 리터럴로 안 옮겼다(원칙 2) |
| ② | [대화 모드 — 착지] 불릿이 "발판의 정도는 **아래** 밴드 정책이 정한다"인데, `{lang_policy}` 는 바로 **위**에 꽂힐다. 모델이 못 찾으면 초급 발판을 자기 재량으로 정한다 | `아래` → `위` |

- **길이**: 5,180 → 5,191자(+11). Live 는 세션 1회 주입이라 턴당 원가가 아니다.
- **스냅샷 재기준화**: ⛔ 동결본을 **통째로 다시 뽑지 않았다.** 소스에 넣은 것과
  **똑같은 두 치환만** `_ORIG_INVARIANTS_TEMPLATE` 에 적용했다(각 문자열이 정확히 1회
  등장하는지 assert). 통째로 재생성하면 **실수로 바꾼 곳까지 같이 얼어붙는다.**
  선례를 따랐다(2026-07 한국어 위주 전환 때도 의도적 재기준화를 했다).
- **검증**: `test_persona_prompt.py` **68 passed**. 전체 회귀에서 실패한 3건은 `git stash` 로
  내 수정을 걷어내고 돌려 **받아온 시점에 이미 깨져 있던 것**임을 확인했다
  (`test_live_model_is_untouched` 등 — 이어하기 `cf5cdae` 가 `ServerCallStarted` 에 `call_id` 를
  붙였다). 나머지 1건은 **부하 flake**(단독 5/5 통과).
- **안 한 것(통화가 갈라야 한다)**: "또박또박" 7회 중복(⚠ 원칙 6 을 그대로 적용하지
  말 것 — Live 는 native audio 라 `speaking_rate` 가 없다) · 금지 예시 리터럴 3종 · 규칙 3 길이(28%).
- **효과 미확정**: 둘 다 논리적 결함이지 **증상으로 관측된 것이 아니다.** 통화 전까지
  "고쳬으니 좋아졌다"고 말하면 안 된다.
- 상세: `docs/20260819_1801_라이브-통화-프롬프트-끊긴-참조-2건.md`

---

### 2026-08-16 — 캐스케이드 전용 표기 규칙 2블록 문구 수정

범위: **프롬프트 문구만**(사장님 지시). 변경 파일 = `core/persona_prompt.py` 의
`_LANGUAGE_MARKER_RULE` · `_EMOTION_TAG_RULE` **두 상수뿐**. ⛔ Live 공용 자산은 안 건드렸다.

| # | 결함 | 고침 |
|---|---|---|
| ① | **예시가 규칙을 배신**(원칙 4) — "{target}를 감싸라"고 해 놓고 예시는 `__How are you?__`(영어를 감쌈). 마커 안쪽은 target 음성으로 읽히므로 그대로 따르면 **영어가 한국어 발음으로** 나간다 | 예시를 `{target}`/`{locale_label}` **자기라벨링**으로 — 어떤 언어 짝에서도 뒤집힐 수 없다. "거꾸로 감싸지 마라" 경고 1줄 추가 |
| ② | **neutral 기본값 유도** — "애매하면 `<neutral>` 을 써라". 가르치는 턴은 대부분 '애매'해 상시 발동(실측 68턴 중 55건=80%) | neutral 을 '모를 때 찍는 칸'이 아니라 '담담한 순간의 감정'으로 재정의 |
| ③ | **예시가 규칙 3 을 어김**(원칙 4) — `<happy> 우와, 정말 잘했어요!` 는 한국어 칭찬. 규칙3 ★[학습자 언어에 끌려가지 마라](07-29 call 784)가 막으려는 행동을 **바로 아래 예시가 시범 보였다.** 마커도 없었다 | 예시에서 대사를 빼고 **태그 위치만**. 언어가 섞인 예시는 언어 마커 블록이 소유 |
| ④ | `{target} 로` 군더더기 공백 | 붙여쓰기 |

- **길이**: 3,904자 → 4,157자(+253). 캐스케이드는 매 턴 재전송이라 턴당 원가다.
- **검증**: 테스트 고정 계약 32/32 통과(Live 출력에 두 블록 미포함 · append-only ·
  `<꺾쇠>` 5종/`[대괄호]` 금지 · "하나만"·"성격대로"·"어긋나면 안 된다" 존속 · 금지문구 3종 부재 ·
  멀티랭귀지 3쌍 ko-en/fr-ko/ja-vi 에서 예시 안 뒤집힘).
  ✅ **회귀 전체 통과**(2026-08-17, 1212/1212). 작성 시점엔 conda env 가 없어 "못 돌렸다"고 적혀 있었지만 그 뒤 env 를 만들어 돌렸다 — 닫힌 항목이다.
- **안 한 것**: 재료 주입(§6 🔴 — 코드 작업) · 규칙3 슬림(Live 공용) ·
  블록 위치 이동(**철회** — 지금 맨 끝이 사용자 턴에 가장 가깝고 테스트가 append-only 를 고정한다).
- **효과 미확정**: ②는 원인이 안 갈렸다(§6 🟠). **"고쳤으니 감정이 살아난다"고 말하면 안 된다.**
- 상세: `docs/20260816_2222_캐스케이드-통화-프롬프트-품질-개선-계획.md`

---

## §9. 통화 후 볼 로그 (계측은 이미 붙어 있다)

| 로그 | 무엇을 알려주나 |
|---|---|
| `마커 fixed=N` | 언어 마커가 몇 번 뒤집혔나. **0 이면** 애초에 안 뒤집히고 있었다는 뜻(가설 기각) |
| 감정 태그 개수(`read_emotion_tags`) | 모델이 **1개만** 내면 프롬프트 몫, **여러 개** 내면 우리가 뒤를 버리는 것 → 문장별 감정을 만들지 판단 |
| `버린태그:` / `라벨 낱말로 시작` | 대괄호 라벨 충돌이 아직 사는지 |
| `truncated` | 응답 길이 문구와 토큰 상한이 부딪히는지 |
| `tts_fallbacks=N` | 통화 도중 목소리가 몇 번 바뀌었나(429 폴백) |

dev 도구: `scripts/dev_inspect_call.py <call_id>` (전사·문장·증거·레벨 덤프)

---

## 부록. 프롬프트 전문 뽑기

⚠ 새 스크립트 파일을 만들지 않는다 — **사본은 낡는다.** 필요할 때 이걸 붙여 실행한다.
`core/persona_prompt.py` 는 표준 라이브러리만 쓰므로 **의존성 없이 돈다.**

```python
# 캐스케이드 통화에 실제로 들어가는 system_instruction 전문
import sys, pathlib
sys.path.insert(0, r"<프로젝트 루트>")
from core.persona_prompt import build_system_instruction, seed_opening

text = build_system_instruction(
    role="한국어를 가르치는 다정한 비버 선생님",
    personality="친근하고 밝다. 짧게 말하고 자주 되묻는다.",
    level_profile="아주 쉬운 단어와 짧은 문장으로 말한다.",
    locale="en", interests=["여행", "K-pop"], name="Sam",
    lang_band="beginner", target_language="한국어", max_sentences=4,
    language_marker=True,                                   # ← 캐스케이드 전용
    emotion_tags=("neutral","happy","surprised","sad","angry"),  # ← 캐스케이드 전용
)
# Live 일반 통화를 보려면 위 두 줄을 빼고, 체크판을 보려면 study_items=[...] 를 넣는다.
# 콘솔 한글이 깨지면 print 하지 말고 파일로 쓴 뒤 읽는다:
pathlib.Path("prompt.txt").write_text(text, encoding="utf-8")
```

고치기 **전후로 각각 뽑아 `diff -u` 하면** 의도한 줄만 바뀌었는지 바로 보인다.
