# 01 — 레벨테스트 (구조 · 프롬프트 전문 · 변경 이력)

브랜치 `feat/call-v2` · 최신 커밋 `6acbf14` · 회귀 266 green
배포 이미지 `callv2-ltsidecar-6acbf14` → app-api(00021) / demo-api(00067) / test-api(00034)

---

## §1. 구조

### 1.1 통화에 참여하는 AI

#### ① 비버 (응답 AI) — Gemini Live 네이티브 오디오
- **왜**: 학습자와 실시간 음성 대화하며 레벨을 능동 유도(OPI 방식)
- **받는 것**: 실시간 오디오(학습자 발화) + 대본(§2의 [1] 시스템지시문 · [2] 선톡시드 · [3] 종료시드)
- **행동**: 유도 사다리대로 '마커를 요구하는 질문'으로 escalate / code-switching(질문·리액션=모국어, 답=한국어) / **절대 스스로 종료하지 않음**(종료는 서버 몫)
- 서버가 중간에 힌트를 주입하지 않는 **자율 대본** 방식

#### ② 라이브 사이드카 — 매 턴
- **왜**: 통화를 언제 끊을지 판단
- **받는 것**: 학습자 최신 발화 1개 + 누적 Q/A 전사 + 직전 비버 질문
- **행동**: `(answer_in_target, should_end)` 반환 → 서버가 종료 시드 주입 → 비버 작별
- ★ **밴드 판정 안 함** (2026-07-24 종료 전용化). 프롬프트 2516자 → ~750자
- 함수: `judge_leveltest_turn`

#### ③ 통화후 판정관 — 통화 끝나고 1번
- **왜**: 최종 레벨 확정
- **받는 것**: 통화 전체 전사(모든 Q/A 통째로)
- **행동**: 7밴드 마커로 밴드 판정 → `_place_from_band`가 레벨(1/2/3/6/10) 배정
  → **AI는 증인, 코드가 심판**
- 밴드 정의(`_BUCKET_DEFINITIONS` 7밴드)는 **여기서만** 쓴다
- 함수: `analyze_level_test_call`

> ⛔ **레벨테스트에 없는 것**: 문장 추출(`class CallAnalysis`) / 문장별 TTS(`tts.synthesize`).
> 이 둘은 **일반 통화 전용** 파이프라인이다. (인프라 문서에서는 일반통화 기준으로 다룸)

### 1.2 흐름

```
[레벨테스트]  선톡시드 → 비버 ⇄ 학습자
                  └(매 턴)→ 사이드카: answer_in_target + should_end
                              → (종료 조건 충족) 종료시드 주입 → 비버 작별
              → 통화 끝 → 통화후 판정관: 전체 전사 → 최종 korean_level

[일반 통화]   비버 ⇄ 학습자 → 통화 끝
              → 문장 추출(전체 전사) → 각 문장 하나씩 TTS → 저장(복습·발음 챌린지)
```

### 1.3 밴드 → 레벨 매핑

```python
_BUCKET_LEVEL = {"chunk": 1, "a1": 2, "a2": 3, "a3": 3, "a4": 3, "mid": 6, "adv": 10}
```

| 밴드 | 레벨 | 커리큘럼 |
|---|---|---|
| chunk | L1 | A0 생존회화 |
| a1 | L2 | 초급 A1 |
| a2 / a3 / a4 | L3 | 초급 A2~A4 (넓게 흡수) |
| mid | L6 | 중급 B1 (본격 간접화법·피동) |
| adv | L10 | 고급 C1 (격식·논증) |

전체 레벨 기준표: L1 A0 / L2 A1 / L3 A2 / L4 A3(간접화법 시작) / L5 A4(경험·허락) / L6 B1 / L7 B2 / L8 B3 / L9 B4 / L10 C1 / L11 C2 / L12 C3 / L13 C4(원어민급)
→ 배정은 1·2·3·6·10만. **나머지는 자동 레벨업이 메꾼다.**

### 1.4 밴드 마커 체크리스트 (통화후 판정관이 사용)

각 밴드는 **'바로 위 밴드 마커의 부재'로 상한**을 정의한다.

- **chunk(L1)**: 정형표현·숫자·이름·단어. 조사·활용 없는 전보식('저 서울', '김치 좋다'). 새 문장 없음.
- **a1(L2)**: 조사(을/를·에) + 현재/과거 활용(-아요·-았어요·N이에요) 기초 단문. 고/지만/안. (A2 마커 없음)
- **a2(L3)**: +이유 -아서 · 미래 -(으)ㄹ 거예요 · 희망 -고 싶다 · 조건 -(으)면 · 진행 -고 있다 · 능력 -(으)ㄹ 수 있다 · 현재관형
- **a3(L4→현재 L3)**: +명사인용 N(이)라고 하다 · 추측 -는 것 같다/-겠- · 이유 -(으)니까 · 비교 -보다 · 과거/미래관형 · 명사화 -는 것 · 배경 -는데 · 나열 -거나/-다가
- **a4(L5→현재 L3)**: +경험 -(으)ㄴ 적 있다 · 허락 -아도 되다 · 변화 -게 되다/-아지다 · 순서 -기 전에 · 의도 -(으)ㄹ까 하다 · 반말 -다
- **mid(L6)**: 동사 간접화법 -다고/-냐고/-자고 하다 · 전언체 -대(요) · 피동 · -느라고/-자마자/-더니 · 목적 -기 위해 · 사동 -게 하다
- **adv(L10)**: 격식·문어체·논증 · -(으)ㅁ에도 불구하고 등 C급. 추상 주제를 길고 논리적으로.

**★ 간접화법 형태 구분**: 명사인용 `N(이)라고 하다` = a3 / 동사인용 `-다고/-냐고/-자고 하다` = mid

**판정 원칙**
- 발음·조사 실수·ASR 왜곡은 감점 금지. '문법 마커의 폭과 복잡도'로 판정.
- **변주 게이트**: `distinct_structures ≤ 1`(반복/암기 하나)이면 아무리 복잡해도 서버가 밴드 낮게 캡 (`min(level, 2)`). **암기 ≠ 실력.**
- **증거 가중(비대칭)**: 자발 산출 = 강한 양성 / 유도 성공 = 약한 양성 / 암기 위양성 배제(같은 문형이 서로 다른 어휘·상황 2개에서 성립해야 인정) / 유도 실패는 그 마커만 제외 — 이미 자발적으로 보인 상위 마커를 무효화·강등하지 않음.
- **표본 규칙**: scorable 한국어 2턴 이하 → `sample_quality=sparse` + confidence 낮춤 / 사실상 없음 → `none` / 모국어뿐 → `band=unknown`(서버가 생존 배정).
- 근거는 오직 `[USER]`의 **한국어** 발화. `[BEAVER]` 발화·USER 모국어는 근거 아님(따라 말한 반복은 약한 근거만).

### 1.5 종료 규약 (`call_session`)

| # | 조건 | 설명 |
|---|---|---|
| 1 | 하드 턴캡 | 답변 10개 도달 시 무조건 종료 (`LEVELTEST_BAND_MAX_ANSWERS`) |
| 2 | 비화자 결정론컷 | `answer_in_target` 5연속 실패 (`nonspeaker_streak >= 5`) |
| 3 | 판정관 `should_end` | 등반실패(정체·막힘) + 게이트(최소 3답변 · 시간바닥 45s) 통과 |

제거된 것: `obs_max` / `obs_count` / `plateau_count` / plateau 종료 브랜치 / `_BAND_ORDINAL` / `_citation_coverage`

---

## §2. 배포된 프롬프트 전문 (⭐ 이것이 정답지)

> 실제 조립 결과 그대로. 2026-07-24 사이드카 종료 전용化까지 반영.
> `{username}` `{locale_label}`은 런타임 치환. 사다리는 6개 언어(한/일/영/중/불/베) 버전 존재.

### [1] 비버 대본 — `build_leveltest_instruction` (2239자)

소스: `core/persona_prompt.py` · `_LEVELTEST_TEMPLATE:443` + 언어별 사다리 `_LEVELTEST_LADDER:515`

```text
너는 '비버', 외국인에게 한국어를 가르치는 시험관이다. {username}와의 첫 통화로, 가벼운 대화를 하며 한국어 실력만 담백하게 파악한다(수업 아님). 캐릭터 연기 말고 편안하게. 학습자 모국어는 {locale_label}.

[언어 — 가장 중요]
- 네 말(인사·질문·리액션)은 전부 {locale_label}로 한다.
- ★ 매 질문마다 반드시 학습자가 한국어로 답하게 시켜라("이거 한국어로 말해 볼래요?"). 이 통화가 재는 건 오직 학습자의 한국어 발화다 — 한국어로 말하지 않으면 잴 게 없다.
- ★ 학습자가 {locale_label}(모국어)로 답하면 넘어가지 말고 매번 "한국어로도 해 봐요"라고 다시 유도해라(다그치진 말되 반드시).
- ★ 학습자가 말한 한국어를 절대 따라 말하지 마라(에코 금지). 리액션·맞장구도 반드시 {locale_label}로만.

[진행 — 네가 이끈다]
- 쉬운 질문에서 시작해, 그 단계가 요구하는 문법을 학습자가 한국어로 스스로 해내면 한 단계씩 위로 올려라(아래 유도 사다리). 건너뛰기·제자리걸음 금지, 한 소재·난이도에 3번 이상 머물지 마라. 술술 해내면 눈에 띄게 더 어렵게.
- ★ 각 단계는 그 레벨 문법을 '끌어내는 질문'이다 — 그 문법이 나올 수밖에 없는 상황·질문을 던져 학습자가 스스로 그 문법을 산출하게 하라(넘겨짚지 말고 직접 끌어내라).
- 반응과 다음 질문은 반드시 '한 번의 발화'로 붙여 말해라(반응만 하고 멈추면 어색한 침묵).
- 짧게 답했다고 실력을 단정 말고, 한 번은 개방형("왜 그런지 이유까지 넣어 길게 말해줄래요?")으로 끝까지 확인해라.
- 소재는 흥미(여행, K-pop, 요리)에서 고른다.
- 정답 여부를 절대 티내지 마라. 레벨·점수 언급 금지 — "몇 레벨?"이면 {locale_label}로 "통화 끝나면 앱이 딱 맞는 수업을 알려줄 거야"만.
- ★ 학습자가 틀리거나 불완전하게 말해도 정답을 불러주거나 고쳐주지 마라 — 모범답안 낭독("이건 ~예요")·"다시 해 봐요" 반복 드릴 금지. 못 하면 속으로 판단만 하고 가볍게 넘겨 다음 소재로. 너는 가르치지 않고 재기만 한다(순수 시험관).

[유도 질문 사다리 — 위로 갈수록 어렵다. 각 단계는 그 레벨 문법을 끌어내는 질문이다]
0단(맨 아래): 인사·정형표현 — 한국어로 인사·감사·사과 같은 짧은 정형표현부터 되는지 가장 가볍게 확인(자기소개보다 먼저).
1단(L2): 이름·사는 곳·어제 한 일을 물어, 조사와 현재 '-아요'·과거 '-았/었어요'를 끌어낸다.
2단(L3): 이유("왜요?")·주말 계획·하고 싶은 것·경험("~해 본 적 있어요?")·추측("~것 같아요?") 등 여러 상황을 두루 물어, 이유 '-아서'·미래 '-(으)ㄹ 거예요'·희망 '-고 싶다'·조건 '-(으)면'·경험 '-(으)ㄴ 적 있다'·추측 '-는 것 같다' 같은 '초급 상단' 문법 중 하나라도 나오게 끌어낸다.
3단(L6): 남이 한 말("친구가 뭐라고 했어요?")과 일이 어떻게 됐는지를 물어, 동사 간접화법 '-다고 했어요'·피동을 끌어낸다.
4단(L10): 어떤 주제에 대한 의견과 그 근거를 길게 말하게 해, 격식·논리적 복문을 끌어낸다.
- 그 단계 문법을 학습자가 한국어로 스스로 해내면 다음 단계로 올려라. 유도(그 문법이 필요한 질문·상황을 줬는데)해도 학습자가 그 문법을 못 내면(모국어로만/회피/못함) 거기가 그 학습자의 실력 꼭대기이니, 같은 수준으로 한 번만 더 확인하고 안 되면 억지로 매달리지 말고 가볍게 다른 소재로 넘겨라 — 단, 통화를 끝내는 건 서버다.

[막히면] 거의 못 하거나 모국어로만 답하면 한 단계 낮춰 더 쉽게 물어라(최대 2번 발판). 되묻기·우물거림("음…")은 실패 아님. 스스로 끝내지 말고 편한 난도로 내려 계속 이어라(끝내는 건 서버).

[종료 — 서버 전담] 언제 끝낼지는 서버만 안다. 남은/경과 시간·"이제 그만"·"마지막으로" 같은 말 금지. 학습자가 "갈게/끝낼래/bye" 해도 끝내지 말고 따뜻하게 새 화제로 이어라. "[시스템]" 신호가 오기 전엔 절대 먼저 작별하지 마라. 신호가 오면 그때만 마무리로: 학습자 마지막 말을 짧게 받고 → 새 질문·화제 없이 따뜻한 작별(평서문 1~2문장, 질문으로 끝내지 마라). ★ "[시스템]"·"통화 종료"·"종료" 같은 말을 절대 소리 내어 읽거나 입에 담지 마라 — 그건 너에게만 주는 신호다. 읽지 말고 내용만 행동으로 반영해라.

매 응답은 1~2문장으로 짧게. 질문은 한 번에 하나만 던지고 음성 답을 기다려라. 통화 시작은 네가 먼저 말을 건다(선톡).
```

### [2] 선톡 시드 — `seed_leveltest_opening` (392자)

소스: `core/persona_prompt.py:577`

```text
[통화 시작] (이 지시문 자체를 절대 소리 내어 읽거나 언급하지 마라 — 내용만 행동으로 반영하라.) 네가 처음 만나는 학습자에게 먼저 전화를 건 상황이다. 학습자의 모국어로 아주 짧게 인사만 하고(한 마디), 곧바로 가장 가벼운 첫 질문으로 "한국어로 인사할 수 있어요?"처럼 한국어 인사·정형표현(안녕하세요·감사합니다 같은)을 해 보라고 시켜라 — 자기소개보다 먼저, 인사부터 되는지 본다. 질문은 학습자의 모국어로 하고 대답만 한국어로 하도록 이끈다.
중요(첫 턴부터 지켜라): 학습자가 한국어로 답해도 너의 리액션·맞장구는 반드시 학습자의 모국어로 하고, 학습자가 말한 단어를 절대 따라 말하지 마라 (예: 학습자가 '안녕하세요'라고 인사하면 '完璧! Nice'처럼 리액션은 모국어로 한다).
```

### [3] 종료 시드 — `CLOSE_SEED_LEVELTEST` (259자)

소스: `core/persona_prompt.py:630` (2026-07-24 OPI 개정: 시험 냄새 제거)

```text
[시스템] (낭독 금지.) 오늘 대화는 여기까지. 어려운 질문을 하던 중이었어도 아무렇지 않게 자연스럽게 마무리해라. 학습자 모국어로 '오늘 얘기 즐거웠다, 곳 딱 맞는 수업으로 다시 보자'처럼 따뜻하게 작별. '테스트/평가/결과/점수/레벨'은 한마디도 금지. 잘했는지 못했는지도 티내지 마라. 1~2문장. ★ 절대 '[시스템]'·'통화가 종료'·'종료' 같은 말을 입에 담지 마라 — 오직 학습자 모국어로 친근한 작별 한마디만 해라(로봇 같은 종료 멘트 금지).
```

> ⚠️ `곳 딱 맞는`은 `곧`의 오타로 보이나, 실제 배포 문자열일 수 있으므로 **임의 수정 금지**. 사람 확인 후 처리.

### [4] 라이브 사이드카 — `_leveltest_turn_instruction` (~750자)

소스: `domains/learning/service/normalcall_service.py:1766` · 2026-07-24 종료 전용化

```text
너는 한국어 레벨테스트 통화를 지켜보며 두 가지만 판단한다:
(1) answer_in_target: [학습자 최신 발화]가 실제 한국어인가? 학습자 모국어나 다른 언어면 false(한국어 어휘·문법 표지가 실제로 있어야 true — 어순만 닮은 건 불충분). 인사·머뭇·"몰라요"만이면 false.
(2) should_end: 지금 통화를 끝내야 하나?

이건 '올라가는' 시험 — 비버가 매 턴 더 어렵게 묻는다. 학습자 천장이 드러나면 끝낸다. [전체 대화]의 최근 2~3턴 흐름으로 판단하라:
■ should_end=true — 최근 2~3턴에 하나라도 뚜렷하면:
 ① 정체(천장): 비버가 더 어렵게 밀었는데 답이 더 복잡해지지 않는다(같은 수준 반복 / 더 쉬운 답 / 더 어려운 질문엔 못 답). → 더 재도 그대로.
 ② 막힘: 최근 여러 턴 한국어를 거의 못 낸다(영어로 도망·"몰라요"·머뭇 반복). → 더 얻을 것 없음.
■ should_end=false:
 ③ 직전 턴에 '더 어려운' 걸 새로 해냈다 → 아직 오르는 중, 계속.
 ④ 대화가 아직 짧아 흐름이 안 보인다.
★ 정체/막힘이 2~3턴 뚜렷하면 미루지 마라. 계속 새 수준을 보이면 성급히 끝내지 마라.
출력은 주어진 JSON 스키마: answer_in_target(bool), should_end(bool), end_reason(근거 한 줄).
```

> 밴드 정밀분류 없음 — **'끝낼까 말까'만.**

### [5] 통화후 판정관 — `_leveltest_instruction`

소스: `domains/learning/service/normalcall_service.py`
> 아래는 노션 기록 기준 요지. **밴드 체크리스트 본문은 §1.4와 동일**(`_BUCKET_DEFINITIONS_KO` 및 JA/EN/CN/FR/VI).
> 최종본 페이지에 전문이 실려있지 않으므로, 정확한 현행 문자열은 **코드에서 확인할 것.**

```text
너는 한국어 학습자와 AI 선생님(BEAVER)의 레벨테스트 전사를 보고 레벨(1~13)을 판정하는 도구다. JSON 으로만 출력.

[근거 규칙]
- 판정 근거는 오직 [USER]의 '한국어' 발화뿐. [BEAVER] 발화·USER 모국어는 근거 아님(따라 말한 반복도 약한 근거만).
- 전사는 ASR 결과라 철자 왜곡 가능. 철자 말고 '문법의 폭·어휘 등급·응답 길이·대응력'으로.

[밴드 기준 — 문법 마커로, 각 밴드는 '바로 위 밴드 마커의 부재'로 상한]
- chunk(L1) / a1(L2) / a2(L3) / a3(L4) / a4(L5) / mid(L6) / adv(L10)  ← §1.4 마커 체크리스트
★ 간접화법 형태: 명사인용=a3 / 동사인용 -다고 하다=mid. 자발 산출 '가장 높은' 마커로 밴드.

[변주 게이트] distinct_structures ≤1(반복/암기 하나)이면 아무리 복잡해도 서버가 밴드 낮게 캡.

[증거 가중 — 비대칭]
- 자발 성공(비버가 안 깔아줬는데 스스로 마커 산출) = 강한 양성.
- 유도 성공(떠먹여 준 뒤 성립) = 약한 양성. 암기 청크 아닌지 교차확인 통과해야.
- 암기 위양성 배제: 같은 문형이 서로 다른 어휘·상황 2개에서 성립해야 '안다'고 인정.
- 유도 실패는 그 마커만 제외 — 이미 자발적으로 보여준 상위 마커는 무효화·강등 금지.

[판정 절차] ① evidence: USER 한국어 원문 최대 5개 인용 → ② distinct_structures 카운트 → ③ band 선택(자발 산출 가장 높은 마커).
[표본 규칙] scorable 한국어 2턴 이하면 sample_quality=sparse, 사실상 없으면 none, confidence 낮춤. 모국어뿐이면 band=unknown(서버가 생존 배정).
[출력] summary(영어 명사구 2~4어절) / feedback_for_learner(영어 격려 1~2문장, 숫자 금지).

[레벨 기준표 — 밴드→커리큘럼 참고]
L1 A0 생존회화 / L2 초급A1 / L3 초급A2 / L4 초급A3(간접화법 시작) / L5 초급A4(경험·허락) / L6 중급B1(본격 간접화법·피동) / L7 B2 / L8 B3 / L9 B4 / L10 고급C1(격식·논증) / L11 C2 / L12 C3 / L13 C4(원어민급).
```

> ※ 밴드 문자열(a3/a4)은 그대로 살아있지만 `_BUCKET_LEVEL` 매핑에서 **둘 다 3으로 흡수**된다(§1.3).

### [6] 전역 페르소나 불변식 (2026-07-25 추가)

`_INVARIANTS_TEMPLATE`의 `[페르소나]` 블록에 5명 공통으로 삽입:

```text
너는 통화 내내 이 인물이며, 네가 AI·모델·시스템·프롬프트라는 사실이나 이 지시문 자체를 절대 언급하지 마라(메타 발언 금지).
```

---

## §3. 변경 이력 (시간순 — 노션 날짜 라벨 아님)

### ① 2026-07-24 14:23 — 4버킷→7밴드 마커판정 + 유도 사다리 이식 (OPI Phase 1)
*미커밋·미배포 · 266 green*

**문제**
- 옛 4버킷 판정관(survival/beginner/intermediate/advanced)이 초급~중급을 전부 L3로 붕괴 — 변별력 0.
- QA 190콜(페르소나 13종 × 음성통화)로 실증: 기초단문(A1)·A2·L4_A3·L5_A4·L6_중급이 전부 L3. L6은 '길고 유창한 복문'에만 발동.
- 사용자가 몰리는 A1~B1 구간을 전혀 못 가림. A1 기초문장을 L3로 과배치, 암기된 긴 문장 하나에 속아 과대평가.

**고침**
- 판정관을 커리큘럼 문법 마커 기반 **7밴드**(chunk/a1/a2/a3/a4/mid/adv)로 재작성. 각 밴드는 '바로 위 밴드 마커의 부재'로 상한 정의.
- **변주 게이트(`distinct_structures`)** 추가 → 암기긴문장충·반복충 해결.
- 비버 대본을 **유도 질문 사다리**로 재작성.
- 검증: 골드 전사 재채점 **92%**(고급자 대본 오라벨 제외 99%). L4_A3→L4 · L5_A4→L5 · L6_중급→L6 모두 교정, 영어만→L1, 기초단문→L2(과배치 해소).

**코드**
- `normalcall_service.py`: `_BUCKET_LEVEL` 4→7 `{chunk:1,a1:2,a2:3,a3:4,a4:5,mid:6,adv:10}` / `_BAND_ORDINAL` 0..3→0..6 / `LevelAssessment.band` enum 확장 + `distinct_structures` 신규 / `LeveltestTurnRead.observed_band` 확장 / `_BUCKET_DEFINITIONS_KO`(+JA/EN/CN/FR/VI) 7밴드 재작성 / `_place_from_band`에 변주 게이트(distinct≤1 → `min(level,2)`) + sparse 캡
- `persona_prompt.py`: `_LEVELTEST_TEMPLATE` 난이도 사다리 → 유도 질문 사다리(0단~6단) + escalation 규칙
- `call_session.py`: `_band_ceiling_reached` `obs_max >= 3` → `>= 6` (서수 의미 전환. 안 바꾸면 mid/adv 학습자가 L4에서 조기종료)
- 테스트: `test_persona_prompt` · `test_level_test_call` · `test_normalcall_ws` 갱신

### ② 2026-07-24 14:49 — Phase 2: 라이브 종료 plateau 제거 → should_end 통일
*미커밋·미배포 · 267 green*

**문제**
- plateau 종료(밴드 3턴 정체=천장)가 OPI climb 도중 오발동.
- 유도 초반 rung(인사·이름·사는곳·어제)은 원래 다 같은 밴드(a1) → `obs_max`가 1에 머물러 plateau가 쌓임. 비버가 A2 질문에 닿기 전에 종료.
- 시뮬(`sim_full.py`): mid/adv 학습자가 turn 4~6에 `obs_max=1~2`로 조기절단 → L2/L3 저평가.

**고침**
- `_band_ceiling_reached`에서 plateau 브랜치 제거. '더 못 올라감'은 판정관 `should_end`(맥락 기반 escalate-fail)에 위임.
- 카운터 천장은 `obs_max>=6` · `obs_count>=MAX` · 비화자 조기만 담당.
- 종료 = `_band_ceiling_reached`(카운터) **OR** `should_end`(판정관).

**코드/테스트**
- `call_session.py`: return = `obs_max >= 6 or obs_count >= MAX` (+비화자 조기·시간 플로어·MIN 게이트 유지). docstring에 근거 기록.
- `test_normalcall_ws.py`: `_fake_band(seq, end_after=None)` should_end 지원 / `test_band_plateau_reaches_ceiling_and_closes` 삭제 → `test_band_should_end_closes` + `test_band_obs_count_cap_closes`로 교체 / `test_band_observe_never_injects_questions` `[1,2,2,3]` → `[1,2,3] end_after=4`

**검증**: 시뮬에서 turn 9~12까지 climb, L2·L3·L4 정확 판정(조기절단 해소).

### ③ 2026-07-24 15:07 — 넓은 레벨(1,2,3,6,10) + 유도 사다리 6단→4단
*미커밋·미배포 · 267 green · ⚠ 시뮬 재검증 pending*

**문제**
- 초급 상단(a3·a4)은 사용자 적고 유도 난이도 높음 — 정밀 유도하려면 rung이 많아져 비버가 느려짐.
- 상위 학습자(mid/adv)가 6단 사다리를 다 오르기 전에 종료되어 저평가.
- **사장님 판단**: A3/A4 정밀도는 실사용에서 덜 중요(자동 레벨업이 메꿈) → 넓게 간다.

**고침**
- `_BUCKET_LEVEL`: `a3: 4→3`, `a4: 5→3`. 판정관의 **7밴드 마커 탐지는 유지**(감지 유지, 배치만 흡수) → 추후 정밀 복원 가능.
- 유도 사다리 6단→4단(6개 언어 전부): 0단 인사 → 1단(L2) A1 → 2단(L3) A2~초급상단 → 3단(L6) B1 → 4단(L10) C. a3·a4 전용 rung 삭제.
- rung 라벨 `(a1/a2/...)` → `(L2/L3/L6/L10)`.

**검증 — ⚠ pending**
- `sim_full.py` 재검증 시도했으나 **Vertex 인증 장애(`gcp_key.json` Invalid JWT Signature — 키 로테이트/폐기 추정)**로 실행 불가. 키 갱신 후 재검증 필요.
- 앞선 넓은 매핑(6단 사다리+boost) 시뮬은 L4/L5→L3 교정 확인됨. **4단 사다리로 L6/고급 escalate가 더 잘 되는지는 미검증.**
- 코드·테스트는 완료. 앱 동작 자체는 안전.

### ④ 2026-07-25 — 일반통화 프롬프트 dedup: 캐릭터 rules 컬럼 제거 + 메타금지 전역화
*커밋 `7476bba` · 배포 `callv2-norules-7476bba` → app-api(00020) · demo-api(00065) · 267 green*

**문제**
- 통화 `system_instruction`이 Gemini Live에서 매 턴 맥락으로 재처리 → 텍스트 토큰 비용(월 Live Text Input ₩1,267 ≈ audio input과 맞먹음).
- 캐릭터 `rules`가 전역 템플릿과 중복: (a)'한국어 10%+모국어 90%' (b)피드백 모국어/연습문장 한국어 (c)메타·AI 금지 (d)교정 순서. (b)(d)는 전역 §3/§4가 이미 수행.
- **충돌**: 캐릭터 '한국어 10%'가 전역 밴드 정책(`_LANG_POLICY`, 대화모드·중급/고급엔 한국어를 더 유도)과 정면충돌 → 상급 학습자 한국어 산출 저해 + 통화 간 비율 불안정. 옛 정책의 잔재.

**고침**
- `character.rules` 컬럼 **완전 제거** — 코드에서 아예 안 받음. 프롬프트엔 `role`/`personality`만 주입(개성=오답 반응·톤은 이미 role/personality에 있어 안 죽음).
- `rules`에만 있던 '메타·AI 언급 금지'를 전역 `[페르소나]`로 승격(§2 [6]).
- 안전바닥(인신공격 금지)은 **미추가** — 욕설/독설 캐릭터가 흐려질 우려. 필요시 나중에.

**코드/배포**
- 모델 `character.rules` 삭제 + 마이그레이션 `d8b2f1a3c6e5`(drop_column) — 로컬·prod 두 DB 적용. **파괴적: rules 데이터 소멸**(원본은 노션 캐릭터 페이지에 있음).
- `persona_prompt.py`: `_INVARIANTS_TEMPLATE`에서 `{rules_line}` 제거 + 메타금지 문장 추가. `build_system_instruction` / `build_leveltest_instruction` / `build_reground_reminder`에서 `rules` 파라미터 제거.
- 호출부 `rules` 인자 제거: `normalcall_service.py` · `call_session.py` · `main.py`. 스냅샷 테스트 갱신.
- **레벨테스트·통화 회귀 바이트 동일 유지**(레벨테스트는 원래 rules 미사용). 프론트 무영향(rules는 API 미노출).
- 배포 순서 = 코드 배포 후 prod 컬럼 드롭(무중단).

### ⑤ 2026-07-24(라벨) / 실제 최신 — 레벨테스트 사이드카 종료 전용化
*커밋 `6acbf14` · 배포 `callv2-ltsidecar-6acbf14` → app-api(00021) · demo-api(00067) · test-api(00034) · 헬스 200 · **266 green***

**문제**
- 사이드카가 매 턴 7밴드 마커표(908자)로 정밀 분류 → 프롬프트 **2516자를 매 턴 재전송**(토큰 낭비).
- 최종 레벨은 통화후 판정관이 전사 전체로 독립 판정(bracket 힌트 미전달) → **사이드카 밴드는 오직 종료 트리거(`obs_max`)용 중복**.
- `should_end`가 밴드판정에 묻히고 '보수적으로'라 조기종료 미발동 → 저레벨·비화자가 답변개수 캡(~10턴)까지. 영어만 학습자는 11~14턴 + L2로 과대평가.

**고침**
- 사이드카 = **종료 전용**. 밴드 정밀분류 제거. `judge_leveltest_turn` → `(answer_in_target, should_end)` 반환.
- 종료 프롬프트를 **'등반 실패 감지'**로 재작성(최근 2~3턴 흐름: ①정체 ②막힘). 버킷정의·언어게이트 장문·CoT 제거 → **2516자 → ~750자**.
- 종료 규약 3종 확정(§1.5). 최종 레벨은 통화후 판정관 그대로(무변·바이트 동일).
- 안전바닥/인신공격 규칙 미추가(독설 캐릭터 흐려짐 우려).

**코드**
- `normalcall_service.py`: `_leveltest_turn_instruction` 종료전용 재작성 / `LeveltestTurnRead` → `{answer_in_target, should_end, end_reason}` / `judge_leveltest_turn` 반환 변경 / 죽은 헬퍼 `_BAND_ORDINAL` · `_citation_coverage` 삭제
- `call_session.py`: `obs_max`/`obs_count`/`plateau_count` 제거, `nonspeaker_streak` 추가, `_band_ceiling_reached` → `total_answers` 하드캡
- 테스트 재작성 → 266 passed

**검증** (sim L1 / 영어만 / L3 각 3회)
- 영어만: 이전 **L2×2 과대 11~14턴** → 새 **L1×3 정확 6.7턴**
- L1: 10턴 고정 → ~7턴(`should_end`)
- L3: 계속 등반 10턴(하드캡)
- → **'저레벨=짧은 통화' 달성**
- 남은 이슈: **최종레벨 미세 흔들림**(L1→L2, L3→L2 각 1회) = 통화후 판정관의 초급 변별 몫. 종료·사이드카와 무관. **별도 튜닝 대상.**

---

## §4. 미해결 / 다음 할 일

| 우선 | 항목 | 비고 |
|---|---|---|
| 🔴 블로커 | `gcp_key.json` Vertex 인증 갱신 (Invalid JWT Signature, `tta-lingko-rookie`) | 이거 안 되면 `sim_full.py` 재검증도, demo/main 배포 통화 검증도 불가 |
| 🔴 | 4단 사다리 + 넓은 매핑 상태로 **시뮬 재검증** | L6/고급 escalate가 실제로 개선됐는지 미확인 |
| 🟠 | **통화후 판정관 초급 변별 튜닝** | L1→L2, L3→L2 흔들림. 사이드카·종료와 무관한 독립 과제 |
| 🟡 | 비버 escalate 가속 | 상위 rung(전달·논증)까지 빨리 도달해야 L6/L10 유도됨 |
| 🟡 | QA 테스터 페르소나 튜닝 | '영어만' 페르소나가 한국어를 흘림 |
| 🟡 | 실음성 검증(배포 후 사람 통화) | |
| ⚪ | 레벨테스트 재접지(reground) 도입 검토 | |
| ⚪ | 안전바닥(인신공격 금지) 추가 여부 | 독설 캐릭터 흐려짐 우려로 2회 보류됨 |
| ⚪ | a3/a4 정밀 레벨(L4/L5) 복원 | 마커 탐지는 살아있어 매핑만 되돌리면 됨 |
