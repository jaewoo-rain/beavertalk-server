# 힌트 예시를 책갈피에 담는다 — 기획

작성 2026-09-05 · 상태 **기획 / 미구현** · 요청: 사장님 + 프론트 문서

> 통화 중 힌트로 나오는 문장도 즐겨찾기(책갈피) 등록할 수 있게.

---

## 0. 프론트 주장 검증 — **3개 다 사실이다** (직접 확인)

프론트가 준 문서의 전제를 코드로 대조했다. 하나라도 틀리면 기획이 통째로 달라진다.

| 주장 | 확인 | 근거 |
|---|---|---|
| `source_type` 컬럼이 이미 있다 ⇒ Alembic 불필요 | ✅ 사실 | `models/sentence.py:33` |
| `call_repository` 가 문장을 **필터 없이** 싣는다 | ✅ 사실 | `call_repository.py:29` `selectinload(Call.sentences)` |
| `HintExample` 에 `id` 가 없다 | ✅ 사실 | `protocol.py:389-394` (korean/roman/native 뿐) |

⭐ 그리고 프론트가 **앱 수정을 이미 끝냈다.** 책갈피 UI 는 `sentence_id` 만 있으면 도므로
  (`PATCH /sentences/{id}/bookmark` 가 이미 있다), 서버가 id 를 실어 주면 앱 수정 0줄이다.

---

## 1. ⭐ 위험이 두 번 접혔다 — 결국 필터가 필요 없다

기획 초안에서 «저장하면 결과 화면이 오염된다» 를 1순위 위험으로 적었다. 두 가지가 그걸 지웠다.

### ① 사장님 결정 — 오늘 배운 문장에 섞여도 된다 (2026-09-05)

> "책갈피로 체크한 문장은 오늘 배운 문장에 포함이 안되어야해서 문제라는거면
>  오늘 배운 문장에 포함시켜도돼"

⇒ 담은 힌트가 결과 화면의 «오늘 배운 문장» 에 나오는 것은 **문제가 아니다.**
  사용자가 스스로 담은 문장이므로 «배운 것» 으로 보는 편이 오히려 자연스럽다.

### ② ⛔ 내가 적었던 「증거 오염」은 **틀린 걱정이었다** (코드로 확인, 정정)

초안에 «힌트가 item_evidence 에 잡히면 레벨이 잘못 오른다, 그런데 append-only 라 못
지운다» 고 적었다. **사실이 아니다.**

증거는 `Sentence` 가 아니라 **`CallRawData`(전사)** 에서 만들어진다:

```
normalcall_service — 증거 검출
    "전사 인덱스: USER 턴(행 위치·USER 순번·turn_index·정규화 내용)"
    "if hit is None:  # ② USER 전사 부재 → 폐기"
```

인용 검증이 **전사의 USER 행과 대조**하고, 없으면 폐기한다. 힌트는 전사에 안 들어가므로
`Sentence` 행을 아무리 만들어도 증거·레벨에 **닿지 않는다.**

⇒ dev 의 `f502780`(결과 화면 「이번 통화에서 쓴 표현」)도 `ItemEvidence` 를 읽으므로
  **역시 영향이 없다.** 초안에서 «판을 바꾼다» 고 한 것도 취소한다.

### ⇒ 결론: `source_type != "hint"` 필터는 **넣지 않는다**

전수 조사도 불필요하다. 작업이 3단계에서 2단계로 줄었다.

⚠ 다만 `source_type="hint"` 는 **계속 남긴다.** 나중에 «힌트에서 담은 것만 보기» 같은
  요구가 오면 그때 쓰는 표식이고, 없으면 되짚을 방법이 없다.

## 2. 설계

### 2-1. 흐름 — ⭐ **담을 때만 저장한다** (사장님 결정 2026-09-05)

> "즐겨찾기 안해도 DB저장되면 너무 낭비인데? 쓸일없는 데이터를 저장하겠다고?"

```
힌트 사이드카가 예시 3개를 만든다
   ↓
ServerHint(examples=[{korean, roman, native}]) 로 전송   ← ⭐ DB 안 건드림
   ↓
사용자가 힌트 ②의 🔖 를 누른다
   ↓
POST /api/v1/sentences/from-hint  {call_id, korean, native, roman?}
   ↓
서버가 Sentence(source_type="hint", is_bookmarked=True) 1행 생성 → sentence_id 반환
   ↓
앱이 그 id 로 이후 해제/재등록(PATCH /sentences/{id}/bookmark)
```

⛔ **버린 안(A)**: 힌트가 뜰 때 3개를 모두 저장하고 id 를 실어 보내는 방식.
  앱 수정이 0줄이라는 장점이 있었으나, 5분 통화에 힌트 5회면 **15행이 쌓이고 대부분
  아무도 안 담는다.** 담긴 것만 남기는 편이 옳다.

⚠ 대가: 프론트가 **이미 A 로 구현했다**(`HintExample.id` 를 기대). 재작업이 필요하고,
  그 사실을 프론트에 알려야 한다. 아래 §7 에 전달문을 적어 뒀다.

⭐ 그리고 B 가 §1 의 위험도 **줄인다** — 저장되는 행이 «사용자가 담은 것» 뿐이라
  결과 화면·증거 오염의 표면이 15행에서 실제 담은 수로 작아진다. 다만 **필터는 여전히
  필요하다**(담은 힌트도 «말한 적 없는 문장»이다).

### 2-2. 손댈 곳 3

| 파일 | 무엇 |
|---|---|
| `routers/sentence.py` (또는 신규) | ⭐ `POST /sentences/from-hint` 신설 |
| `service/sentence_service.py` | 힌트 → `Sentence(source_type="hint", is_bookmarked=True)` |

⛔ `protocol.py` 는 **안 건드린다.** B 에서는 `HintExample.id` 가 필요 없다
  (저장을 안 하므로 줄 id 가 없다). A 를 전제로 프론트가 기대하던 필드다.

⚠ `id` 를 **선택 필드**로 둔다. 저장이 실패해도 힌트는 나가야 한다(R5) — 그때 `id=None`
  이면 앱이 책갈피 버튼만 안 보여주면 된다. 힌트 자체가 사라지는 것보다 낫다.

---

## 3. ⚠ 아직 안 정한 것 (착수 전 결정 필요)

### ② 저장한 힌트 문장이 **다른 기능에 섞이나**

`Sentence` 를 읽는 곳이 결과 화면만이 아니다. 최소한 이것들을 봐야 한다:
- 복습(review) · 발음 평가 · 문장 목록 · 레벨 시스템 증거 추출(`item_evidence`)
- ⛔ 특히 **증거 추출**: 힌트 문장이 «학습자가 말했다» 로 잡히면 레벨이 잘못 오른다.
  `item_evidence` 는 append-only 라 **지울 수 없다**(CLAUDE.md 규율).

⇒ 착수 1단계가 **`Sentence`/`Call.sentences` 를 읽는 자리 전수 조사**다. 필터를 빠뜨린
  곳이 하나라도 있으면 그 기능이 조용히 오염된다.

### ⛔ (2026-09-05 추가) 증거 오염이 이제 **사용자 화면에 직접 뜬다**

기획서를 쓴 직후 dev 에 `f502780 feat(call): 통화 결과에 「이번 통화에서 쓴 표현」을 낸다`
가 들어왔다. 이게 판을 바꾼다:

```
call_service._used_items()   select(ItemEvidence.item_id, ItemEvidence.learner_quote, ...)
```

⇒ **증거(`item_evidence`)를 읽어 결과 화면에 「이번 통화에서 쓴 표현」으로 보여준다.**
  전에는 증거가 오염돼도 레벨 계산에만 영향을 줬는데, 이제 **사용자가 직접 본다.**
  힌트 문장이 증거로 잡히면 «당신이 이 표현을 썼습니다» 라고 화면에 뜬다 — 말한 적이 없는데.

⚠ 그리고 `item_evidence` 는 **append-only 라 지울 수 없다**(CLAUDE.md). 한 번 섞이면 끝이다.

⇒ §4 의 0번(전수 조사)에 이 경로를 **반드시 포함**한다. 그리고 §5 의 3번 회귀가
  이제 «레벨이 잘못 오른다» 가 아니라 «사용자에게 거짓말을 한다» 를 막는 시험이다.

### ③ 통화 없이 힌트만 있는 경우

`Sentence.call_id` 가 필수인지 확인해야 한다. 힌트는 통화 중에만 나오므로 문제없어 보이나,
레벨테스트 통화에서도 힌트가 나오는지(그러면 그 통화 결과에도 섞이는지) 확인이 필요하다.

---

## 4. 작업 분해 (구현은 나중)

- [ ] 1. `POST /sentences/from-hint` — DTO(call_id·korean·native·roman?) + 소유 검증
      ⛔ **본인 통화인지 반드시 본다.** 남의 call_id 로 문장을 심을 수 있으면 안 된다
- [ ] 2. service — 같은 (call_id, korean) 이 이미 있으면 **재생성 말고 그 행을 담는다**
      (연타·재진입에 중복 행이 생기면 안 된다)
- [ ] 4. 회귀 — 아래 §5
- [ ] 5. 배포 후 통화 1건 — 힌트를 담고, 결과·복습·발음에서 정상으로 보이는지 눈으로

## 5. 회귀에서 못박을 것

⚠ 초안의 시험 표를 **갈아엎었다.** 방식이 A(뜰 때 저장)→B(담을 때 저장)로 바뀌었고
  필터가 없어졌으므로, 옛 표는 존재하지 않는 것을 검사하고 있었다.

| # | 시험 | 왜 |
|---|---|---|
| 1 | 🔖 를 누르면 `Sentence(source_type="hint", is_bookmarked=True)` 1행이 생긴다 | 기능 자체 |
| 2 | ⛔ **같은 힌트를 두 번 담아도 행은 하나다** | 연타·재진입에 중복이 생기면 목록이 더러워진다 |
| 3 | ⛔ **남의 `call_id` 로는 못 담는다**(403/404) | 이게 없으면 아무 통화에나 문장을 심을 수 있다 |
| 4 | 힌트가 뜰 때는 `Sentence` 가 **0행** 생긴다 | B 의 요점 — 안 담을 문장을 저장하지 않는다 |
| 5 | 담은 뒤 `PATCH /sentences/{id}/bookmark` 로 해제·재등록이 된다 | 기존 API 와 이어진다 |

⭐ **2·3번이 핵심이다.** 나머지는 안 되면 눈에 띄지만, 이 둘은 조용히 틀린다 —
  중복은 목록이 이상해질 때까지 모르고, 소유 검증은 아무도 안 눌러 보면 영영 모른다.

---|---|---|
| 1 | 힌트 저장 후 `ServerHint.examples[].id` 가 실린다 | 앱이 책갈피를 걸 수 있다 |
| 2 | ⛔ 통화 결과 조회에 `source_type="hint"` 문장이 **안 나온다** | 이 기획의 사고 방지 |
| 3 | ⛔ 증거 추출이 힌트 문장을 **안 읽는다** | 레벨이 잘못 오르면 되돌릴 수 없다 |
| 4 | 저장 실패해도 힌트는 나간다(`id=None`) | R5 |
| 5 | 힌트 문장도 `PATCH /sentences/{id}/bookmark` 가 된다 | 기능 자체 |

⭐ 2·3번이 핵심이다. 나머지가 다 돌아도 이 둘이 깨지면 **조용히 데이터가 오염**된다.

---

## 6. 함께 온 것 — TTS 는 서버 쪽이 끝났다

프론트가 같은 문서에서 `POST /api/v1/tts/speech` 를 이미 붙였다고 알려 왔다.
서버 구현은 **완료·커밋됨**(`8c875a4`), 배포만 남았다.

⚠ 프론트가 `character_id` 를 요청에서 뺐는데, 우리 구현은 그 필드가 **선택**이라
  `{"text": "..."}` 만 보내도 대표 캐릭터로 동작한다. 계약이 맞는다.
⭐ 프론트가 «캐시 키에는 character_id 를 남겼다» 고 했는데, 옳은 판단이다 — 서버 ETag 도
  같은 이유로 `voice` 를 키에 넣었다(캐릭터가 바뀌면 다른 소리다).


---

## 7. ⚠ 프론트에 전달할 것 — 방식이 A 에서 B 로 바뀌었다

프론트는 «힌트가 뜰 때 서버가 3개를 저장하고 `HintExample.id` 를 실어 준다»(A)를 전제로
이미 구현했다. 사장님 결정으로 **B** 로 간다:

> 힌트가 뜰 때는 DB 에 아무것도 안 씁니다(안 담을 문장을 저장하는 건 낭비라서요).
> `HintExample.id` 는 **안 내려갑니다.**
>
> 🔖 를 누르면 이 API 를 불러 주세요:
> ```
> POST /api/v1/sentences/from-hint
> { "call_id": 1234, "korean": "화장실이 어디예요?", "native": "Where is the restroom?" }
> → 200 { "sentence_id": 567, "is_bookmarked": true }
> ```
> 그 뒤 해제/재등록은 기존 `PATCH /sentences/{id}/bookmark` 그대로입니다.
>
> ⚠ 같은 힌트를 두 번 담아도 행이 하나만 생깁니다(서버가 중복을 막습니다).
