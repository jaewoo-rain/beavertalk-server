# 가상환경 / 캐시
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# VCS / 에디터 / 도구
.git/
.gitignore
.vscode/
.idea/
.claude/
.mcp.json

# 비밀값 / 로컬 설정 (절대 이미지에 넣지 않음)
.env
.env.*

# 런타임에 불필요
front/
docs/
tests/
*.md

# 로컬 커리큘럼 소스 원본(빌드에 불필요 — 2.4GB)
level/

# 대용량 생성물
api-docs.html
openapi.json
BeaverTalk.vuerd.json

# 멀티랭귀지 커리큘럼 원천(최상위 level/ — 1.9GB, 런타임 불필요·DB에서 읽음). /level/ 는
#   루트 앵커라 런타임 루브릭이 있는 assets/level/ 는 유지된다.
/level/
# 파생 커리큘럼 JSON(まるごと '재배포 금지' 승계·런타임 불필요 — 이미지에 넣지 않음).
assets/level/curriculum_v2_ja/

gcp_key.json
tts_key.json

# FCM/서비스계정 키 (절대 이미지에 넣지 않음)
*fcm*.json
*service-account*.json# (Cloud Build 업로드 제외 — .dockerignore 와 동일 유지. .gitignore 와 분리해 gitignore 변경이 빌드를 깨지 않게)

# 캐릭터 아바타 원본(1440x1440 PNG 5장, 16MB) — 런타임 불필요. 배포처는 Supabase
#   공개 버킷 character_image/ 이고 서버는 URL만 DB에 들고 있다. 빌드 업로드·이미지에서 제외.
assets/avatars/
