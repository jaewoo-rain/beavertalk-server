"""잠금 — 표정(set_face) 규칙 블록 + 툴 선언 문구 · 캐스케이드 표기 규칙(감정 태그·언어 마커). 옛 core/persona_prompt.py·core/gemini_live.py 에서 **이동**(2026-09-12).

⭐ FACE_TOOL_RULE 은 **한 상수**다 — 연구실(exp/ctx-compress-31)의 표정 블록 변경(neutral 제거·두 코스 연결·중복 억제)을 여기 한 자리에서 갈아끼운다.
⛔ 규칙 블록과 툴 선언(SET_FACE_DESCRIPTION·SET_FACE_EMOTION_DESCRIPTION)은 **같은 말**을 해야 한다 — 모델은 둘 다 읽는다(R1, 2026-08-20).
"""
from __future__ import annotations

# ⛔⛔ 절대 고치지 마라 — 서버 판정/표정/진도 배관이 이 문장을 **그대로** 기대한다(gemini 2.5·3.1 두 모델 모두 같은 문장을 쓴다).
#   고치려면 bt-back 과 시험(tests/test_prompt_locked_hash.py · tests/test_prompt_*.py)을 같이. 사람이 고치는 문구는 core/prompts/editable/*.md 다.


# ⛔⛔ 절대 고치지 마라 — 서버 판정/표정/진도 배관이 이 문장을 **그대로** 기대한다(gemini 2.5·3.1 두 모델 모두 같은 문장을 쓴다).
#   고치려면 bt-back 과 시험(tests/test_prompt_locked_hash.py · tests/test_prompt_*.py)을 같이. 사람이 고치는 문구는 core/prompts/editable/*.md 다.
# 언어 마커 표기 규칙(캐스케이드 전용, 옵트인).
# ⛔ **code-switching 규칙(불변 규칙 3)은 손대지 않는다.** 무엇을 어느 언어로 말할지는 거기서
#   정하고, 이 블록은 **이미 정해진 그것을 어떻게 표기하느냐**만 얹는다.
# 왜 따옴표가 아니라 __ 인가: 이 프롬프트는 이미 따옴표를 두 용도로 쓴다(대사 인용 / 특정
#   표현만 타깃으로 들려주기). 언어 마커로 겹쳐 쓰면 충돌한다. __ 는 자연스러운 한국어·영어
#   문장에 안 나오고, 마크다운 굵게 문법이라 모델이 잘 지킨다.
LANGUAGE_MARKER_RULE = """
[표기 규칙 — 언어 마커]
- {target} 로 말하는 부분은 __이렇게__ 밑줄 두 개로 감싸라. {locale_label} 부분은 감싸지 않는다.
  예) 오늘은 __How are you?__ 를 배워볼까?
- 서버가 이 경계로 잘라 **부분마다 그 언어의 목소리로 읽는다.** 안 감싸면 {locale_label}
  발음으로 읽혀서 {target} 학습에 방해가 된다.
- 마커는 소리 내어 읽는 기호가 아니다("밑줄"이라고 말하지 마라). 한 문장에 여러 번 써도 된다.
- 무엇을 {target} 로 말할지는 위 규칙 3 그대로다 — 이 규칙은 **표기만** 정한다."""


# ⛔⛔ 태그 표기가 **꺾쇠**인 이유(2026-08-12 실통화 00147/00148). 예전엔 `[칭찬]` 이었는데,
#   이 프롬프트는 `[공부 모드]` `[대화 모드]` `[학습자 수준]` 처럼 **대괄호를 구획 라벨로**
#   쓴다. 두 규약이 같은 기호를 쓰니 모델이 태그 자리에 **모드 이름**을 넣었다:
#     · `[공부 모드] ~~` 가 그대로 TTS 로 가서 "공부 모드"를 소리 내어 읽었다
#     · 집합 밖이라 감정도 못 읽어 그 통화의 감정이 **매 대답 '없음'** 이었다
#   ⚠ 라벨 쪽을 못 바꾸는 이유: 그 블록은 **Live 도 쓰는 공용 자산**이고 바이트 동일
#     스냅샷 회귀가 걸려 있다. 그래서 **캐스케이드 전용 옵트인인 이쪽**이 비킨다.
EMOTION_TAG_RULE = """
[표기 규칙 — 감정 태그]
- 대사 **맨 앞에** 이번 대답의 감정을 하나만 붙여라: {tags} 중에서 고른다.
  예) <happy> 우와, 정말 잘했어요!
- ⛔ **대괄호를 쓰지 마라.** 위에 나오는 [공부 모드] 같은 대괄호 표기는 이 문서의 구획
  이름이지 대사에 쓰는 기호가 아니다. 감정 태그는 반드시 <꺾쇠> 로 쓴다.
- **한 대답에 하나만.** 문장마다 바꾸지 마라.
- ★ **네 성격대로 골라라.** 같은 상황이라도 캐릭터마다 다른 감정이 나오는 게 맞다 —
  학습자가 틀렸을 때 어떤 캐릭터는 안타까워하고(sad), 어떤 캐릭터는 빈정대고(angry),
  어떤 캐릭터는 아무렇지 않게 넘어간다(neutral). 위 [페르소나]·[성격]이 네 기준이다.
- ⛔ 다만 **대사와 어긋나면 안 된다.** 칭찬하면서 angry, 나무라면서 happy 는 쓰지 마라 —
  학습자가 보는 얼굴과 듣는 말이 따로 놀면 그게 제일 이상하다.
- 목록에 없는 말을 지어내지 마라. 애매하면 <neutral> 을 써라.
- 태그는 소리 내어 읽는 기호가 아니다("꺾쇠"라고 말하지 마라). 서버가 걷어내고
  **그 감정에 맞는 목소리로 읽는다.**"""


# ⭐ 라이브 표정 신호(2026-08-18 계측 스파이크). **텍스트 태그가 아니다** —
#   native-audio 는 쓰는 족족 낭독하므로(EMOTION_TAG_RULE 이 Live 에 금지된 이유),
#   표정은 **function-call** 로만 나갈 수 있다.
# ⛔ 말 구조를 고정하지 않는다("반응 먼저, 설명 나중" 같은 틀을 강요하지 않는다).
#   대사가 틀에 박히고 문장상한과도 부딪힌다. 여기서 정하는 건 **타이밍뿐**이다.
# ⭐⭐ R1(2026-08-20) — **"부른 뒤에 말한다" 를 "동시에 낸다" 로 바꿨다.**
#
#   ⛔ 왜: 옛 문구가 "부른 뒤에는 반드시 말을 해야 한다"였다. **순차**를 가르친 것이고,
#     모델이 그대로 했다 — 실측 call 1103 은 함수 호출이 **전부 오디오 0.00초**에 왔다
#     (= 말하기 전에 부르고 턴을 닫았다). native-audio 는 그렇게 오디오 없는 호출을
#     side-effect 로 보고 `turnComplete` 를 같이 보내며(google/adk-python#4902),
#     그 뒤 도착한 우리 응답이 **새 입력으로 소비돼 두 번째 발화**를 만든다
#     (livekit/agents#4554 — 같은 모델·Vertex, 미해결). 실측 11턴 전부 두 번 말했다.
#   ⇒ 호출을 **발화의 일부**로 만들어야 턴이 안 쪼개진다. 커뮤니티에서 이 모델로 20개
#     함수를 안정화한 사례의 핵심 문장도 같은 것이다:
#     "You MUST generate BOTH the necessary function call(s) AND a brief, **simultaneous**
#      spoken acknowledgement."
#
#   ⚠ "첫 인사 턴 금지"는 두 출처가 독립적으로 같은 말을 해서 넣었다:
#     · 커뮤니티: "첫 상호작용의 함수 호출은 항상 실패, 대화 한 번 뒤엔 95% 성공"
#     · adk#4902: "툴이 붙은 인사 턴에서 중복률 100%, 나머지 툴 턴은 47~80%"
#     우리 실측도 set_face #1 이 인사 턴에서 발동해 두 번 말했다.
#
#   ⚠ 낱말을 "도구"→"함수"로 통일했다. 근거는 일화뿐이지만(커뮤니티: "'tool' 을
#     'function' 으로 바꾸자 호출이 나오기 시작했다") 비용이 0이다.
#   ⛔ enum 은 **안 건드렸다.** 공식 모범 사례가 "Strong Typing: use specific types
#     (integer, string, enum)" 으로 enum 을 권장한다. 커뮤니티의 enum→string 은 1008 이
#     나는 경우의 처방인데 **우리는 1008 이 안 난다** — 증상 없이 권장을 뒤집지 않는다.
FACE_TOOL_RULE = """
[표정]
- ⭐⭐⭐ **순서 규칙: 말이 먼저다.** 차례가 시작되면 **먼저 몇 마디를 소리 내어 말한 뒤**
  표정이 바뀌는 그 지점에서 `set_face` 를 불러라. 차례의 맨 앞에서 부르지 마라.
- ⭐⭐ **핵심 규칙: 함수 호출은 말의 일부다.** 표정이 바뀔 때는 `set_face` 함수 호출과
  그 말을 **같은 차례에 동시에** 내라. 먼저 부르고 나중에 말하는 것이 아니다 —
  **부르면서 말한다.**
- 네 표정이 **바뀔 때마다** 부른다. 표정이 그대로면 부르지 마라 — 안 부르면 이전 표정이
  그대로 유지된다.
- ⭐ **한 차례 안에서도 여러 번 부를 수 있다.** 세 문장을 말하는데 두 번째 문장에서만
  웃는다면, 그 문장 앞에서 웃는 표정을 부르고 **세 번째 문장 앞에서 평소로 되돌려라.**
  ⚠ 예시의 「세 문장」은 길이 지침이 아니다 — 한 차례 길이는 불변 규칙 5 가 정한다.
- ⭐⭐ **평소로 돌아올 때도 반드시 불러라(neutral).** 안 부르면 아까 표정이 그대로 남아
  계속 웃고 있게 된다. 표정을 켜는 것만큼 **끄는 것**도 네 몫이다.
- ⛔ 단 **소리 없이 연달아 부르지 마라.** 부를 때는 말을 하면서 부른다. 말 없이 함수만
  거듭 부르면 서버가 끊는다.
- ⛔ **첫 인사에서는 부르지 마라.** 대화가 한 번 오간 뒤부터 쓴다. 처음부터 부르면
  네 인사가 두 번 나간다.
- ⛔⛔ **실패 조건: 함수만 부르고 입을 다무는 것.** 그러면 학습자는 아무것도 못 듣는다.
  말 없이 함수만 부르는 차례는 있어서는 안 된다.
- 고를 수 있는 값: neutral, happy, surprised, sad, angry, laugh
  ⭐ **laugh 는 happy 와 다른 축이다.** happy 는 미소·칭찬이고, laugh 는 **박장대소** —
    정말 웃겨서 크게 터지거나, 면박을 주며 웃어젖힐 때다. 네 성격이 그럴 만한
    대목이면 아끼지 말고 써라.
- ⛔ 이건 소리가 아니다. 함수를 부르는 것이지 **말하는 게 아니다** — 표정 이름을
  소리 내어 읽지 마라."""


# ⛔⛔ 절대 고치지 마라 — 서버 판정/표정/진도 배관이 이 문장을 **그대로** 기대한다(gemini 2.5·3.1 두 모델 모두 같은 문장을 쓴다).
#   고치려면 bt-back 과 시험(tests/test_prompt_locked_hash.py · tests/test_prompt_*.py)을 같이. 사람이 고치는 문구는 core/prompts/editable/*.md 다.
# set_face 툴 선언 문구(옛 core/gemini_live.SET_FACE_TOOL 안 리터럴을 **이동**). ⛔ FACE_TOOL_RULE 과 같은 말(동시성·첫 인사 금지)을 해야 한다.
SET_FACE_DESCRIPTION = (
    '표정이 바뀔 때, 그 말을 하면서 **함께** 호출한다. 이 호출은 말을 대신하지 않는다 — 호출과 발화를 같은 차례에 동시에 낸다. 표정이 그대로면 호출하지 않는다. 첫 인사에서는 호출하지 않는다.'
)
SET_FACE_EMOTION_DESCRIPTION = (
    '지금부터의 표정. neutral=평소로 돌아옴, happy=기쁨·칭찬(미소), surprised=예상 밖의 말에 놀람·감탄, sad=안타까움·아쉬움, angry=화남·짜증, laugh=박장대소. 크게 터져 웃는다 — 정말 웃겨서, 또는 면박을 주며 웃어젖힐 때. happy 의 미소와 다른 축이다.'
)
#: enum 값 — 클라 아바타 어휘와 같은 축(커밋 d1139d8 «매핑 계층을 만들지 않는다»). 연구실(neutral 제거)은 이 한 줄에서.
SET_FACE_EMOTIONS: tuple[str, ...] = ('neutral', 'happy', 'surprised', 'sad', 'angry', 'laugh')
