# -*- coding: utf-8 -*-
"""표현학습 문자열 정규화 — **판정하지 않는다.** (기획 §2-6 재설계, 2026-09-10)

## ⛔⛔ 이 모듈은 «퀴즈 통과» 를 결정하지 않는다 (사장님 결정)

한때 여기에 3갈래 판정기가 있었다 — 정확일치→통과 / 공통 어절 0→오답 / 그 사이→LLM.
**그 설계를 통째로 걷어냈다.** 문자열에 통과 결정권을 주면 두 가지가 반드시 따라온다:

  ① **포함 통과가 진도를 부풀린다.** 정답 `물` 에 "저는 물을 좋아해요" 가 통과했다.
     L2~L13 커리큘럼의 90%가 어휘(낱말 1개)라, 그 낱말이 든 아무 문장이나 통과가 된다.
     길이 상한으로 막아 봤지만 그건 증상 처방이었다.
  ② **«따라 말한 것» 과 «맞힌 것» 을 문자열로는 못 가른다.** 드릴이 «들려주고 따라 말하게»
     이므로 학습자가 항목을 그대로 말하는 순간이 통화에 수없이 많다. 직전 비버 발화를 보는
     앵무새 관문을 넣어 봤지만:
        1턴 창 → 재시도 경로가 샌다(B2 가 "다시 해봐" 면 표면형이 없다)
        2턴 창 → **정상 퀴즈가 막힌다**(B1 이 드릴이면 B2 퀴즈 정답이 차단된다)
     ⇒ 고정 N턴으로는 드릴과 퀴즈를 가를 수 없다. 필요한 것은 «지금 몇 번 문항의 어느
       단계인가» 인데, 문자열은 그걸 볼 수 없다.

⇒ (2026-09-10) 판정은 LLM 이, 쓰기는 파이썬이 — 로 한 번 갔다가,
⇒ **(2026-09-11 T16) 퀴즈 창을 서버가 연다.** 그러자 위 ①②가 사라졌다 — ①은 창 안 quiz_set 항목만 보니 «아무 문장»
  이 통과할 자리가 없고, ②는 드릴이 창 밖이라 복창을 가를 필요가 없다. 그래서 창 **안에서는** 서버가 문자열로
  passed/failed 를 찾는다(`call_session._server_judge_quiz` — 첫 사건 규칙: U 표면형+격식 → passed, B 표면형 → failed).
  ⛔ 그래도 이 모듈은 여전히 **판정하지 않는다** — «이 발화에 그 표현이 있나»(`mentions`)·«격식 표지가 있나»
  (`keeps_formality`) 라는 사실 확인만 준다. 창을 열고 닫고 사건 순서를 읽는 것은 call_session 의 상태기계다.
  창 밖(드릴)에서 이 함수들로 통과를 만들지 마라 — 그 순간 ①②가 돌아온다.

## 그래서 여기 남은 것
서버가 여전히 문자열로 하는 일은 **판단이 아니라 사실 확인** 하나다 —
«비버가 이 문장을 말했나»(`call_session._note_covered_items`). 그 대조를 전사 잡음
(띄어쓰기·문장부호)에 안 흔들리게 하는 정규화만 여기 남는다.

⚠ 실제 호출자는 그 한 곳이고, **표현학습 경로에서만** 켜진다 — `normal` 통화는 생짜
  비교 그대로다(정규화를 켜면 그쪽 covered 검출 폭이 넓어져 재접지 쪽지가 바뀐다).

⛔ 이 모듈은 DB·LLM·도메인 모델을 모른다(어댑터 순수성). 표준 라이브러리만 쓴다 —
  `tests/test_quiz_judge.py` 가 **import 그래프로** 그걸 잠근다(소스 grep 이 아니다).
"""

from __future__ import annotations

import re
import unicodedata

# 걷어낼 것: 공백류 + 문장부호. ⛔ 한글 자모를 건드리지 않는다.
# ⚠ `\w` 를 쓰지 않는다 — 로케일에 따라 한글 포함 여부가 흔들린다. 버릴 것을 직접 적는다.
_PUNCT_RE = re.compile(r"[\s.,!?;:~…·\"'“”‘’()\[\]{}<>«»\-–—/\\|]+")


def normalize(text: str | None) -> str:
    """비교용 정규화 — **공백·문장부호만** 걷어낸다(NFC 정규화 포함).

    ⚠ NFC 를 거는 이유: 전사가 자모 분해형(NFD)으로 올 수 있고, 그러면 눈에 같은 글자가
      코드포인트로 달라 대조가 **조용히** 실패한다. macOS 계열 입력·일부 STT 의 실측 함정이다.
    ⛔ **조사·어미를 잘라내지 마라.** `안녕히 가세요` 와 `안녕히 계세요` 는 두 글자 차이인데
      **뜻이 반대다**(가는 사람에게 / 남는 사람에게) — 어미를 지우면 두 문장이 **같아진다.**
      지금은 판정을 안 하니 당장 사고가 나진 않지만, 이 함수를 쓰는 대조가 곧바로 틀린다.
    ⚠ 소문자화는 한다(라틴 문자 답변·영어 라벨 대비).
    """
    s = unicodedata.normalize("NFC", (text or "").strip())
    s = _PUNCT_RE.sub("", s).lower()
    # ⭐ T17-3 (통화 1405 ①, 매 통화 재현): STT 가 「어디예요」 를 「어디에요」 로 적는다 — 의미 불변 철자 변이라
    #   양쪽을 같은 꼴로 맞춘다(비교 전용 정규화라 저장본은 안 바뀐다). ⛔ 다른 등가를 여기 늘리지 마라 —
    #   「가세요/계세요」 처럼 뜻이 다른 쌍이 같아지는 순간 판정이 틀린다(bt-back: 이 1건만).
    return s.replace("에요", "예요")


# 한 어절 항목 뒤에 붙어도 «같은 낱말» 로 보는 꼬리(조사·어미). 빈 문자열 = 정확 일치.
# ⛔ **함부로 늘리지 마라.** 이 목록이 길어질수록 「개나리」류 접두 결합어가 다시 새어 들어온다.
#   늘릴 일이 생기면 그 낱말이 **오검출을 만들지 않는지** 먼저 시험으로 확인해라.
# ⚠ 활용 어미(-었어요·-습니다…)는 일부러 **안 넣었다** — 그걸 넣으면 어간 추출이 되고,
#   그건 이 모듈이 하지 않기로 한 일이다(모듈 독스트링).
_PARTICLES = frozenset({
    "", "은", "는", "이", "가", "을", "를", "에", "의", "도", "만",
    "과", "와", "로", "으로", "에서", "에게", "까지", "부터", "랑", "이랑",
    "요", "이요", "야", "아",
})


def mentions(text: str | None, label: str | None) -> bool:
    """전사에 그 항목이 **실제로 나왔나**(대조 전용 — 판정이 아니다).

    ## ⛔⛔ 부분문자열 하나로 끝내지 마라 — 이 설계가 이미 이름 붙인 실패다
    옛 대조는 `label in text` 였다. 그러면:

        항목 「물」  ·  학습자 발화 "어제 **선물**을 받았어요"   → 다뤘다로 잡힌다  ⛔

    ⇒ 그 항목이 «완료» 로 처리돼 **가르치지도 않고 건너뛰고**, DB 엔 drilled 로 남는다.
      L2 이상 커리큘럼의 90%가 어휘이고 그중 대부분이 1~2글자라, 여기가 **주 무대**다.
    ⚠⚠ 같은 결함을 이미 한 번 걷어냈다 — «정답 `물` 에 "저는 물을 좋아해요" 가 통과» 는
      **통과(pass) 경로**의 이야기였고, 그때 그 경로만 고치고 **대조(covered) 경로에는
      그대로 남겨 뒀다.** 두 경로가 같은 결함을 나눠 갖고 있었다.

    ## 그래서 낱말 경계를 요구한다
    · 여러 어절 항목(「안녕히 가세요」)  → 정규화 후 **부분문자열**. 어절 경계가 이미 있다
    · 한 어절 항목(「물」·「가다」)      → 어절이 그 항목으로 시작하고, **남는 꼬리가
      조사·어미**여야 한다(`_PARTICLES`)

    ## ⛔⛔ `startswith` 하나로 끝내지 마라 — 세 번째 되살아남을 막는 자리다
    이 결함은 이미 **두 번** 모양을 바꿔 돌아왔다(통과 경로 → 대조 경로 → **접두 결합어**).
    `startswith` 만 쓰면 뒤에 붙는 「선물」은 막지만 **앞에 붙는 것은 그대로 통과한다**:

        label「개」 ← "**개나리**가 피었어요"   → 어절이 「개」로 시작한다  ⛔ 오검출

    흔하다: 개→개나리·개인·개월·개발 / 눈→눈물 / 물→물건 / 말→말씀…
    ⇒ 라벨을 뗀 **꼬리가 조사·어미 목록에 있을 때만** 참으로 본다.
        「물을」→꼬리「을」 ✅   「물」→꼬리「」(정확일치) ✅
        「눈물이」→꼬리「물이」 ⛔   「개나리가」→꼬리「나리가」 ⛔

    ⚠ 동사·형용사는 활용이 **어미를 갈아치우므로**(들다→들었어요) 여기서 안 잡힌다. 그건
      **미검출**이고 안전한 방향이다 — 원형 그대로 나온 「가방을 들다」만 잡히면 된다.
      ⛔ 활용형까지 잡으려 들지 마라. 그 욕심이 정확히 오검출을 부른다.
    ⚠ **미검출은 안전하고 오검출은 아니다** — 못 잡으면 한 번 더 가르치면 되지만, 잘못 잡으면
      **가르칠 기회가 영영 사라진다**(`_note_covered_items` 독스트링의 보수성 규율 그대로).
    """
    lab = normalize(label)
    if not lab:
        return False
    raw = (text or "").strip()
    if not raw:
        return False
    # ⭐ T16 — 자리표시·교체·대괄호가 든 표면형(「저는 ◯◯ 사람이에요」「N이/가 있어요[없어요]」)은 부분문자열로는
    #   **영원히** 거짓이다 → covered 가 안 돼 3개가 안 차고 퀴즈 큐 자체가 안 열린다. 템플릿 대조로 간다
    #   (`template_mentions`). 자리표시가 없는 라벨은 아래 기존 경로 그대로다.
    if is_template(label):
        return template_mentions(raw, label)
    if len((label or "").split()) > 1:
        return lab in normalize(raw)
    for word in raw.split():
        w = normalize(word)
        if w.startswith(lab) and w[len(lab):] in _PARTICLES:
            return True
    return False


#: 예문 OR 의 최소 길이(정규화 후 글자 수). 「네.」「좋아요.」 같은 짧은 예문은 대화 어디서나 나와 오검출이 된다.
EXAMPLE_MIN_CHARS = 4


def item_mentioned(text: str | None, surface: str | None, example: str | None = None) -> bool:
    """항목이 **표면형으로든 그 예문으로든** 발화에 나왔나(대조 전용 — 판정이 아니다). 2026-09-12 판정 보강(bt-back 지시).

    ## 왜 예문까지 보나
    문법 항목의 표면형은 「V-아요/어요」「N은/는 N이에요/예요」 같은 **주형**이라 발화에 그대로 나올 수 없다 — `mentions` 의
    템플릿 대조(V2)가 자리표시를 와일드카드로 풀어도 「V-아요/어요」 는 고정 조각이 «아요|어요» 하나뿐이어서 사실상 아무 문장에나
    붙거나(어미 하나) 전혀 안 잡힌다(슬래시 앞이 하이픈 결합). 그런데 비버는 드릴할 때 **예문을 그대로 인용**하고(「나무가 타요.」)
    학습자도 그 예문을 따라 말한다 ⇒ 예문이 발화에 통째로 들어 있으면 그 항목을 다룬 것이다.

    ## 규칙(보수적)
    · `mentions(text, surface)` 가 참이면 참(기존 경로 그대로 — 어휘·청크는 여기서 끝난다).
    · 아니면 예문을 **정규화 부분문자열**로 본다. 단 정규화한 예문이 `EXAMPLE_MIN_CHARS`(4) 글자 미만이면 안 본다 —
      「네.」「가요.」 는 어디서나 나온다. 예문은 문장이라 어절 경계가 이미 있어 「선물」류 접두·접미 결합 오검출이 없다.
    · 예문이 None/빈 문자열이면 표면형만(옛 동작과 동일).
    ⛔ 판정(passed)에 쓸 때 격식(`keeps_formality`)은 **표면형** 기준 그대로다 — 예문이 맞았다고 격식 검사를 건너뛰지 않는다.
    """
    if mentions(text, surface):
        return True
    ex = normalize(example)
    if len(ex) < EXAMPLE_MIN_CHARS:
        return False
    return ex in normalize(text)


# --------------------------------------------------------------------------- #
# T16 — 파이썬 검증(V2·V4)의 순수 함수. ⛔ 판정이 아니다 — LLM 이 «passed» 라고 **제안한** 것을
#   코드가 사실로 확인한다(레벨 시스템 관통원칙 ①: AI 는 증인, 판정은 코드). 이 함수들이 거짓이면
#   그 항목은 **미판정**으로 남는다(failed 로 바꾸지 않는다 — 침묵 ≠ 오답).
# --------------------------------------------------------------------------- #
# 격식 표지 — 긴 것부터(«주세요» 가 «세요» 보다, «세요» 가 «요» 보다 앞). 표면형이 이걸로 끝나면 정중형이다.
# bt-back 확정(T16 codex P1-3): 요(어요·아요·예요·세요·주세요 전부) / 니다(습니다·ㅂ니다) / 십시오 / 죠.
_POLITE_MARKERS = ("니다", "십시오", "죠", "요")
# 절 경계 — 문장부호로 가른다(normalize 는 부호를 지우므로 그 전에 갈라야 «절의 끝» 이 남는다).
_CLAUSE_SPLIT_RE = re.compile(r"[.!?…~,;:]+")


def polite_marker(surface: str | None) -> str | None:
    """표면형 **마지막 어절의 종결 표지**(«요»·«니다»·«십시오»·«죠»), 없으면 None(명사·반말 항목·자리표시 주형).

    ⚠ «◯◯이/가 뭐예요?» 처럼 자리표시·슬래시가 섞여도 **끝**만 본다 — 문장부호는 normalize 가 걷어낸다.
    ⚠ 대괄호 대안(«가요[와요]»)은 떼고 본다 — 주형의 끝이 기준이다.
    """
    s = normalize(_BRACKET_RE.sub("", surface or ""))
    for m in _POLITE_MARKERS:
        if s.endswith(m):
            return m
    return None


def keeps_formality(text: str | None, surface: str | None) -> bool:
    """V4 — 표면형이 정중형이면 학습자 발화의 **어느 어절이 그 표지로 끝나야** 한다.

    1397 «얼마야?»·«나는 미국 사람» / 1398 t9 「잘 못 들었다」 가 정답 반응을 받았다(B 유형). 지시문이
    «반말은 passed 가 아니다» 라고 해도 LLM 이 어겼다 — 그래서 코드가 한 번 더 본다.
    · 표면형이 반말/명사형이면 항상 참(볼 표지가 없다).
    · «요» 표지는 **어절 끝**만 본다 — «요리»·«필요한» 의 «요» 는 표지가 아니다.
    """
    m = polite_marker(surface)
    if m is None:
        return True
    raw = text or ""
    # ⭐ T18 (통화 1407 #7) — STT 가 「만나서 반갑습니 다.」 처럼 **어절 안에 공백**을 넣는다. 원문 어절만 보면 어느 어절도
    #   «니다» 로 끝나지 않아 자발 정답이 반말로 기각됐다. 그래서 세 겹으로 본다 — 원문 어절 끝 / 문장부호로 가른 절을
    #   normalize(공백 제거·NFC)한 끝 / 전체를 normalize 한 끝. 어느 하나라도 표지로 끝나면 참.
    #   ⚠ «요» 는 여전히 **끝**만 본다 — 「요리 좋아」 의 «요» 는 어디로 봐도 끝이 아니다.
    candidates = list(raw.split()) + _CLAUSE_SPLIT_RE.split(raw) + [raw]
    for c in candidates:
        if normalize(c).endswith(m):
            return True
    return False


# --------------------------------------------------------------------------- #
# T16 V2 — **템플릿 인식 대조**. 표면형에 자리표시·교체·대괄호가 있으면 `mentions` 로는 영원히 거짓이다:
#   「저는 ◯◯ 사람이에요」 ← "저는 미국 사람이에요"     정규화 부분문자열 «저는◯◯사람이에요» 는 어디에도 없다
#   「◯◯이/가 뭐예요?」    ← "이게 뭐예요?"
#   「N이/가 있어요[없어요]」← "물이 있어요"               (grammar.json 459 중 21 건이 대괄호, N/V/A 자리표시는 대부분)
#   ⇒ V2 가 구조적으로 거짓 → 영구 미판정 → 그 레벨 전량 통과(D12)가 성립 불가(P0-3 과 같은 구멍).
# 규칙: 자리표시 = 와일드카드 · 「이/가」류 교체 = (이|가) · 대괄호 [없어요] 는 **제거**(주형만 인정 — 「없어요」 로
#   답하면 미판정, 안전한 방향) · 남는 고정 조각이 정규화 발화에 **순서대로** 있어야 참.
# ⛔ 이 함수도 판정이 아니다 — LLM 이 passed 라고 제안한 세그먼트에 그 표현이 «실제로 있나» 만 본다.
# --------------------------------------------------------------------------- #
_BRACKET_RE = re.compile(r"\[[^\]]*\]")
# 자리표시: ◯◯(개수 무관) · 홀로 선 라틴 대문자 토큰(N · V · A · Adj · S · N1 …) · «(누구)» 류 괄호 안 안내
# ⚠ `` 를 쓰지 않는다 — 파이썬 유니코드 `` 는 한글도 단어 문자라 「N이」 의 N 뒤에 경계가 없다(라틴 자리표시가
#   한글에 바로 붙는 게 커리큘럼 표기의 기본꼴이다). 라틴 글자에 안 둘러싸인 대문자 토큰이면 자리표시다.
_PLACEHOLDER_RE = re.compile(r"◯+|(?<![A-Za-z])[A-Z][A-Za-z]{0,3}\d?(?![A-Za-z])|\([^)]*\)")
# 조사 교체: 이/가 · 을/를 · 은/는 · 와/과 · 아/어 · (으)로 … — 슬래시 양쪽이 한글 1~2자
# ⚠ 최대 3자 — 「N이에요/예요」 는 «이에요 | 예요» 교체다(2자로 자르면 «에요/예요» 가 되어 「사과예요」 를 놓친다).
_ALT_RE = re.compile(r"([가-힣]{1,3})/([가-힣]{1,3})")


def is_template(surface: str | None) -> bool:
    """표면형에 자리표시·교체·대괄호가 있나 — 있으면 `mentions` 대신 `template_mentions` 를 써야 한다."""
    s = surface or ""
    return bool(_BRACKET_RE.search(s) or _PLACEHOLDER_RE.search(s) or _ALT_RE.search(s))


def template_mentions(text: str | None, surface: str | None) -> bool:
    """학습자 발화에 템플릿 표면형이 **채워진 꼴로** 들어 있나. 템플릿이 아니면 `mentions` 와 같다.
    ⚠ `mentions` 가 템플릿이면 여기로 넘긴다 — covered 경로와 V2 가 **한 함수**다(두 경로가 갈리면 covered 는
      되는데 V2 는 안 되는 항목이 생긴다)."""
    if not is_template(surface):
        return mentions(text, surface)
    src = _BRACKET_RE.sub("", surface or "")
    # 토큰화: 자리표시(와일드카드) / 교체 / 고정 글자 를 순서대로 정규식 조각으로
    parts: list[str] = []
    pos = 0
    for m in sorted(list(_PLACEHOLDER_RE.finditer(src)) + list(_ALT_RE.finditer(src)), key=lambda m: m.start()):
        if m.start() < pos:
            continue
        fixed = normalize(src[pos:m.start()])
        if fixed:
            parts.append(re.escape(fixed))
        if _ALT_RE.fullmatch(m.group(0)):
            a, b = normalize(m.group(1)), normalize(m.group(2))
            alt = "(?:%s|%s)" % (re.escape(a), re.escape(b))
            # 한 글자 조사 교체(이/가·을/를·은/는·와/과)는 **선택**으로 — 학습자는 「이게 뭐예요」(이것+이 축약)처럼
            # 조사를 붙이거나 생략하거나 축약한다. 옳고 그름은 판정기 몫이고, 여기는 «그 표현이 있나» 만 본다.
            if len(a) == 1 and len(b) == 1:
                alt += "?"
            parts.append(alt)
        else:
            parts.append(".*?")
        pos = m.end()
    fixed = normalize(src[pos:])
    if fixed:
        parts.append(re.escape(fixed))
    fixed_parts = [p for p in parts if p != ".*?"]
    if not fixed_parts:
        return False                       # 전부 자리표시 — 확인할 글자가 없다(미판정)
    pattern = "".join(parts)
    # 앞뒤 와일드카드는 의미가 없다 — 그냥 부분문자열 탐색이다
    while pattern.startswith(".*?"):
        pattern = pattern[3:]
    while pattern.endswith(".*?"):
        pattern = pattern[:-3]
    return re.search(pattern, normalize(text)) is not None
